---
layout: ../../layouts/Layout.astro
title: "Getting Started with C64 Development"
description: "Set up your development environment for Commodore 64 assembly programming. Install VICE emulator, ACME assembler, and learn the edit-compile-run workflow."
---

<style>{`
  .setup-content {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-8) var(--space-4);
  }

  .breadcrumb {
    font-size: 0.875rem;
    margin-bottom: var(--space-6);
    color: var(--color-text-secondary);
  }

  .breadcrumb a {
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: color 0.2s;
  }

  .breadcrumb a:hover {
    color: #4a4dff;
  }

  .breadcrumb .separator {
    margin: 0 var(--space-2);
    opacity: 0.5;
  }

  .breadcrumb .current {
    color: #4a4dff;
    font-weight: 500;
  }

  .setup-content h1 {
    font-family: var(--font-family-mono);
    font-size: 2.5rem;
    margin-bottom: var(--space-4);
    color: var(--color-text);
  }

  .setup-content h2 {
    font-size: 1.5rem;
    margin-top: var(--space-8);
    margin-bottom: var(--space-4);
    color: var(--color-text);
    border-bottom: 2px solid var(--color-border);
    padding-bottom: var(--space-2);
  }

  .setup-content h3 {
    font-size: 1.25rem;
    margin-top: var(--space-6);
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .setup-content p {
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-4);
  }

  .setup-content ul, .setup-content ol {
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-4);
    padding-left: var(--space-6);
  }

  .setup-content li {
    margin-bottom: var(--space-2);
  }

  .setup-content code {
    font-family: var(--font-family-mono);
    background: var(--color-surface);
    padding: 0.2em 0.4em;
    border-radius: 0.25rem;
    font-size: 0.9em;
  }

  .setup-content pre {
    background: #1e293b;
    border: 1px solid #334155;
    border-radius: 0.5rem;
    padding: var(--space-4);
    overflow-x: auto;
    margin-bottom: var(--space-4);
  }

  .setup-content pre code {
    background: none;
    padding: 0;
    color: #22c55e;
  }

  .setup-content a {
    color: #4a4dff;
    text-decoration: none;
  }

  .setup-content a:hover {
    text-decoration: underline;
  }

  .tool-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    padding: var(--space-6);
    margin-bottom: var(--space-4);
  }

  .tool-card h3 {
    margin-top: 0;
    color: #4a4dff;
  }

  .platform-tabs {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-4);
    flex-wrap: wrap;
  }

  .platform-tab {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 0.25rem;
    padding: var(--space-2) var(--space-4);
    font-family: var(--font-family-mono);
    font-size: 0.875rem;
  }

  .next-step {
    background: linear-gradient(135deg, #4a4dff 0%, #7c70da 100%);
    border-radius: 0.5rem;
    padding: var(--space-6);
    margin-top: var(--space-8);
    text-align: center;
  }

  .next-step h2 {
    color: white;
    border: none;
    margin-top: 0;
  }

  .next-step p {
    color: rgba(255, 255, 255, 0.9);
  }

  .next-step a {
    display: inline-block;
    background: white;
    color: #4a4dff;
    padding: var(--space-3) var(--space-6);
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    margin-top: var(--space-4);
  }

  .next-step a:hover {
    text-decoration: none;
    opacity: 0.95;
  }
`}</style>

<div class="setup-content">

<nav class="breadcrumb">
  <a href="/">Home</a>
  <span class="separator">/</span>
  <a href="/commodore-64">Commodore 64</a>
  <span class="separator">/</span>
  <span class="current">Getting Started</span>
</nav>

# Getting Started

You'll need two things: a way to compile your assembly code, and an emulator to run the results. Our Docker toolchain handles compilation; you'll install the emulator natively for the best experience.

## Docker Toolchain (Recommended)

Our Docker image includes ACME assembler, petcat for BASIC, and build tools — everything needed to compile C64 programs. No dependency management, no version conflicts.

<div class="tool-card">

### Quick Start

```bash
docker pull code198x/commodore-64
```

That's it. To compile a program:

```bash
docker run --rm -v "$(pwd)":/code code198x/commodore-64 \
  acme -f cbm -o game.prg game.asm
```

Your compiled `.prg` file appears in your current directory, ready to run.

</div>

## The Emulator: VICE

VICE runs your compiled programs. Install it natively on your system for smooth, responsive testing.

<div class="tool-card">

### Installing VICE

**macOS (Homebrew)**
```bash
brew install vice
```

**Windows**

Download from [vice-emu.sourceforge.io](https://vice-emu.sourceforge.io/). Extract the ZIP and add the folder to your PATH, or run `x64sc.exe` directly.

**Linux (Debian/Ubuntu)**
```bash
sudo apt install vice
```

**Linux (Fedora)**
```bash
sudo dnf install vice
```

</div>

The executable you'll use is `x64sc` — that's the accurate C64 emulator. (There's also `x64` which is faster but less accurate. Stick with `x64sc` for development.)

### First Run

Launch VICE to make sure it works:

```bash
x64sc
```

You should see the familiar C64 blue screen with `READY.` and a blinking cursor. Press `Alt+Q` (or `Cmd+Q` on macOS) to quit.

## The Workflow

Every change follows the same cycle:

1. **Edit** your `.asm` file in any text editor
2. **Compile** with Docker to produce a `.prg` file
3. **Run** the `.prg` in VICE

Here's what that looks like:

```bash
# Compile
docker run --rm -v "$(pwd)":/code code198x/commodore-64 \
  acme -f cbm -o game.prg game.asm

# Run
x64sc game.prg
```

The `-f cbm` flag tells ACME to output in Commodore format with the correct load address header.

## Editor Setup (Optional)

Any text editor works. If you want syntax highlighting for 6502 assembly:

- **VS Code**: Install the "6502 Assembly" extension
- **Sublime Text**: Install "6502 Assembly" via Package Control
- **Vim**: The built-in asm syntax works reasonably well

Syntax highlighting isn't essential, but it makes reading code easier.

## Downloading vs Typing

Each lesson provides a download link for the complete `.prg` file. You can run these immediately to see what you're building.

But we recommend typing the code yourself. Assembly has details that matter — addressing modes, instruction order, the relationship between code and data. Typing forces you to notice these details in a way that copy-paste doesn't.

Start by downloading and running. Then type the code yourself and compare.

<span id="native"></span>

## Alternative: Native Assembler

If you prefer not to use Docker, you can install ACME directly on your system.

<div class="tool-card">

### Installing ACME

**macOS (Homebrew)**
```bash
brew install acme
```

**Windows**

Download from [sourceforge.net/projects/acme-crossass](https://sourceforge.net/projects/acme-crossass/). Extract and add to your PATH.

**Linux**

Most distributions have ACME in their repositories:
```bash
sudo apt install acme                    # Debian/Ubuntu
sudo dnf install acme-crossassembler     # Fedora
```

Or build from source — it's just a single C file with no dependencies.

</div>

With native ACME, the compile command simplifies to:

```bash
acme -f cbm -o game.prg game.asm && x64sc game.prg
```

Both approaches produce identical `.prg` files.

## Troubleshooting

**VICE shows a blank screen or error**

Make sure you're using `x64sc`, not `x64`. If the screen stays black, the program might have crashed — check your code for errors.

**ACME reports "File not found"**

Check you're in the right directory. ACME looks for files relative to where you run it.

**ACME reports syntax errors**

The error message includes a line number. Assembly is unforgiving — check for typos, missing colons after labels, and correct instruction spelling.

**Program runs but behaves wrong**

Welcome to debugging! The lessons include explanations of what each part does. Compare your code character-by-character with the lesson code.

<div class="next-step">

## Ready to Build

Your tools are set up. Time to write some code.

[Start Game 1: SID Symphony →](/commodore-64/assembly/game-01-sid-symphony)

</div>

</div>
