---
import Layout from "../../../layouts/Layout.astro";
import Hero from "../../../components/Hero.astro";
import VaultSearch from "../../../components/VaultSearch.astro";
import { vaultData } from "../../../data/vault-data.js";
import "../../../styles/vault.css";

// Filter to just people entries
const peopleData = vaultData.filter(entry => entry.c === 'people');
---

<Layout title="People - The Vault">
  <Hero
    title="People"
    subtitle="Pioneers, programmers, and visionaries who shaped computing history"
    description={`Discover ${peopleData.length} influential figures who created the hardware, software, and culture of the golden age of computing.`}
    variant="gradient"
    badge={{
      text: "👤 People",
      color: "#10b981",
      href: "/vault"
    }}
  />

  <div class="vault-category-container">
    <div class="container">
      <!-- Use the shared search component -->
      <VaultSearch
        data={peopleData}
        placeholder="Search people by name, role, company, or contributions..."
        showCategoryFilter={false}
        showTypeFilter={true}
        showTagFilter={true}
        showYearFilter={false}
        showStatusFilter={true}
      />

      <!-- Additional Information Section -->
      <section class="vault-category-info-section">
        <h2>About the People Collection</h2>
        <p>
          This collection celebrates the individuals who drove innovation in computing.
          From hardware engineers to game designers, from business leaders to demo scene
          legends, these are the people who made computing history.
        </p>

        <div class="vault-stats-grid">
          <div class="vault-stat-card">
            <span class="vault-stat-number">{peopleData.filter(p => p.t === 'engineer').length}</span>
            <span class="vault-stat-label">Engineers</span>
          </div>
          <div class="vault-stat-card">
            <span class="vault-stat-number">{peopleData.filter(p => p.t === 'executive').length}</span>
            <span class="vault-stat-label">Executives</span>
          </div>
          <div class="vault-stat-card">
            <span class="vault-stat-number">{peopleData.filter(p => p.t === 'designer').length}</span>
            <span class="vault-stat-label">Designers</span>
          </div>
          <div class="vault-stat-card">
            <span class="vault-stat-number">{peopleData.filter(p => p.t === 'programmer').length}</span>
            <span class="vault-stat-label">Programmers</span>
          </div>
        </div>
      </section>
    </div>
  </div>
</Layout>