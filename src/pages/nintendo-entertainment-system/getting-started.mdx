---
title: "Getting Started with NES Development"
description: "Set up your tools for NES 6502 assembly programming."
layout: "../../layouts/GettingStartedLayout.astro"
system: "NES"
systemSlug: "nintendo-entertainment-system"
accentColor: "#e30613"
---

## What You Need

To develop for the NES, you need two things: a way to **compile** your code, and an **emulator** to run it.

<div class="setup-grid">
<div class="setup-column">

### <span class="icon">⚙️</span> Dev Toolchain

Our Docker image includes ca65 assembler and ld65 linker from the cc65 suite.

#### Install

```bash
docker pull code198x/nintendo-entertainment-system
```

#### Compile

```bash
docker run --rm -v "$(pwd)":/code -w /code \
  code198x/nintendo-entertainment-system bash -c \
  "ca65 game.asm -o game.o && ld65 -C nes.cfg game.o -o game.nes"
```

NES builds require a linker config file (`nes.cfg`) — each lesson includes one.

</div>
<div class="setup-column">

### <span class="icon">🖥️</span> Emulator

Install **FCEUX** natively for the best experience.

**macOS**
```bash
brew install fceux
```

**Linux**
```bash
sudo apt install fceux
```

**Windows** — Download from [fceux.com](https://fceux.com/)

#### Run

```bash
fceux game.nes
```

</div>
</div>

<div class="next-step">

## Ready to Build

Your tools are set up. Time to write some code.

[Start with Assembly →](/nintendo-entertainment-system/assembly)

</div>
