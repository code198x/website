---
title: "High Score"
description: "Track and display the best score across games."
pubDate: 2026-01-04
game: 1
gameName: "Neon Nexus"
unit: 12
totalUnits: 16
system: "Nintendo Entertainment System"
tags: ["assembly", "6502", "hud", "persistence"]
nextLesson: "/nintendo-entertainment-system/assembly/game-01-neon-nexus/unit-13"
layout: "../../../../layouts/UnitLayout.astro"
---

import CodeFromFile from "@components/CodeFromFile.astro";

*Need to catch up? Check out [Unit 11: Difficulty](/nintendo-entertainment-system/game-01-neon-nexus/unit-11).*

## What You're Building

Every arcade game has a high score—a target to beat, a reason to play "just one more game". Without a high score, there's no persistence between sessions, no goal beyond mere survival.

This unit adds:
- A high score variable that persists between games
- Comparison logic to check if current score beats the record
- High score display on the game over screen
- "NEW HIGH" notification when the record is broken

## The Plan

1. Add a `high_score` variable (16-bit like `score`)
2. Compare score to high score at game over
3. Update high score if beaten
4. Display high score on game over screen
5. Add "NEW HIGH" text when record is broken

## High Score Storage

Add the high score variable to zero page:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/01-high-score-storage.asm" lang="asm" />

Note: This high score only persists while the console is powered on. True persistence would require battery-backed RAM (SRAM), which is beyond this unit's scope.

## Initialising High Score

On first boot, set high score to 0:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/02-initialising-high-score.asm" lang="asm" />

Important: We do NOT reset high score in `init_game`. The high score persists across game sessions—only a power cycle resets it.

## Comparing Scores

At game over, we compare the current score to the high score. Since scores are 16-bit, we compare high bytes first, then low bytes if tied:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/03-comparing-scores.asm" lang="asm" />

The comparison logic:
1. If `score+1 > high_score+1`: new record
2. If `score+1 < high_score+1`: not new
3. If equal: compare low bytes
4. If `score > high_score`: new record

## Calling the Check

Call `check_high_score` when entering game over state:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/04-calling-the-check.asm" lang="asm" />

## Displaying High Score

Modify `draw_game_over` to show the high score:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/05-displaying-high-score.asm" lang="asm" />

## Adding New Letter Tiles

We need tiles for 'H', 'I', and 'W' that we haven't used yet. Add these to the CHR-ROM:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/06-adding-new-letter-tiles.asm" lang="asm" />

And add the tile graphics:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/07-adding-new-letter-tiles.asm" lang="asm" />

## Update Letter Tile Constants

Update the tile index list:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/08-update-letter-tile-constants.asm" lang="asm" />

## Clearing More Screen Area

Since we're showing more text on game over, update `clear_playfield` to clear more rows:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/snippets/09-clearing-more-screen-area.asm" lang="asm" />

## Build It. Run It.

Assemble and run:

```bash
ca65 nexus.asm -o nexus.o
ld65 -C nes.cfg nexus.o -o nexus.nes
```

Test the high score:
1. Play a game and get some points
2. Game over—you should see "HI" followed by your score
3. If this is your first game, you should also see "NEW"
4. Play again and score lower—high score remains, no "NEW"
5. Score higher—high score updates, "NEW" appears

## What You've Built

Your game now tracks the best performance. Players have a concrete goal: beat the high score. This simple addition transforms aimless play into competitive challenge.

## What You've Learnt

- **16-bit comparison**: Comparing multi-byte values byte-by-byte
- **Conditional flags**: Using flags to track state for later display
- **Persistent state**: Keeping data across game sessions (within power cycle)
- **Game over enhancement**: Expanding the end-game display

## The Complete Code

Here's the complete `nexus.asm` with high score tracking:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-12/nexus.asm" lang="asm" />

## Recap / Sanity Check

Before moving on, verify:

- [ ] High score starts at 0 on power-on
- [ ] Game over screen shows "HI" followed by the high score
- [ ] First game with any score shows "NEW"
- [ ] Lower score on second game shows high score but not "NEW"
- [ ] Higher score updates high score and shows "NEW"
- [ ] High score persists between games (without power cycling)

## Next Unit

[Unit 13: Polish](/nintendo-entertainment-system/game-01-neon-nexus/unit-13) — refine enemy behaviour and add visual variety.
