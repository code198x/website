---
title: "Title Screen"
description: "Create a title screen and complete game state machine."
pubDate: 2026-01-04
game: 1
gameName: "Neon Nexus"
unit: 9
totalUnits: 16
system: "Nintendo Entertainment System"
tags: ["assembly", "6502", "state", "flow"]
nextLesson: "/nintendo-entertainment-system/game-01-neon-nexus/unit-10"
layout: "../../../layouts/UnitLayout.astro"
---

import CodeFromFile from "@components/CodeFromFile.astro";

*Need to catch up? Check out [Unit 8: Lives](/nintendo-entertainment-system/game-01-neon-nexus/unit-08).*

## What You're Building

Every proper game starts with a title screen. It's the first thing players see—a moment to present your game before the action begins. We already have `STATE_PLAYING` and `STATE_GAMEOVER`. Now we add `STATE_TITLE` to create a complete three-state game flow.

This unit adds:
- A title screen showing the game name
- "PRESS START" prompt
- A game over message when lives run out
- Clean transitions between all three states

## The Plan

1. Add `STATE_TITLE` constant and start there on boot
2. Add letter tiles to CHR-ROM for text display
3. Create routines to draw title screen and game over text
4. Restructure the main loop to dispatch based on state
5. Handle button presses to transition between states

## The Three-State Machine

Our game now has three distinct states:

```
        [TITLE]
           |
       Press Start
           ↓
        [PLAYING] ←──────┐
           |              |
       Lose all lives     |
           ↓              |
       [GAMEOVER]         |
           |              |
       Press Start ───────┘
```

Update the constants to include the new state:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/01-the-three-state-machine.asm" lang="asm" />

On reset, we now start at `STATE_TITLE` instead of jumping straight into gameplay.

## Letter Tiles for Text

To display "NEON NEXUS", "PRESS START", and "GAME OVER", we need letter tiles in CHR-ROM. Each letter is an 8x8 pixel tile, just like digits.

We'll add tiles 14-39 for letters A-Z. The tile index for any letter is: `letter_ascii - 'A' + 14`. For our specific text, we only need: A, E, G, M, N, O, P, R, S, T, U, V, X.

Since we're writing directly to the nametable, we need to calculate PPU addresses. The NES nametable starts at $2000, with 32 tiles per row. To write text at row R, column C:

```
PPU address = $2000 + (R × 32) + C
```

For row 10, column 11 (roughly centred):
- $2000 + (10 × 32) + 11 = $2000 + 320 + 11 = $214B

## Drawing the Title Screen

The title screen draws text to the nametable during vblank. We only need to draw it once when entering the title state, not every frame.

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/02-drawing-the-title-screen.asm" lang="asm" />

Wait—that's getting complicated with manual tile indices. Let's simplify by defining our letter tiles starting at index 14 and using a helper table:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/03-drawing-the-title-screen.asm" lang="asm" />

Actually, for clarity in the code listing, we'll use direct byte values that match our CHR-ROM tile layout.

## Drawing Game Over Text

Similar to the title screen, game over draws text to the nametable:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/04-drawing-game-over-text.asm" lang="asm" />

## Clearing the Screen

When transitioning between states, we need to clear text from the previous state. A simple approach: clear a portion of the nametable when entering a new state.

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/05-clearing-the-screen.asm" lang="asm" />

## Restructured Main Loop

The main loop now dispatches based on `game_state`:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/06-restructured-main-loop.asm" lang="asm" />

## Tracking Screen State

One issue: we only want to draw the title or game over text once, not every frame. We can use a flag to track whether we've drawn the current state's screen:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/07-tracking-screen-state.asm" lang="asm" />

When changing states, set `screen_drawn = 0`. In each state handler, check this flag:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/08-tracking-screen-state.asm" lang="asm" />

## Initialisation Changes

On reset, we now start at the title screen:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/09-initialisation-changes.asm" lang="asm" />

## Transitioning to Game Over

When `lose_life` detects zero lives remaining, it sets the game over state and clears the screen_drawn flag:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/snippets/10-transitioning-to-game-over.asm" lang="asm" />

## Build It. Run It.

Assemble and run:

```bash
ca65 nexus.asm -o nexus.o
ld65 -C nes.cfg nexus.o -o nexus.nes
```

Test the complete flow:
1. Game boots to title screen showing "NEON NEXUS" and "PRESS START"
2. Press Start—title clears, gameplay begins
3. Play until you lose all lives
4. "GAME OVER" appears
5. Press Start—game over clears, new game begins with 3 lives

## What You've Built

Your game now has proper structure: a title screen that introduces the game, gameplay, and a game over screen that provides closure. This three-state pattern is the foundation of virtually every arcade game.

## What You've Learnt

- **State machines**: Organising game flow with distinct states
- **Nametable text**: Drawing text by writing tile indices to PPU
- **Screen management**: Drawing screens once and clearing when transitioning
- **State flags**: Tracking whether setup for a state has been done
- **PPU address calculation**: Converting row/column to nametable address

## The Complete Code

Here's the complete `nexus.asm` with title screen and game state machine:

<CodeFromFile src="nintendo-entertainment-system/game-01-neon-nexus/unit-09/nexus.asm" lang="asm" />

## Recap / Sanity Check

Before moving on, verify:

- [ ] Game starts at title screen showing "NEON NEXUS" and "PRESS START"
- [ ] No sprites visible on title screen
- [ ] Pressing Start transitions to gameplay
- [ ] HUD (score and lives) appears during gameplay
- [ ] Gameplay works as before (movement, collision, scoring)
- [ ] Losing all lives shows "GAME OVER"
- [ ] No sprites visible on game over screen
- [ ] Pressing Start from game over begins a new game

## Next Unit

[Unit 10: Sound Effects](/nintendo-entertainment-system/game-01-neon-nexus/unit-10) — add audio feedback using the APU.
