---
title: "Getting Started with C64 Development"
description: "Set up your tools for Commodore 64 programming."
systemSlug: "commodore-64"
accentColor: "#4a4dff"
---

## What You Need

To develop for the C64, you need two things: a way to **compile** your code, and an **emulator** to run it.

<div class="setup-grid">
<div class="setup-column">

### <span class="icon">⚙️</span> Dev Toolchain

Our Docker image includes ACME assembler and petcat for BASIC.

#### Install

```bash
docker pull code198x/commodore-64
```

#### Compile

**BASIC**
```bash
docker run --rm -v "$(pwd)":/code -w /code \
  code198x/commodore-64 petcat -w2 -o game.prg game.bas
```

**Assembly**
```bash
docker run --rm -v "$(pwd)":/code -w /code \
  code198x/commodore-64 acme -f cbm -o game.prg game.asm
```

</div>
<div class="setup-column">

### <span class="icon">🖥️</span> Emulator

Install **VICE** natively for the best experience.

**macOS**
```bash
brew install vice
```

**Linux**
```bash
sudo apt install vice
```

**Windows** — Download from [vice-emu.sourceforge.io](https://vice-emu.sourceforge.io/)

#### Run

```bash
x64sc game.prg
```

Use `x64sc` (accurate) rather than `x64` (faster but less accurate).

</div>
</div>

<div class="next-step">

## Ready to Build

Your tools are set up. Time to write some code.

[Start with BASIC →](/commodore-64/basic) · [Start with Assembly →](/commodore-64/assembly)

</div>
