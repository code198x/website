---
title: "Pickups and Score"
description: "Scatter pickups in a second colour, then teach the look-ahead PEEK a new trick: tell wall from pickup from empty by the character code it reads. Roll over a pickup, collect it, and the score climbs on a status row that never disturbs the maze."
pubDate: 2026-06-08
game: 7
unit: 4
tags: ["basic", "peek", "score", "game"]
---

import CodeFromFile from "@components/CodeFromFile.astro";
import Figure from "@components/Figure.astro";

A maze with nothing to collect is just a corridor. Scatter some pickups — bright dots in their
own colour — and give the rover a reason to explore. The clever part: the same look-ahead `PEEK`
that spots a wall can tell a *pickup* apart from a wall, just by the character it finds.

<CodeFromFile src="commodore-64/basic/rover/unit-04/pickups.bas" lang="commodore-basic" />

<Figure
  src="/images/commodore-64/basic/rover/unit-04/pickups.png"
  alt="A C64 maze with SCORE 10 on the top row, the rover mid-maze, and four cyan pickups scattered about."
  caption="Pickups scattered in cyan, a score on the status row. The rover has rolled over one — SCORE 10 — and four remain. The same PEEK that stops it at walls lets it through a pickup and counts it."
/>

The pickups are POKEd in lines 600–640 — character `81`, a round dot, in cyan (`3`) so they read
as "go here", distinct from the white walls. The new cleverness is in the collision check. Line
130 now *stores* what the look-ahead `PEEK` finds: `TC = PEEK(1024 + NR*40 + NC)`. Then line 140
blocks the move if `TC` is a wall (`160`), but line 150 does something new — if `TC` is a pickup
(`81`), it adds 10 to the score `SC` and lets the rover move in. Rolling onto the pickup draws the
rover over it, so it's collected and gone.

That's three outcomes from one read: wall (`160`) stops you, pickup (`81`) scores, anything else
is open space you move into freely. The score itself is drawn by the routine at line 700, which
POKEs `SCORE` and the number onto the top row — written straight to screen memory so updating it
never scrolls or disturbs the maze below. **Read the cell ahead, branch on what's there** — the
same pattern as walls, now doing more.

## Try this

- **Worth more.** Change the `SC = SC+10` on line 150 to `SC+25`, or give different pickups
  different values by POKEing them as different characters and testing for each.
- **A pickup tone.** Add a bright SID blip on collection (line 150) so a point is *heard* as well
  as seen — distinct from the bump tone, so your ear knows which happened.

## What's next

You can collect and score, but the game never ends — there's always another pickup, or none left
and nothing happens. In **Unit 5** counting the pickups down gives the game a finish.
