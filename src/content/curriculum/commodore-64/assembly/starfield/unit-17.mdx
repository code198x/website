---
title: "The Curve"
description: "Starfield works; nothing escalates. A wave counter, a speed table and a live fall speed put a difficulty curve in the game; a SID jingle gives the title a voice; and a dwell on both ends of the loop lets the endings land. Then the winnability gate does its work: the turret park — park under the lane, hold fire, never lose — is closed with a real LFSR spawn, full-field 9-bit enemies, and a hunt that leaves no square safe. And finally the game gets a curve worth playing: the swarm grows from three enemies to six over the waves, the hunt tightens late, and a four-digit score can hold a long game."
pubDate: 2026-06-12
game: 1
unit: 17
tags: ["assembly", "6510", "difficulty", "sid", "music", "game-feel", "randomness", "debugging"]
---

import CodeDiff from "@components/CodeDiff.astro";
import CodeFromFile from "@components/CodeFromFile.astro";
import Figure from "@components/Figure.astro";
import AudioClip from "@components/AudioClip.astro";

Unit 16 closed the loop, and called the game finished. Play it for five minutes and you'll find the two things the loop is missing. First: the fiftieth enemy falls exactly like the first — there is no *curve*, nothing that answers skill with pressure. Second: the machine is silent everywhere except mid-fight, and the title — the screen you stare at the longest — says nothing at all. This unit fixes both, and tucks in one piece of justice the state machine owed us from the start.

## Where we start

Unit 16's complete game: title, play, game over, round again. Three enemies fall at one pixel per frame, forever.

## Milestone 1 — the lanes quicken

A difficulty curve needs three small ideas, and they are worth keeping apart in your head:

- **`wave`** counts where you are — it starts at 1 and *never stops climbing*.
- **`wave_speed_tbl`** says what each wave means — one byte per wave, pixels of fall per frame.
- **`fall_speed`** is the live tuning — the one byte the enemy loop reads, every frame.

Ten kills end a wave. `advance_wave` resets the kill count, bumps the counter, looks up the new speed and pokes it into `fall_speed` — and that is the whole trick: **the enemies never learn that waves exist**. The drift code reads its tuning fresh each frame, and the tuning changed while it wasn't looking. No `if wave >= 3` scattered through the loop, no per-wave code at all.

Three different caps live in this milestone, and they are not the same thing. The wave *counter* never stops. The table *index* clamps at the last entry — past wave five the game repeats its hardest setting, because an unclamped index would read whatever byte happens to follow the table and call it a speed. And the HUD *digit* shows 9 forever after. The player's number tells the truth about how far they got; the physics quietly admits it ran out of new ideas. Conflate those caps and the game ends up lying about one of them.

The wave-up also *says so*: a bright triangle ding on voice 3, right over the explosion that earned it, and a `W` readout at the top of the screen — "I died on wave four" finally has a four.

<CodeDiff
  before="commodore-64/assembly/starfield/unit-17/steps/step-00.asm"
  after="commodore-64/assembly/starfield/unit-17/steps/step-01.asm"
  lang="6502"
  title="Step 1: a wave counter, a speed table, a live fall speed, and a readout"
  context={3}
/>

<details>
<summary>The complete program</summary>
<CodeFromFile src="commodore-64/assembly/starfield/unit-17/steps/step-01.asm" lang="6502" />
</details>

<Figure src="/images/commodore-64/assembly/starfield/unit-17/step-01-wave-a.png" alt="Mid-game: the white ship holding a column on the left, green enemies falling, the score and lives readouts at the corners and a white W2 wave readout at the top centre." caption="Wave two, mid-fight: the W readout at the top centre, and every enemy on screen falling twice as fast as it did a minute ago." />

Here is the moment the curve bends — the tenth kill's explosion with the wave-up chirp riding on top of it:

<AudioClip src="/images/commodore-64/assembly/starfield/unit-17/wave-up.wav" title="The tenth kill — explosion, chirp, and a faster game" source="SID voices 2 and 3 · noise and triangle" />

## Milestone 2 — the band, and a beat of stillness

The title music is the same table idea pointed at the SID. A melody is rows of *pitch* and *duration*; `jingle_tick` runs once per title frame, counts the current note down, and serves the next row when it expires. A pitch of zero is a **rest** — gate the voice off and keep counting, because silence is a note too — and `$ff` loops back to the top.

Notice how little the 6510 does. On the Spectrum, in Gloaming, a beeper tune *owns the processor* — every half-cycle of every note is a CPU loop, and the game stops while music plays. Here the SID generates the waveform between player calls. A sounding row makes four register writes: frequency high, frequency low, gate off and gate on. A rest writes gate off; other calls still count down the timer. The starfield can continue while the SID sounds, provided the game meets its update budget. Compare the [sequencer contracts](/patterns/cross-platform/audio/tune-sequencer) before adapting the table or timer. That is what a sound *chip* buys you over a sound *bit* — and the tune is Twinkle Twinkle, Little Star, because what else would a starfield play?

One catch worth meeting now: voice 1 already belongs to the laser. The title lends it to the band — `enter_title` installs a softer envelope, `enter_game` gates the voice off and hands the laser its envelope back. One voice, two owners, and the transitions are exactly where the handover lives. Miss half of it and the laser comes back sounding like a flute.

<AudioClip src="/images/commodore-64/assembly/starfield/unit-17/jingle.wav" title="The title jingle — one loop of Twinkle Twinkle on voice 1" source="SID voice 1 · triangle wave" />

The last change is the smallest and the most felt. In Unit 16, dying with the fire button held — which is how everyone dies — blew straight through the ending: game over read the held button, snapped to the title, the title read the same held button, and a new game started before the old one's death tone finished. Three screens in three frames. The fix is `ui_lock`: entering game over loads it with a second of frames, entering the title with half a second, and neither screen *believes* the fire button until the lock runs out. The pause is not dead time — it is the punctuation that lets you read your own ending.

<CodeDiff
  before="commodore-64/assembly/starfield/unit-17/steps/step-01.asm"
  after="commodore-64/assembly/starfield/unit-17/steps/step-02.asm"
  lang="6502"
  title="Step 2: a table-driven SID jingle, and a dwell on both ends of the loop"
  context={3}
/>

<details>
<summary>The complete program</summary>
<CodeFromFile src="commodore-64/assembly/starfield/unit-17/steps/step-02.asm" lang="6502" />
</details>

## Milestone 3 — the turret park, closed

Starfield looked finished at step 2. But *finished* has a second test on this course — the one the winnability gate asks of every game: can it be **lost** by a player who's actually trying to win? Drive Starfield the way a bored, clever player does, and it can't.

**Park the ship under the enemies' lane, hold fire, never move.** Every enemy funnels into your bullet stream and dies; you score freely and you are immortal. You needn't even aim — fly to the right third of the screen and *stop*, and enemies never reach there at all. Two ways to switch the danger off, and both are one bug wearing two coats.

The bug is where the enemies come from. The shipped spawn picks a column from the raster line:

```
        lda $d012           ; the raster line, as "randomness"
        and #$7f
        adc #$30            ; X = 48..175
```

It reads at *the same instant every time* — respawns fire at a fixed point in the frame, so the beam has barely moved — so every enemy appears in a band about a dozen pixels wide, **narrower than the 16-pixel hit box**. One parked, firing ship covers the whole band. And that band (48–175) is far narrower than the ship's own travel (out past 300): everything to the right is a place no enemy can go.

The design-stress pass named it and its fix in one breath: give the spawn a *real* random source, spread it across the *whole* field, and make no square safe.

### A real random column — the LFSR

We replace the repeated raster sample with a **linear-feedback shift register** — one byte walking a maximal 255-long pseudo-random cycle, one step per spawn:

```
advance_rng:
        lda rng_seed
        asl                 ; shift left; the bit that falls out -> carry
        bcc +
        eor #$1d            ; a 1 fell out: fold the tap back in
+       sta rng_seed
        rts
```

The routine advances one byte of state per spawn. A non-zero seed walks all 255 non-zero byte values before repeating. Seeding from the raster once at game entry can vary the starting point, but two starts can choose the same seed. The same seed and call order reproduce the sequence; this generator is predictable, not a security tool.

### Enemies across the whole field — the 9th bit

To threaten the sky the ship can reach, an enemy has to cross X=255 — and a sprite's X is 9 bits: the low eight in its position register, the ninth over in `$d010`. You met that bit in the Primer, on the ship. Now the enemies use it too: an `enemy_xhi_tbl` byte each, written into `$d010` beside the low X, and both collision tests taught to compare the full nine bits instead of skipping the right half. The enemies can now spawn on both sides of X=255. The mapping is `24 + b + floor(b/8)`. For an arbitrary byte 0–255 that would span 24–310, as the source comment describes. Our LFSR never supplies zero, so its actual spawn set contains **255 distinct columns from 25 through 310**, with gaps. For example, b=7 gives X=31 and b=8 gives X=33: X=32 is skipped. It is not a uniform choice among all screen columns.

Predict whether X=24 can appear as a spawn, and whether that makes it a permanently safe place to park. The first answer comes from the mapping; the second also depends on enemy width, collisions and the hunt movement below. [LFSR range mapping](/patterns/cross-platform/framework/lfsr-random) explains why reaching a useful range does not establish fairness.

<Figure src="/images/commodore-64/assembly/starfield/unit-17/step-03-spread.png" alt="Three green enemies at widely different X positions across the whole width of the screen, the white ship near the bottom." caption="The LFSR spawn: the three enemies scatter across the whole field, past the old X=175 ceiling. No single column to park under, and no right-side corner the danger can't reach." />

### No square is safe — the hunt

Spreading the enemies stops you *scoring* from a park, but a still ship can still hide: shoot the one lane it sits under, let the rest fall wide. So the last piece makes the enemies **come to you**. Every other frame, each enemy drifts one pixel toward the ship's column:

```
drift_enemy_x:
        ; step this enemy's 9-bit X one pixel toward the ship's 9-bit X
        ...
        bmi .drift_right    ; enemy left of the ship -> move right
        bne .drift_left     ; enemy right of the ship -> move left
        ...
```

Slow enough that a moving ship shakes them; fatal to one that doesn't. Parking is a death sentence now: the enemies converge on your column from across the field, and with one bullet in the air at a time a still ship can't clear them all.

<Figure src="/images/commodore-64/assembly/starfield/unit-17/step-03-park-shipped.png" alt="The shipped game: the ship sitting at the bottom centre, three lives, enemies drifting in a lane away from it." caption="The shipped game, thirty seconds of parking and holding fire: three lives, untouched. The turret park." />

<Figure src="/images/commodore-64/assembly/starfield/unit-17/step-03-park-fixed.png" alt="The fixed game: the ship at the bottom centre with enemies converging on its column from above, lives down to 2." caption="The fixed game, the same thirty seconds of the same lazy play: a life already gone, the wave bearing down. Sitting still is no longer a strategy — the game can be lost." />

<CodeDiff
  before="commodore-64/assembly/starfield/unit-17/steps/step-02.asm"
  after="commodore-64/assembly/starfield/unit-17/steps/step-03.asm"
  lang="6502"
  title="Step 3: an LFSR spawn, full-field 9-bit enemies, and a hunt that leaves no square safe"
  context={3}
/>

<details>
<summary>The complete corrected program</summary>
<CodeFromFile src="commodore-64/assembly/starfield/unit-17/steps/step-03.asm" lang="6502" />
</details>

None of this was invented for a lesson — the turret park shipped, and a scripted "park and hold fire" run found it exactly the way the winnability gate is meant to: by playing to break the game and watching it break. `step-03` is Starfield with the hole closed — the LFSR earns its place, and the game finally can't be won by refusing to play it.

## Milestone 4 — a curve that keeps bending

Closing the turret park made Starfield a game you can lose. It didn't make it a game worth a long sitting. Three enemies, a field of black, a difficulty that stops climbing at wave 5 — the design-stress pass named that too: *minute ten is identical to minute two*. The lanes quicken, then run out of ideas and stop. So this last step gives the game a curve that keeps bending.

### The swarm grows

The speed table ran out of room; the *sprite hardware* did not. The ship is sprite 0, the bullet sprite 1, the enemies 2, 3 and 4 — which leaves **sprites 5, 6 and 7 idle**. The escalation the game was missing sat in the VIC-II the whole time: more enemies.

Turning three enemies into a variable count is the parallel-array pattern you've used since Unit 1, one size larger. The four enemy arrays — X, Y, flash, the 9th bit — widen from three bytes to six, and every loop that walked `#$03` now walks `enemy_count`:

```
        inx
        cpx enemy_count         ; every active enemy this wave (3..6)
        bne enemy_loop
```

`advance_wave` grows that count — one more enemy every other wave, up to six — switching on the next sprite and dropping it in from the top:

```
        lda enemy_count
        cmp #$06
        bcs no_grow             ; already six
        tax                     ; X = the new enemy's index
        inc enemy_count
        lda enemy_d010_bit,x    ; its sprite's enable bit
        ora $d015               ; switch the sprite on
        sta $d015
        lda #$32
        jsr spawn_enemy         ; drop it in, LFSR column
```

<Figure src="/images/commodore-64/assembly/starfield/unit-17/step-04-start.png" alt="Wave 1: three green enemies spread across the screen, the ship near the bottom, score 0001." caption="Wave one, where the curve begins: three enemies, scattered by the LFSR." />

<Figure src="/images/commodore-64/assembly/starfield/unit-17/step-04-swarm.png" alt="Wave 9: six green enemies spread across the field homing on the ship, the readout at W9, score 0016." caption="Nine waves on: six enemies — the idle sprites 5, 6 and 7 pressed into service — and a score four digits deep. The field is no longer empty, and the pressure hasn't stopped rising." />

### The hunt tightens

The count caps at six around wave six, and the speed table topped out at five. Past that the game would go flat again — so the *homing* becomes the late escalator. Early on, enemies drift toward your column every other frame (a gentle half-pixel); from wave seven, three frames in four — firmer, a pull you feel, without the lunge that collapses the swarm onto you at once:

```
        lda wave
        cmp #$07
        bcc drift_slow          ; before wave 7: every other frame (~0.5 px)
        lda frame_count
        and #$03
        bne drift_now           ; wave 7+: three frames in four (~0.75 px)
```

### A score that can hold a long game

A game you can play for nine waves needs a scoreboard that counts that high. The old score was one BCD byte — two digits, wrapping silently at a hundred. Now it is two bytes, four digits, the carry rippling from the low pair to the high:

```
        sed
        lda score
        clc
        adc #$01
        sta score
        lda score_hi
        adc #$00                ; the carry ripples up into the top two digits
        sta score_hi
        cld
```

Four digits is room for 9,999 kills — more than any sitting will spend, which is the point: the number never lies to you again.

<CodeDiff
  before="commodore-64/assembly/starfield/unit-17/steps/step-03.asm"
  after="commodore-64/assembly/starfield/unit-17/steps/step-04.asm"
  lang="6502"
  title="Step 4: enemy count as a difficulty axis, a tightening hunt, and a four-digit score"
  context={3}
/>

<details>
<summary>The complete program</summary>
<CodeFromFile src="commodore-64/assembly/starfield/unit-17/steps/step-04.asm" lang="6502" />
</details>

The design-stress pass asked for a second escalation axis once the speed table gave out. Enemy count is that axis, the tightening hunt is a third, and the score finally counts high enough to prove you climbed. Starfield isn't just un-broken now — it's a game with somewhere to go.

## Restart investigation: does game three begin like game one?

A title screen returning is useful evidence, but it does not prove that the next
game starts cleanly. Use the [optional restart companion](https://github.com/Code198x/code-samples/tree/main/commodore-64/assembly/starfield/unit-17/restart)
with step 4. It builds a baseline, one deliberately faulty copy and a repair.
Keep the original game as our working checkpoint.

We call the real entry and wave-growth routines in a controlled test. After each
of the first two entries, five calls to `advance_wave` grow the swarm to six.
The fixture also supplies a non-zero score, one remaining life, an active bullet
and seven kills. It then returns to the title and enters the game again. These
are prepared test conditions, not three played-through games.

Before running, predict the state immediately after each `enter_game`:

| State | First, second and third entry should each have |
|---|---|
| Enemy count | 3 |
| Score, including its high byte | 0000 |
| Lives | 3 |
| Wave, kills, fall speed | 1, 0, 1 |
| Bullet active | 0 |
| Sprite enable bits | Ship and first three enemies on; bullet off |

The faulty copy omits just `sta enemy_count` from `enter_game`. The fixture
explicitly sets the count to three **once** before the first entry. That makes
the first faulty game look correct without relying on initial RAM contents.
Deleting the store in an ordinary launch could fail on the first game instead.

Run the baseline, then the faulty copy. Record all three displayed enemy counts.
Does a correct first entry establish that restart works? Choose a hypothesis:
a wrong starting count, a wrong wave-growth rule, or a missing per-game reset.
What evidence would distinguish them?

<details>
<summary>Hint 1: follow the lifetime of the count</summary>

Find the store in `enter_game` before the spawn loop. Then find the increment
in `advance_wave`. Does entering the title restore that value? Which value does
the next spawn loop use if the store is missing?

</details>

<details>
<summary>Hint 2: compare a rule with its presentation</summary>

`enter_game` still resets wave to 1 and enables the first three enemy sprites.
Neither operation changes `enemy_count`. Inspect the recorded count as well as
the readout and sprite-enable bits.

</details>

<details>
<summary>Explain the evidence and check the repair</summary>

The baseline reports **3, 3, 3**. The faulty copy reports **3, 6, 6**: the first
entry borrows the fixture's initial value, then the larger count survives each
restart. Wave 1 and three enabled sprites do not make the logical count three.
The later loops can still process the extra entries.

Restore `sta enemy_count` after `lda #$03`, **before** spawning the starting wave.
Rebuild and repeat the three entries. Expect **3, 3, 3**, and check the other
fields in the table too. The companion records them in memory and includes a
native verification script; repairing the count must not undo score, lives,
wave or bullet initialisation.

Changing the wave table, hiding more sprites or setting the count only at launch
would not repair this lifetime error. The count belongs to each play session,
so each new session must establish it.

</details>

Explain the fault without naming the missing instruction: which old state
crossed a boundary where it no longer belonged? Then identify another field in
the entry table with the same lifetime. Keep random-call order and sound handover
out of this investigation; one reliable fault is enough. Return to the original
step-04 game after the experiment.

## Position investigation: repeat the choices, find the first difference

After the restart investigation, use the [optional fixed-seed companion](https://github.com/Code198x/code-samples/tree/main/commodore-64/assembly/starfield/unit-17/position).
It makes twelve calls to the existing spawn routine with seed **1**, always
using enemy 0, which is sprite 2. There are no joystick, movement or collision
updates between calls. The same launch input schedule runs each copy. We have
fixed the starting state and call order as well as the seed; this is a small
routine experiment, not a recorded playthrough.

One deliberately faulty copy omits the call to `set_enemy_hibit` at the end of
`spawn_enemy`. The random generator and coordinate calculation are unchanged.
Predict whether the first error will appear in the random byte, the logical
coordinate or the sprite registers. Run the baseline and fault, then compare
their records in order instead of hunting for an arbitrary difference on screen.

Each record contains the random byte, logical X low/high bytes, `$d004` and
`$d010`. The screen shows the last three records in **hexadecimal**. The companion
records all twelve for inspection. Initially `$d010=$ab`: sprite 2's high bit is
clear, while several other bits are set so we can check that the helper preserves
them. Its mask for this sprite is `$04`.

| Call | Random byte (decimal) | Calculate `24 + b + floor(b/8)` | Predict low byte / high byte |
|---|---:|---|---|
| 10 | 116 | Write our result | Write before running |
| 11 | 232 | Write our result | Write before running |
| 12 | 205 | Write our result | Write before running |

<details>
<summary>Hint 1: find where the representations separate</summary>

For X above 255, subtract 256 to find its low byte. Compare the logical high byte
with bit 2 of `$d010`. A correct random byte does not prove that its position
reached the display correctly.

</details>

<details>
<summary>Hint 2: follow the next spawn too</summary>

The same sprite is reused. After setting its high bit, what must happen when its
next coordinate is below 256? What would happen if we left the bit set, or wrote
the logical high byte over all of `$d010`?

</details>

<details>
<summary>Compare the first divergence and repair</summary>

The coordinates are **154, 285, 254**. Calls 1–10 agree. On call 11, both programs
calculate X=285: low byte 29 (`$1d`), high byte 1. Both write 29 to `$d004`.
The working helper changes `$d010` from `$ab` to `$af`; the faulty copy leaves
it at `$ab`. The register-selected position is therefore 29, not 285. This is a
handoff fault, not a different random choice.

Restore the original `jsr set_enemy_hibit`. On call 12, the helper must also clear
sprite 2's high bit, returning `$d010` to `$ab` for X=254. Leaving it set would
select X=510. The other sprite bits must survive both operations.

The companion's checks compare all twelve random bytes and coordinates, verify
the register values on both sides of 255, and repeat the baseline from a cold
start. The repair reproduces the baseline PRG byte for byte. A single plausible
sprite position would be weaker evidence than this ordered trace.

</details>

Explain the first wrong value and which routine owns the transfer. Then explain
why changing the seed would hide or move this failure without repairing it.
Keep the random calls in their original order for this investigation. We will
change that variable separately; a seed alone cannot guarantee reproducibility.
Return to the original step-04 game afterwards.

## Call-order investigation: who gets the next byte?

Now keep the position handling correct. The [optional call-order companion](https://github.com/Code198x/code-samples/tree/main/commodore-64/assembly/starfield/unit-17/call-order)
reuses the fixed-seed fixture and adds **one** cosmetic draw for a border colour.
This decoration belongs to the experiment, not the normal game's starfield.
Both versions consume thirteen bytes: twelve for enemies and one for decoration.
We move the cosmetic draw from just before the first spawn to just after it.

Keep seed 1, the initial state and the launch inputs identical. Before running,
assign the first four bytes—**2, 4, 8, 16**—to their consumers. Use
`24 + b + floor(b/8)` to predict the first two enemy positions. Will every later
spawn differ, or can the two traces agree again?

Run both copies. The display shows their first three spawn records in hexadecimal;
the companion also logs each draw as a consumer ID and its byte. We can inspect
those values without distinguishing the border colours. Compare the raw random
sequence **and** the consumers: matching seeds alone do not answer both questions.

<details>
<summary>Hint 1: label the draws before calculating positions</summary>

In one version the order begins decoration, enemy, enemy. In the other it begins
enemy, decoration, enemy. Each request advances the same generator once.

</details>

<details>
<summary>Hint 2: count what has happened before spawn two</summary>

How many bytes has each version consumed by then? There are no collision or
movement updates between these calls that could introduce another difference.

</details>

<details>
<summary>Compare the consumers and explain the result</summary>

| Draw | Decoration before spawn 1 | Decoration after spawn 1 |
|---|---|---|
| Byte 2 | Decoration | Spawn 1: X=26 |
| Byte 4 | Spawn 1: X=28 | Decoration |
| Byte 8 | Spawn 2: X=33 | Spawn 2: X=33 |
| Byte 16 | Spawn 3: X=42 | Spawn 3: X=42 |

The generator produces exactly the same sequence. The first enemy receives a
different byte because decoration takes a different turn. Unlike the previous
fault, the logical position and sprite registers agree in both versions.

Spawns 2–12 agree: both versions have consumed two bytes before spawn 2. That
does **not** mean a real game would automatically recover the same history.
A changed first position could affect a collision, score or later random calls.
Our isolated fixture excludes those consequences so we can see this one cause.

</details>

Explain what we should do if a harmless-looking decoration edit changes an
important test scenario. Restoring the old call order restores these choices;
accepting a deliberate behaviour change means recording a new baseline. A separate
random state for decoration is another possible design when that independence
is needed, but it brings another seed and reset responsibility. We do not need
a new generator or replay engine to investigate this example.

Finish by describing the evidence a bug report needs beyond its seed: which
build, initial state, inputs and random consumers produced the result? Return
to the original step-04 game afterwards. These three investigations isolate a
reset fault, a representation handoff and a change in who consumes the data.

## When it's wrong, see why

- **The jingle plays its first note and holds it forever.** `jingle_timer` isn't being reloaded, or `jingle_idx` never increments — the tick serves one row and then keeps agreeing with itself.
- **The jingle keeps playing into the game.** `enter_game` must gate voice 1 off. The SID holds notes by itself — that is the feature — so nobody stops the band except you.
- **The laser sounds soft and woolly after a game.** The envelope handover is half-done: `enter_title` installed the band's attack/decay and `enter_game` never restored the laser's. Write both registers back.
- **The wave never advances.** `kills` isn't reset in `advance_wave` (it sails past 10 and the `cmp #10` / `bcc` never matches again), or the increment isn't in the hit path at all.
- **Wave six is absurdly, unplayably fast.** The table index isn't clamped — past the end of `wave_speed_tbl` the lookup reads neighbouring bytes as speeds. `WAVE_TOP` is the wall.
- **Fire still skips the game-over screen.** `ui_lock` has to be loaded *where the state changes* — in the out-of-lives branch — not in `show_game_over`, which repaints every frame and would reload the lock forever.
- **A parked, firing ship never dies.** The spawn column is seeded from the raster at a fixed point in the frame, so enemies cluster in a band tighter than the hit box and a still ship clears it. Give the spawn a real LFSR, spread it across the whole 9-bit field, and home the enemies toward the ship — a park has to be punishable.
- **Enemies vanish at the right edge, or never reach it.** The spawn X tops out at 175 and the collision test skips the ship's 9th-bit half. Carry enemies past X=255 with a per-enemy high bit and compare the full nine bits, or the right of the screen is a no-death zone.
- **The swarm never grows past three.** A loop still walks `#$03` instead of `enemy_count`, or `advance_wave` never increments the count. Widen the arrays to six and count with the variable.
- **A new enemy shows as garbage, or not at all.** Its sprite bit in `$d015` wasn't switched on, or its shape pointer at `$07fd`-`$07ff` wasn't set. Enable the bit and set the pointer before the enemy can appear.
- **The score still snaps back to zero.** The high byte isn't carried — the `adc #$00` into `score_hi` is missing, so the low pair wraps at a hundred as before.

## Before and after

We started with a finished loop that treated every minute the same and ended with a game that pushes back: waves that quicken on a schedule the data owns, a title with a voice, endings with room to land. None of it touched the systems built in Units 1–16 — the curve is a poked byte, the music is a visited table, the dwell is a counted-down lock. That is what the architecture was for. And when the winnability gate went looking, it found a game that pushed back only if you *let* it — a turret park that made losing optional — so a real random spawn, full-field enemies and a hunt closed the last hole: now the pushing-back is not optional. Then, with the game finally fair, we gave it somewhere to go — a swarm that grows into the idle sprites, a hunt that tightens late, a score that counts a long run — the curve bending on past the point the speed table admitted defeat.

## Try this

- **Act on the press, not the hold.** `ui_lock` slows a held button down; a *latch* ignores it entirely — keep last frame's fire bit, act only on the 1→0 edge. It's the full fix Unit 16's "Try this" promised, and every screen with a button wants it.
- **Bend the curve.** Make the table longer and gentler: `1, 1, 2, 2, 2, 3` with a wave every *fifteen* kills. Difficulty design is now a data-entry job — that was the point.
- **Transpose the jingle.** Double every pitch value and the tune jumps an octave (on the SID, *bigger* number means *higher* note — the opposite of the Spectrum's delay loops). `NOTE*2` arithmetic is the assembler's job, not yours.
- **A two-note chirp.** The wave-up ding is one gate of voice 3. Make it a quick low-high pair — two pokes and a few frames apart — and it starts to sound like a promotion instead of a doorbell.

## What you've learnt

- **Escalation is data** — a counter, a table, and one live byte the game reads fresh every frame; the systems never know what wave it is.
- **Three caps, three jobs** — the counter tells the truth, the index clamps for safety, the digit caps for display. Keeping them separate keeps the game honest.
- **A sequencer is a table and a tick** — pitch, duration, rest-as-a-note, a loop marker; the SID holds the notes so the game keeps running.
- **Shared voices change hands at transitions** — the title lends the laser's voice to the band; both halves of the handover live in `enter_title` and `enter_game`.
- **Endings need dwell** — a lock the ending screens count down before believing the button; the pause is part of the design.
- **A game is proven by trying to break it** — a scripted "park and hold fire" run found a dominant strategy the *finished* game shipped with. Randomness you can stand under isn't random; a field the danger can't reach isn't a field. A real LFSR, full-field 9-bit enemies, and a hunt made losing possible again.
- **Difficulty is more than one axis** — when the speed table topped out, enemy count and homing pressure carried the curve on. Escalation lives in the hardware you haven't used yet (three idle sprites) and the behaviour you can tighten.
- **The parallel-array pattern scales** — three enemies to six is the same arrays one size wider and one variable in place of a literal; the loops never learn how many there are.

## What's next

*Now* Starfield is finished — and finished means *losable and worth replaying*: opening music, a curve that keeps bending past where the speed table gave out, a swarm that grows into the idle sprites, endings that land, a four-digit number for the run you died on, and no square left to switch the danger off. The vocabulary list from Unit 16 gains its last entries: escalation as data, music as a table, patience as a lock, a random source you can't out-park, and difficulty spread across more axes than one.

The next game on this machine starts from all of it. **Platform Panic** trades the open sky for floors and ladders — and brings the first problem sprites can't solve alone.
