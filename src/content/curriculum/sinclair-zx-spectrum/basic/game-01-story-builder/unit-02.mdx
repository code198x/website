---
title: "Asking Questions"
description: "INPUT asks the player to type something. String variables remember the answer."
pubDate: 2026-05-25
game: 1
unit: 2
tags: ["basic", "input", "string-variables"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

PRINT talks. INPUT listens.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-01-story-builder/unit-02/ask.bas" />

Run this. The screen clears, then shows `What is your name?` and waits. Type your name and press Enter.

The computer prints `Hello, ` followed by whatever you typed. It remembered your answer.

## How it works

Line 50: `INPUT "What is your name? "; n$` does two things. It prints the prompt (the text in quotation marks), then waits for you to type. Whatever you type is stored in `n$`.

The `$` at the end of `n$` means it holds text, not a number. The letter `n` is just a name you chose — you could call it `x$` or `z$`.

Line 130: `PRINT "Hello, "; n$` joins two pieces. The fixed text `"Hello, "` and whatever `n$` contains. The semicolon glues them onto the same line.

## Why line 130?

The line numbers jump from 50 to 130. That gap is deliberate — it leaves room for more lines between 50 and 130 without renumbering anything. You will fill that gap in the next unit.
