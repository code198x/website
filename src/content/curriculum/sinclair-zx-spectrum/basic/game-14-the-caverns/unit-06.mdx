---
title: "The Creature"
description: "Something lives in the cave — smell it before it finds you."
pubDate: 2026-05-27
game: 14
unit: 6
tags: ["basic", "creature", "hazard", "adjacency", "smell"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

The pit stays where it is. The creature does not.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-14-the-caverns/unit-06/creature.bas" />

Lines 66–67 place the creature in a random room, avoiding the pit (there is no point having the creature start in the pit). Line 97 now shows both hazard positions for debugging.

Lines 105–106 add the smell clue, reusing the same adjacency subroutine. Set `chk` to the creature's room, call `GO SUB 200`, and if `adj = 1`, print "Something smells terrible..." The subroutine does not know it is checking for a creature — it just answers the same question: "Is room `chk` next to room `rm`?"

Line 145 checks whether the player walked into the creature's room. If so, lines 420–430 show the death screen: "The creature got you!"

## Two hazards, one subroutine

The adjacency subroutine now serves two callers. Line 102 checks for the pit's draft. Line 105 checks for the creature's smell. The code is identical in structure — set `chk`, call the subroutine, read `adj`. The only difference is which room number goes into `chk`.

This is the power of `GO SUB`. Without it, we would need to copy the four-way check twice — once for the pit, once for the creature. With the subroutine, adding a new adjacency check costs two lines instead of six.

## Smell is different from draft

Both clues use adjacency, but they mean different things. The draft is fixed — the pit never moves, so a draft in room 5 always means the same thing. The smell changes meaning every turn because the creature moves. A room that smelled terrible last turn might be safe now, and a room that was silent might suddenly reek. The same sensing mechanism, applied to a moving target, creates a fundamentally different kind of danger.
