---
title: "The Lander"
description: "A V character descends the screen — the spacecraft becomes visible."
pubDate: 2026-05-25
game: 8
unit: 06
tags: ["basic"]
---

import CodeFromFile from "@components/CodeFromFile.astro";
import Figure from "@components/Figure.astro";

Numbers tell the player how high they are. A moving character shows them.

<CodeFromFile src="sinclair-zx-spectrum/basic/touchdown/unit-06/lander.bas" />

Line 360 is the key: `LET row = 20 - INT (alt / 5)`. It converts altitude (0–100) to a screen row (20–0). When altitude is 100, the row is 0 — top of the screen. When altitude is 0, the row is 20 — the ground line. Line 380 clamps the row so it never drops below the ground. Lines 420–430 draw the lander: line 420 blanks a fixed spot (row 20), and line 430 prints `"V"` at the new row. That erase is deliberately crude — it only ever clears row 20, so as the lander falls it leaves a trail of ghost `V`s down the screen. The next unit erases properly.

<Figure src="/images/sinclair-zx-spectrum/basic/touchdown/unit-06/screenshot.png" alt="A column of V characters down the screen — the lander's untidy ghost trail" caption="The lander is visible now — but the crude erase leaves a trail of ghost V's behind it." />

## Number to position

The formula `20 - INT (alt / 5)` does two things. `alt / 5` scales the altitude from 0–100 down to 0–20 (the usable screen rows). `INT` drops the decimal so it maps to a whole row number. `20 -` flips the direction: higher altitude means a smaller row number, which is higher on the screen.

This is a common pattern in games: converting a **game value** (altitude in abstract units) to a **screen position** (row on the display). The game logic works in its own units. The display code translates.

The lander is just a `V` character — one letter, one cell. But it moves, and that movement gives the player something to watch. The dashboard shows the numbers; the lander shows the feeling.
