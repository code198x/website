---
title: "Quiz Master"
description: "Title screen, per-round feedback, replay — the complete game."
pubDate: 2026-05-26
game: 10
unit: 10
tags: ["basic"]
---

import CodeFromFile from "@components/CodeFromFile.astro";
import Figure from "@components/Figure.astro";

The final touches: a title, per-category feedback, a fanfare, and a replay option.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-10-quiz-master/unit-10/quiz-master.bas" />

<Figure src="/images/sinclair-zx-spectrum/basic/game-10-quiz-master/unit-10/screenshot.png" alt="Title screen: 'QUIZ MASTER' in red, a gold trophy, and 'Press any key to start'" caption="The finished title: a drawn trophy and a prompt that waits with PAUSE 0." />

Lines 10–40 add a title screen. Lines 320–330 show a mini-summary after each category ends: the category name and how many the player got right, displayed at the bottom of the last question card. The player sees their performance in each subject before moving to the next.

Line 470 plays a fanfare — four ascending notes. Lines 480–510 add a replay prompt: `INPUT "Play again? (y/n): ";p$`. If yes, `GO TO 10` restarts. If no, "Thanks for playing!" and `STOP`. This is the first game to ask explicitly rather than using `PAUSE 0` — a design choice. A quiz that replays automatically might feel like it is forcing the player to continue.

## What you have built

Quiz Master is complete. Fifty-three lines of game logic, plus DATA: a title screen, four categories with colour-coded headers, eight multiple-choice questions from an answer-key array, high/low sound feedback, per-category breakdown with a score array, a rating system, and a replay option.

This game used more data structures than any previous one — two arrays (`k()` for answers, `s()` for scores), nested FOR loops, multiple DATA sections with RESTORE, and string variables driven by DATA. It is the most structured program so far, and the one that most clearly separates data from logic.

Next: **Locksmith** — crack a hidden code by deduction, with numeric arrays and clues that reveal themselves a layer at a time.
