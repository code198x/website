---
title: "Make contact matter"
description: "Calculate the next position, resolve contact and turn a moving display into a rally game."
game: 2
unit: 5
---

import CodeFromFile from '@components/CodeFromFile.astro';
import Question from '@components/Question.astro';
import Sources from '@components/Sources.astro';

At present the ball turns even if the paddle is nowhere near it. We need one
clear rule: arriving beside an occupied paddle row is a return; arriving outside
those rows is a miss.

Continue from [Move the paddle](../unit-04-move-the-paddle/), with the program
stopped. This change reorganises movement, so enter the full set of edits before
running it.

Replace lines **200**, **210**, **220**, **230**, **240**, **250**. Add lines **26**, **245**, **600**, **610**, **620**, **630**, **700**.

<CodeFromFile src="sinclair-zx-spectrum/basic/volley/prototype/snippets/step-05.bas" />

## Decide before drawing

Previously we changed `x` and `y`, then changed a direction at an inner limit.
Now we calculate a **candidate position**, `nx` and `ny`, meaning “where this
step would take the ball”. We can inspect and adjust it before it becomes the
position we draw.

`LET nx=x+dx` does not change `x`. That separation gives us somewhere to resolve
contact. Once the checks are finished, line 250 copies the candidate into `x`
and `y` for the next drawing.

At the top, a candidate row of 2 would be inside the wall. Set `dy` to 1 and the
candidate row to 4 instead: the ball leaves row 3 and takes its next step inward.
The bottom rule changes a candidate 20 to 18. At the right wall, candidate column
30 becomes 28 and `dx` becomes −1. These rules preserve the one-cell path beside
the walls while putting the decision before drawing.

## Which row reaches the paddle?

A candidate column of 2 means the ball is arriving at the paddle column. Given
our limits and one-cell steps, it can reach that column only from column 3 while
travelling left. That is when line 240 calls the contact routine.

The routine tests the **candidate row after vertical contact has been resolved**:

- If `ny` is less than `p`, it is above the paddle.
- If `ny` is greater than `p+2`, it is below the paddle.
- Otherwise it is in one of the paddle’s three rows, including either endpoint.

On a hit, set `dx` to 1 and `nx` to 4. The ball moves back into the court without
printing over the paddle. On a miss, set `missed` to 1 and return. We initialise
that flag to 0 at the start: zero means no miss; one means the rally has ended.

Both outcomes return to the main loop. Line 245 then sends a miss to the result
at 700, which prints a message and stops. We do not jump out of the subroutine
straight into another game: returning first completes the call properly.

## Trace the edge cases

With `p=9`, the paddle covers rows 9–11. A candidate row of 9 or 11 is a hit;
8 or 12 is a miss. The strict `<` and `>` tests deliberately leave the endpoints
inside the accepted interval.

<Question prompt="The ball is at row 3, column 3, moving up and left. The paddle starts at row 3. What happens next?">

The first candidate is row 2, column 2. The top-wall rule changes its row to 4
and its vertical direction to down. Row 4 is inside the paddle’s rows 3–5, so
the paddle returns it to column 4. The next drawing is at row 4, column 4,
travelling down and right. This follows our stated order of contact checks.

</Question>

## Follow one press through the rally

Pause over the complete listing before running another rally. Imagine one update
starts with `p=9`, `x=3`, `y=8`, `dx=-1`, `dy=1` and `missed=0`. At line 105,
`INKEY$` reads `"z"`. Predict the new paddle position, whether the ball hits it,
and which message or picture follows. Explain the order, not just the final answer.

We can work through this on paper; it is a prepared state, not a claim that we
reached it during ordinary play. Try it before opening the hints.

<details>
<summary>Hint 1: find the owner of each decision</summary>

Line 105 captures the input in `k$`. Lines 125–126 use that saved reading to change
`p`. Lines 200–240 calculate the ball's candidate and resolve contact. Trace those
in source order; the paddle can move before the contact test.

</details>

<details>
<summary>Hint 2: compare intervals</summary>

After moving down, which three rows does the paddle occupy? The ball's candidate
row is 9. Check it against the new interval, including both ends.

</details>

<details>
<summary>Compare the complete trace</summary>

| Boundary | What happens |
|---|---|
| Input | The physical press matters here because the reading at 105 is `"z"`; `k$` retains it through `PAUSE 2` |
| Paddle state/display | Line 126 erases the old top cell, then moves `p` from 9 to 10; line 130 calls the drawing routine to show rows 10–12 |
| Ball candidate | `nx=2`, `ny=9`; no wall correction applies |
| Contact rule | Row 9 is above the new paddle, so `missed` becomes 1 |
| Outcome | Line 245 goes to the miss message and STOP before committing the candidate at 250 |
| Perception | We see a miss; this checkpoint has no sound event or background audio player |

The ball's old character was erased at 120. We do not draw the rejected candidate.
Drawing the paddle and ball is BASIC printing in this loop, not a separate graphics
queue. `PAUSE` and printing also take time; this is an instruction-order trace,
not an exact measurement from key press to screen light.

Now predict the same case with an empty input reading. The paddle stays at 9,
so candidate row 9 is accepted; the ball returns to column 4 with `dx=1`.
The next loop draws it at row 9. One changed input changes the collision result.

</details>

The essential result is an explanation connecting input, stored state, rule and
visible outcome. If the source order is difficult to follow, revisit
[Move the paddle](../unit-04-move-the-paddle/) before changing anything.

**Optional variation:** in a saved copy, make a four-cell paddle whose drawing,
movement limits and contact interval agree. Choose the required edits yourself;
first predict a top-edge hit, bottom-edge hit and adjacent miss. A longer drawing
alone is not a longer collision shape. Keep the original until all three checks
and movement at both court edges agree with the new rule.

## Play the first rally

Run the program. Use A and Z to meet the returning ball. It can now escape on
the left, producing `Miss. RUN to try again.` and the intentional STOP report.
`RUN` starts another attempt. Q still stops during play.

The paddle will not rescue a ball merely by being close. Compare its occupied
rows with the row where the ball arrives. If every approach returns regardless
of paddle position, check that the old unconditional left-boundary rule has
been replaced. If a new run immediately reports a miss, check line 26 resets
`missed` to zero.

This is a small, discrete collision model. We are not calculating curved
surfaces, spin or the point where two continuous paths intersect. Its virtue
here is that we can trace every decision and compare it with the display.

<details>
<summary>Complete rally program</summary>

<CodeFromFile src="sinclair-zx-spectrum/basic/volley/prototype/steps/step-05.bas" />

</details>

<Sources>

Steven Vickers, edited by Robin Bradbeer, *ZX Spectrum BASIC Programming*, second
edition (Sinclair Research, 1983), chapters 3, 5 and 7 (conditions, subroutines and expressions). The contact rules are the model implemented in this game.

</Sources>
