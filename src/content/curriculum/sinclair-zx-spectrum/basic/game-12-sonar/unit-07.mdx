---
title: "Winning"
description: "Count hits and track guesses — find all three targets to win."
pubDate: 2026-05-27
game: 12
unit: 7
tags: ["basic", "counter", "win-condition", "game-over"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

The game detects hits but does not count them. Three found targets should mean victory.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-12-sonar/unit-07/winning.bas" />

Line 70 initialises two counters: `hits` (targets found) and `guesses` (probes made). Line 87 shows both on a status line below the title. Line 130 increments `guesses` on each valid probe. Lines 300–340 handle hits: increment `hits`, play a celebratory beep, and check whether all three are found. Line 330 `IF hits = 3 THEN GO TO 400` triggers the win screen.

Lines 400–440 are the win screen: title, "All targets found!" in green, and the guess count. The number of guesses is the player's score — fewer is better.

## Efficiency as score

Unlike Locksmith (where the score is how many guesses you needed), Sonar's guess count measures efficiency. A skilled player triangulates from distance clues and finds all three targets in 8–10 probes. A random guesser might need 30. The guess counter creates a secondary challenge: not just "can you find them?" but "how efficiently can you find them?"
