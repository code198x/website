---
title: "Distance Clue"
description: "On a miss, calculate the Manhattan distance to the nearest target — ABS measures the gap."
pubDate: 2026-05-27
game: 12
unit: 5
tags: ["basic", "abs", "for-next", "manhattan-distance", "algorithm"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

"Miss" tells the player nothing useful. A distance clue tells them how close they are.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-12-sonar/unit-05/distance.bas" />

Lines 210–240 calculate the distance to the nearest target. Line 210 starts with `dist = 99` (impossibly far). Lines 215–240 scan every cell in the grid. For each target found (`g(a,b) = 9`), line 225 calculates the distance: `ABS(r - a) + ABS(c - b)`. If this distance is smaller than the current best, it replaces it.

Line 250 stores the distance in the grid: `LET g(r,c) = dist`. Line 105 in the grid drawing shows it: cells with values 1–8 print their number instead of a dot. The player now sees how far each miss was from the nearest target.

## Manhattan distance

`ABS(r - a) + ABS(c - b)` is the Manhattan distance — how many steps it takes to walk from one cell to another, moving only horizontally and vertically (no diagonals). `ABS` gives the absolute value: `ABS(3 - 7)` is 4, not -4. The distance between row 3 and row 7 is 4 regardless of direction.

The name comes from the grid layout of Manhattan streets — you cannot walk diagonally through buildings, so you add the horizontal and vertical distances separately.

## Why scan the whole grid?

There are three targets. The distance clue shows the distance to the *nearest* one, not all three. The scan checks every cell, finds each target, calculates the distance, and keeps the smallest. The `IF d < dist` comparison ensures only the minimum survives. The player sees one number — the closest target — and must triangulate from multiple probes.
