---
title: "A Different Number Every Time"
description: "RND and INT generate random numbers — every game is different now."
pubDate: 2026-05-25
game: 2
unit: 6
tags: ["basic", "rnd", "int", "randomize", "random"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

The secret is always 42. You already know the answer before you start. That is not a game — that is a memory test.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-02-lucky-number/unit-06/random.bas" />

Line 20 is new: `RANDOMIZE` shuffles the random number generator. Line 30 changed: `LET n = INT (RND * 100) + 1` picks a random number between 1 and 100. Line 200 now shows the answer when you win.

Run it twice. Different number each time. The game is now replayable.

## Taking the formula apart

`RND` gives a random decimal between 0 and 0.999...

`RND * 100` scales that to 0 up to 99.999...

`INT (RND * 100)` rounds down to a whole number: 0 to 99.

`INT (RND * 100) + 1` shifts the range up by one: 1 to 100.

That is the **range formula**: `INT (RND * range) + base`. Change the range and base to get different spans:

- `INT (RND * 6) + 1` — a die roll (1 to 6)
- `INT (RND * 10) + 1` — an easy guessing game (1 to 10)
- `INT (RND * 1000) + 1` — a punishing one (1 to 1000)

The game works. Next: making it *feel* like a game — with colour.
