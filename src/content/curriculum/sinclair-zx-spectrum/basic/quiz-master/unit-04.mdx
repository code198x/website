---
title: "The Answer Key"
description: "DIM stores correct answers — RESTORE separates answers from questions."
pubDate: 2026-05-26
game: 10
unit: 04
tags: ["basic"]
---

import CodeFromFile from "@components/CodeFromFile.astro";
import Figure from "@components/Figure.astro";

The previous version cheated — the correct answer always matched the question number. Real questions need a separate answer key.

<CodeFromFile src="sinclair-zx-spectrum/basic/quiz-master/unit-04/arrays.bas" />

<Figure src="/images/sinclair-zx-spectrum/basic/quiz-master/unit-04/screenshot.png" alt="'Question 1 of 4' with 'The answer was 1' revealed from the answer array" caption="The correct answer now comes from an array — it can sit at any option." />

Line 5 creates an array: `DIM k(4)` — four slots, one per question. Line 550 holds the answers in DATA: `1,2,3,4` — question 1's correct answer is option 1, question 2's is option 2, question 3's is option 3, question 4's is option 4. Line 50 reads these into the array with a FOR loop.

Line 55 `RESTORE 610` rewinds DATA to line 610 — where the questions start. Without it, the next `READ` would try to read past the answer data and find question text where it expects numbers.

Lines 270–280 now check `k(n)` instead of `n`: `IF g = k(n) THEN ...`. The answer for question `n` is read from slot `n` of the array. These four answers still happen to line up as 1, 2, 3, 4 — but the difference is *where they live*. The cheat baked the assumption into the test itself; here the answers are data on line 550, free to be anything. The next unit adds four more questions whose answers are 3, 2, 3, 2 — no longer matching their positions — and the same `IF g = k(n)` handles them without a single change.

## DIM and RESTORE

`DIM` creates an array — a numbered list of values. `DIM k(4)` makes slots 1 through 4. You read and write them with `k(1)`, `k(2)`, etc.

`RESTORE 610` is a targeted rewind. Plain `RESTORE` resets DATA to the very first DATA line. `RESTORE 610` resets to line 610 specifically. This lets you have multiple DATA sections: answers at line 550, questions at line 610. Read the answers first, rewind to the questions, then read those.

The answer DATA sits apart from the question DATA, each on its own line. Rearrange a question's options and the correct position changes — you record that by editing one number on line 550, never by touching the logic on line 270. The answers are content now, not code.
