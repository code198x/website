---
title: "The Display"
description: "PRINT AT, colour, and a lives bar — the game screen looks designed."
pubDate: 2026-05-25
game: 9
unit: 05
tags: ["basic"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

The game works but looks raw — plain text, no colour, no structure. This unit redesigns the screen.

<CodeFromFile src="sinclair-zx-spectrum/basic/game-09-cipher/unit-05/display.bas" />

Line 230 adds the title at the top. Lines 260–290 rebuild the word display: underscores print in white (`INK 7`), revealed letters print in green (`INK 4`). The colour difference means the player instantly sees which positions are solved.

Lines 310–330 replace the "Lives: 5" number with a visual bar — green asterisks. A FOR loop prints `lives` asterisks, then a second loop prints spaces to clear any that were removed. The bar shrinks with each wrong guess, just like the fuel bar in Touchdown.

Line 350 shows tried letters at a fixed position. Line 420 shows "Already tried!" in yellow (`INK 6`) at a fixed position too — no more scrolling text.

## Designing the screen

Every element now has a fixed address: title at row 0, word at row 4, lives at row 7, tried letters at row 9, input at row 12. The layout is deliberate. The word sits in the middle where the eye naturally rests. Lives are below it — close enough to see, but not in the way. Tried letters are further down, available when needed but not demanding attention.

The colour scheme serves the game: green for good (revealed letters, lives remaining), white for neutral (underscores, input), yellow for warnings (already tried). The player reads the colours before the text.
