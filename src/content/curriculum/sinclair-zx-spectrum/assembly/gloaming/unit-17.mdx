---
title: "A Small Sound"
description: "Give Gloaming a voice. The Spectrum's beeper is bit 4 of port $FE — toggle it as a square wave for a tone. A bright blip when a lamp lights, a colder note when one is snuffed. The honest single-blip 'before' of a sound driver."
pubDate: 2026-06-01
game: 1
unit: 17
tags: ["assembly", "z80", "sound", "beeper", "out", "audio"]
---

import CodeFromFile from "@components/CodeFromFile.astro";
import AudioClip from "@components/AudioClip.astro";

The game has been silent. This unit gives it the simplest voice the Spectrum has — the beeper — and ties it to what happens: a bright little blip when you light a lamp, a colder, lower note when the draught snuffs one. Two short sounds, a handful of bytes each, and the square suddenly feels alive.

## What you'll see — and hear

![The playing field with two lamps lit gold near the lamplighter and a tally of two at the top.](/images/sinclair-zx-spectrum/assembly/gloaming/unit-17/screenshot.png)

The screen looks like any moment of play — but now each lamp you light *announces itself*. Here are the two blips from lighting two lamps:

<AudioClip src="/audio/sinclair-zx-spectrum/assembly/gloaming/unit-17/lamp-blips.wav" title="Lighting two lamps — the beeper blip" source="ZX Spectrum beeper · port $FE bit 4" />

## The speaker is one bit

The Spectrum's speaker is **bit 4 of port `$FE`** — the very port we set the border with. Write a 1 to bit 4 and the cone pushes out; write a 0 and it pulls back. A single write is a *click*. To make a *tone*, you flip the bit on and off, over and over: each on-off pair is one cycle of a square wave, and **how long you wait between flips sets the pitch** — shorter waits, higher note.

So a beep needs two numbers:

```
beep:   B = how many cycles  (the length of the sound)
        C = the delay between flips  (the pitch — smaller is higher)
```

The routine writes `%00010000` (speaker out, border still black), waits `C`, writes `0`, waits `C`, and repeats `B` times. That's the whole sound engine.

## Blocking, and on purpose

The blip is **blocking**: while it sounds, the game does nothing else — the loop pauses for those few milliseconds. That's exactly how beeper games worked, and you can hear it as a tiny hitch when a lamp lights. It's the honest, simple "before" of a real **sound driver** — one that plays music *while* the game keeps running, which a later, bigger game builds. For a single blip, blocking is perfect.

One detail: we turn interrupts off (`di`) around the beep and back on (`ei`) after. Without that, the 50 Hz frame interrupt would fire mid-tone and jitter it into a warble. Off for the blip, on for the game.

## Two notes, two events

The sound is **feedback** — it's tied to things that happen. `blip_lit` (short, bright) plays in the same place the lamp lights; `blip_snuff` (lower, colder) plays where the draught puts one out. Pick different `B`/`C` for each and the two events *sound* different — you can play with your eyes half-closed and still know what just happened.

### The code, complete

<CodeFromFile src="sinclair-zx-spectrum/assembly/gloaming/unit-17/a-small-sound.asm" />

### What you hear

A clean, bright tick each time a lamp catches; a duller, lower one when the draught snuffs a lit lamp. Small sounds — but they turn a silent diagram into something that responds.

## Assemble and run

```
pasmonext --sna a-small-sound.asm a-small-sound.sna
```

Press space, light a lamp, and listen. Let the draught cross a lit one and hear the colder note.

## Try this: tune the blip

The blip is two numbers. In `blip_lit`, shrink `C` for a higher note, grow `B` for a longer one. Find a tick that feels *bright* without being shrill. A few values in and you'll have an ear for what the pair does — that's the whole of beeper sound design.

## Try this: a two-tone chime

Make lighting a lamp a little richer: call `beep` twice in a row with two different pitches — a quick low-then-high gives a cheerful "ding". Chaining a few short beeps is how beeper games built their fanfares, all from this one routine.

## Try this: a win fanfare

On a win, play something celebratory before the line prints. In `draw_win_screen`, chain three or four rising beeps (drop `C` a little each call) for a short triumphant run. It's the same `beep`, just sequenced — and a tiny tune is a big lift at the end of a game.

## If it doesn't work

- **No sound at all.** The speaker is **bit 4** of `$FE`. Confirm the beep writes `%00010000` then `%00000000`, and that `blip_lit` is actually called in the lamp-lit branch.
- **A click, not a note.** You're toggling once, not repeatedly. The tone comes from the `B` loop flipping the bit many times.
- **The tone warbles.** The frame interrupt is jittering it. Wrap the beep in `di` … `ei`.
- **The game hitches hard on a blip.** The sound is too long — shrink `B`. (Some hitch is expected; the blip blocks by design.)

## What you've learnt

- The **beeper is bit 4 of port `$FE`**; a tone is that bit toggled as a square wave, the flip-delay setting the pitch.
- A **blocking blip** pauses the game for a few milliseconds — authentic, and the honest "before" of a non-blocking sound driver.
- **`di`/`ei`** around the beep keeps the 50 Hz interrupt from warbling the tone.
- **Sound is feedback** — tie distinct notes to distinct events and the game speaks.

## What's next

Gloaming looks and sounds like a game now. **Unit 18, "The Square Warms"**, adds the last layer of polish to the play itself: atmosphere. As lamps light, the square *warms* — small attribute touches that make the lit town feel different from the cold, dark one you started in. It's a first taste of the mood-making that a later, larger game makes its whole craft, built from the colour writes you've used since Unit 1.
