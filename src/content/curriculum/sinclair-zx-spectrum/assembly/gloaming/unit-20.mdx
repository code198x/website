---
title: "On Real Iron"
description: "The last truth: does it run on a real Spectrum? Build the finished game to a tape with an auto-running loader, watch the loading stripes, and verify Gloaming behaves on the metal exactly as it did on the emulator."
pubDate: 2026-06-01
game: 1
unit: 20
tags: ["assembly", "z80", "real-hardware", "tape", "spectrum-next", "packaging"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

Gloaming is finished — a complete game, played from a title and back again. There's one truth left, and it's the one that matters most: **does it run on a real Spectrum?** This unit takes the game off the emulator and onto the metal. No new mechanics — just the oldest ritual in Spectrum software: building a tape, watching the stripes, and seeing your game appear on actual hardware.

## What you'll see by the end

![The classic ZX Spectrum loading screen — red and cyan horizontal stripes filling the border around a grey screen, the game loading from tape.](/images/sinclair-zx-spectrum/assembly/gloaming/unit-20/screenshot.png)

Those red-and-cyan stripes are the sound of the 1980s made visible: the Spectrum's ROM reading your game off a tape, one bit at a time. When they stop, the border goes black and **GLOAMING** appears — the very same title, game and ending you built, now running from the same kind of tape a Spectrum loaded in 1983.

## The snapshot was a convenience

We've used `.sna` snapshots all along, because they're instant — the emulator pours the whole of memory into place and starts. A real Spectrum has no such shortcut. It loads from **tape** (or, on a Spectrum Next, from storage), through the ROM's loader, byte by byte. So to run on real iron, the finished game needs to be a *tape*, not a snapshot.

## Building a tape

`pasmonext` builds one for us, complete with a tiny BASIC loader that loads the code and runs it:

```
# the snapshot — emulators, and the Next loads these too
pasmonext --sna gloaming.asm gloaming.sna

# a real tape image, with an auto-running BASIC loader
pasmonext --tapbas gloaming.asm gloaming.tap
```

`--tapbas` writes a `.tap` containing two blocks: a short BASIC program — `CLEAR`, `LOAD ""CODE`, `RANDOMIZE USR 32768` — and the machine code itself. The loader auto-runs, so on a real machine you simply type `LOAD ""` (or `J`, `SYMBOL SHIFT`+`P` `P`), press play, and the game loads and starts on its own. The entry address comes straight from our `end start` directive — the assembler and the loader agree on where the game begins.

## Loading it for real

- **48K / 128K Spectrum** — write the `.tap` to tape (or use a DivMMC / tape-replay device), `LOAD ""`, and watch the stripes.
- **Spectrum Next** — copy `gloaming.tap` (or the `.sna`) to the SD card and load it from the browser; the Next runs 48K software faithfully. This is the easiest "real hardware" today, and the one we target for the final sign-off.

## Why it behaves the same

Here's the quiet reward for building carefully. Gloaming makes **no** assumptions that hold only on an emulator:

- It's **frame-locked** — `IM 1` + `HALT` pace it to the real 50 Hz interrupt, so it runs at exactly the right speed on any Spectrum, not "however fast the host is".
- It reads the keyboard through the documented port `$FE`, and makes sound through the documented beeper bit — no undocumented tricks, no exact-contention timing, no host-specific behaviour.

So what you saw on the emulator is what you get on the metal. That isn't luck; it's the result of every "do it the honest way" choice across the last nineteen units. A game built on quirks surprises you on real hardware. This one doesn't.

## The finished game, entire

This is the whole of Gloaming in one file — the title and state machine, the lamplighter and the draught, lamps and tally and lives, sound and atmosphere, win, loss, and the loop back to the start. Everything you built, from Unit 1's first attribute write to here:

<CodeFromFile src="sinclair-zx-spectrum/assembly/gloaming/unit-20/gloaming.asm" />

## Assemble and run

```
pasmonext --tapbas gloaming.asm gloaming.tap
```

Load it in Emu198x's tape mode — or, better, on your own Next — and play it through: title, light the lamps, dodge the draught, win or lose, again.

## Try this: load it on your own hardware

This is the unit that only you can finish. Get `gloaming.tap` (or the `.sna`) onto a real Spectrum Next, or a 48K with a tape-replay device, and play it. Confirm every piece: the title waits, QAOP moves, lamps light with a blip, the draught snuffs and chases, the walls warm, the win and loss screens appear, and space starts you over. When it all behaves on the metal exactly as it did on screen — that's the sign-off. That's *your* hardware truth.

## Try this: a loading screen

Real Spectrum games greeted you with a picture while they loaded. Add one: a 6912-byte `SCREEN$` (the full bitmap + attributes) as a tape block before the code, and the artwork appears under the loading stripes. Drawing it is a project in itself — but it's the final flourish that makes a tape feel like a *published* game.

## Try this: make a label

You've made a real, loadable, period-faithful game. Print an inlay. Give it a price in 1983 pounds. It earned the £1.99 budget-classic treatment — and writing the back-of-the-box blurb is a surprisingly good test of whether you can say what your game *is*.

## If it doesn't work (on the metal)

- **`R Tape loading error`** on a real cassette — almost always volume or azimuth on the replay side; on a Next/SD or a digital tape device it loads clean. Rebuild the `.tap` and try again.
- **Loads, but doesn't start.** The BASIC loader's `RANDOMIZE USR` address must match the code's origin. `--tapbas` takes it from `end start`, so keep `org 32768` and `end start` in step.
- **Runs, but feels wrong.** If it ever ran differently on hardware than on the emulator, suspect a reliance on host timing — but Gloaming's frame-lock and documented I/O are exactly there to stop that. It should feel identical.

## What you've learnt

- Real hardware loads from **tape or storage**, not snapshots; build a `.tap` with an auto-running BASIC loader (`pasmonext --tapbas`).
- The **loading stripes are the ROM loader** — the same code path on the emulator and on the metal.
- A **frame-locked game using only documented I/O behaves identically** on real hardware — the payoff of building it honestly.
- The final verification is **yours to make**, on your own iron.

## What's next

You've finished Gloaming — a complete, winnable, losable, replayable game, built in Z80 assembly from a single attribute write and verified on real hardware. That's no small thing: most people who set out to write an assembly game never finish one. You have.

Next in the track is **Shadowkeep**, the first *substantial* game — and it doesn't start from scratch. The cell sprite, the save/restore, the collision, the game loop, the state machine, the HUD, the sound: every one of them was born here, in Gloaming, and Shadowkeep picks them up and builds a whole walled keep on top. You didn't just make a game. You made the engine your next game runs on.
