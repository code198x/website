---
title: "LD Is Not LET"
description: "Meet the registers — the handful of physical, 8-bit stores inside the CPU. Move a value from A into B and back again, and watch the border prove it made the trip."
pubDate: 2026-06-01
game: 0
unit: 2
tags: ["assembly", "z80", "registers", "ld", "getting-started", "out"]
---

import CodeFromFile from "@components/CodeFromFile.astro";

Last time you sent a number to the border and met the build-run loop. You used a register — `A` — without really meeting it. Let's meet it now, because this is the first place the machine stops behaving like the language you came from.

In BASIC, `LET a = 5` gave you a variable, and you could have as many as you liked — `score`, `lives`, `name$`, on and on. The [CPU](/vault/hardware/z80) is not so generous. It has a **handful** of registers — small stores *inside the chip itself* — and that's all. Their names are fixed: `A`, `B`, `C`, `D`, `E`, `H`, `L`. You don't declare new ones. You learn to *orchestrate the few you have*.

And each one holds a single **8-bit** number: 0 to 255, nothing larger. A register isn't a roomy named box; it's a tiny slot with a hard limit. That constraint shapes everything you'll write.

## What you'll see by the end

![The Spectrum's white screen surrounded by a solid cyan border.](/images/sinclair-zx-spectrum/primer/unit-02/screenshot.png)

A cyan border — colour 5. But the colour isn't really the point this time. The point is *how the 5 got there*: it travelled from `A` into `B`, survived us wiping `A`, and came back. The border is just the proof you can see. The real show is in the **register view** — open it in Emu198x and watch `A` and `B` change step by step.

## `LD` just moves a number

There's one new idea here, and it's `LD` — "load". Despite the name, it doesn't "load" in any grand sense; it simply **moves a number**. Into a register from a literal (`ld a, 5`), or from one register into another (`ld b, a`). That's the whole instruction.

To *prove* a value really moves between registers — rather than just trusting it — we send `5` on a round trip:

<CodeFromFile src="sinclair-zx-spectrum/primer/unit-02/registers.asm" />

This is last unit's program with three lines added in the middle. Follow `A` and `B` down the steps:

| After… | `A` | `B` |
|---|---|---|
| `ld a, 5` | 5 | — |
| `ld b, a` | 5 | 5 |
| `ld a, 0` | 0 | 5 |
| `ld a, b` | 5 | 5 |

We deliberately wipe `A` to 0, then bring the value *back from `B`*. The border ends up cyan — so the `5` must have been safe in `B` the whole time. That's the proof the copy was real.

**Why copy back into `A` just to show it?** Because the `out` instruction only ever sends `A` — never `B` or the others. It's one of the chip's fixed rules; just accept it for now. It's also what makes the round trip prove anything: the `5` *had* to come home through `B`.

## Assemble and run

Same loop as before — change, build, run:

```
pasmonext --sna registers.asm primer.sna
```

Load `primer.sna` in Emu198x with the register view open, and watch `A` and `B` move through the table above before the border settles on cyan.

## Try this: too big to fit

Change `ld a, 5` to `ld a, 300`, assemble, and run. Now look at `A` in the register view: it reads **44**, not 300. A register holds 8 bits — 0 to 255 — and 300 doesn't fit, so the machine keeps only the low 8 bits (300 − 256 = 44). And notice what *didn't* happen: no error, no warning. The assembler let it through; the machine kept the bits that fit and carried on. The tools won't catch this for you — a theme we'll return to.

## Try this: a longer trip

Send a value through *three* registers before showing it — `A` into `B`, `B` into `C`, then `C` back into `A` and `out` it:

<CodeFromFile src="sinclair-zx-spectrum/primer/unit-02/longer-trip.asm" />

Before you run it, predict `A`, `B` and `C` at each step — then open the register view and check. (`6` is yellow.)

## If it doesn't work

- **`pasmonext` says "Expected 'A' but…" on the `out` line.** You tried to `out` a register other than `A`. Only `A` can do `out ($FE), a` — route the value back into `A` first.
- **The border is black, not cyan.** The `ld a, b` step is missing or mistyped, so `A` was still 0 (from the wipe) when you showed it. Put the round trip back exactly as shown.
- **The border's the wrong colour.** Check the number in `ld a, …` — and remember the border reads only the low 3 bits, so values above 7 repeat the eight colours.

## What you've learnt

A register is a physical, finite **8-bit** store — the CPU has only a handful (`A B C D E H L`) — and `LD` moves numbers between them.

## What's next

This isn't a Spectrum quirk: nearly every CPU is built around a small set of registers — some have more, some fewer. You're learning how machines work, not a Spectrum trick. Next — **Everything Is a Number** — we look at what those bytes you're moving around actually *are*, and discover the machine doesn't know a letter from a colour from a number. They're all just bytes.
