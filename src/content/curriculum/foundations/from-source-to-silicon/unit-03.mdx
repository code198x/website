---
title: "Translating Once"
description: "Turn the whole program into numbers before it runs, then get out of the way. The thinnest translator that does this fits in eight lines, and you are about to write it."
pubDate: 2026-09-05
game: 1
unit: 3
tags: ["foundations", "from-source-to-silicon", "assembler"]
---

import Output from "@components/Output.astro";
import TraceTable from "@components/diagrams/TraceTable.astro";

Our interpreter model repeats translation work inside the loop. Another approach
moves that work before execution.

Translate the whole program first. All of it, before a single line runs. What is left
afterwards is a sequence of instructions. The machine still has to perform their
work; the translation lookup is no longer part of each pass. The loop that cost a thousand translations in Unit 2 now costs none,
because the translating happened once, up above it.

<TraceTable
  columns={["When", "Read the line", "Work out what it means", "Do it", "total"]}
  watch="total"
  rows={[
    ["before running", "LET total = total + 1", "put total plus one in total", "—", "—"],
    ["pass 1", "—", "—", "0 + 1", "1"],
    ["pass 2", "—", "—", "1 + 1", "2"],
    ["…", "…", "…", "…", "…"],
    ["pass 1000", "—", "—", "999 + 1", "1000"],
  ]}
>
  The middle column has one entry now, and it is above the loop rather than inside it. The
  table counts the additions and translation lookups. It omits other work such as
controlling the loop; it is not an instruction or timing measurement.
</TraceTable>

A translator that works this way is called a **compiler**. It takes a whole program,
produces the numbers, and is finished before the program starts.

## The thinnest translator there is

A compiler has a hard job. It turns *ideas* into numbers: a `FOR` loop is one line to
you and many instructions to the machine, and the compiler has to work out which.

There is a simpler translator that does not deal in ideas at all. It deals in names. One
name in, one number out, straight from a table. That is an **assembler**, and the table is
the one from Unit 1:

| Name | Number |
|------|--------|
| `INC` | 1 |
| `DOUBLE` | 2 |
| `SHOW` | 3 |

Look up each name, write down its number. That is the entire job.

## Build it

You have everything you need. Two lists, a loop that walks a program, and a question that
checks a name.

```text
LET names = ["INC", "DOUBLE", "SHOW"]
LET codes = [1, 2, 3]
LET program = ["INC", "INC", "DOUBLE", "SHOW"]

FOR EACH word IN program
  FOR i = 1 TO 3
    IF word = names[i] THEN SHOW codes[i]
  END
END
```

<Output>{`1
1
2
3`}</Output>

Read it through. For each word in the program, look along the list of names. When you
find the one that matches, show the number in the same place in the other list.

`1 1 2 3`. That is the row of numbers from Unit 1, and you just produced it from the
names. You have written an assembler.

This toy shows a name-to-number lookup. A real assembler also handles details
such as operands, labels, addresses and diagnostics. The small model gives us a
starting point for understanding those jobs; it does not implement them.

## What runs afterwards

Once the assembler has done its work, the names are gone. The machine is handed `1 1 2 3`
and runs it without consulting our source-name table. Add one, add one, double, show.

Removing the repeated source-name lookup removes that work from execution.
Whether a game keeps up with its intended update rate still depends on the
instructions, data and hardware work that remain.

## When it's wrong, see why

- **A name was not in the table.** The assembler found nothing to match, so it wrote
  nothing down, and the machine got a shorter program than you wrote. A real assembler
  stops and tells you. This toy does not, which is a good reason to make yours do so.
- **You changed the program and nothing changed.** The old numbers are still what the
  machine has. Translating once means translating *again* when you change something.
- **The program runs but gives the wrong result.** Keep the matching source and
  build information so we can connect execution back to our intentions. The next
  unit explains how diagnostics and observation help together.

## What you've learnt

- A **compiler** translates the whole program into numbers before it runs, so the loop pays
  no translating cost.
- An **assembler** is the thinnest translator: one name to one number, from a table.
- You built one from **two lists and a loop**.
- Removing runtime translation work does not establish the whole program's speed.

## What's next

In **Unit 4** we compare the responsibilities that come with more explicit
control: diagnostics, detail and portability.
