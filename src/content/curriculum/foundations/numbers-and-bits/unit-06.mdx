---
title: "Numbers That Aren't Whole"
description: "Two ways to carry a fraction on a machine that counts in whole steps, and why games nearly always chose the second."
pubDate: 2026-06-30
game: 1
unit: 6
tags: ["foundations", "numbers-and-bits", "fixed-point", "floating-point"]
---

import Output from "@components/Output.astro";

Every number so far has been a whole one. The moment you write a game, you need the in-between:
a ship that drifts half a pixel a frame, a ball at two-thirds speed.

The machine counts in whole steps. So how do you give it a fraction? There are two answers, and
choosing between them turns out to be a story about **speed**.

## The flexible way: let the point float

The first answer is to let the decimal point **float**. It sits wherever the number needs it,
close up for tiny fractions and far out for huge values.

It is flexible, and it is what most languages hand you without being asked. Type a fraction and
it works:

```text
SHOW 1 / 3
```

<Output>{`0.33333333`}</Output>

Look at what came back. Not a third, but a run of threes that stops. How many you get depends on
the machine, and every machine stops somewhere. A third has no exact form in tens, and there is
only so much room.

Floating-point is **only ever an approximation**.

## Why it can never be exact

You might think that is a problem only for awkward numbers like a third. It is not, and the
reason goes back to Unit 1.

The places to the *left* of the point double as you go: 1, 2, 4, 8. The places to the **right**
halve: a half, a quarter, an eighth, a sixteenth. Every fraction the machine holds exactly is
built by adding some of those together.

A half is one of them. So is three quarters, which is a half and a quarter. Fine so far.

Now try a tenth. There is no run of halves and quarters and eighths that comes to exactly 0.1.
It recurs, on and on, the way a third does in tens:

```text
0.1 in tens   0.1                 exact
0.1 in twos   0.000110011001100…  recurring
```

So `0.1` is perfectly exact when you write it in tens, and not exact at all inside the machine.
The machine stores the closest it can hold, which is a hair out. Add a few of those hairs
together and the error shows up in your answer. This catches people out in every language there
is.

There is a second catch, and for games it mattered more. The processors in these machines have
**no hardware for floating-point**. Every floating-point sum happens in software, step by
laborious step. Flexible, but **slow**. Far too slow to do dozens of times a frame.

## The cheap way: nail the point down

So games did the opposite. They fixed the point instead of floating it.

Pick a scale, say **256**, and agree once and for all that every number you store is that many
*256ths*. Then 1.5 is not stored as "1.5" at all. It is stored as 1.5 × 256, which is **384**, an
ordinary whole number.

To read the real value back, divide by the agreed scale:

```text
SHOW 384 / 256
```

<Output>{`1.5`}</Output>

The point is not floating. It is fixed at "divide by 256", and everyone agrees to it. The
fraction is carried as a **whole number**, which is the trick and also the name: **fixed-point**.

## The maths is just integer maths

Here is why that is such a win. Fixed-point values *are* whole numbers, so you do arithmetic on
them with plain integer addition, which is the fast kind.

Add 1.5 and 0.5. Store both scaled, as 384 and 128, and add the integers:

```text
SHOW 384 + 128
```

<Output>{`512`}</Output>

512 is 2 at our scale, so 1.5 + 0.5 came to 2 with nothing but whole-number addition.

Scaling by 256 is not even a multiplication. 256 is 2⁸, so ×256 is a shift left by eight and
÷256 is a shift right by eight. Fixed-point is built from the two fastest things the machine
does.

## Why games chose fixed-point

Fixed-point costs you some bookkeeping and buys you speed. In a game moving dozens of objects
fifty times a second, that was the trade worth making, every time.

## When it's wrong, see why

- **A float was not exactly the value you expected.** It stores the closest it can, and that
  includes tidy-looking values like `0.1`. Never test two floating-point numbers for being
  exactly equal.
- **A fixed-point value printed as a big whole number**, like 384 rather than 1.5. You forgot to
  divide by the scale. The scaling is yours to remember, because the machine only sees integers.
- **The numbers ran out of room.** A scale eats range. At ×256 the largest value you can hold is
  256 times smaller than the raw integer's limit.
- **Two values fought.** Adding a ×256 number to a ×16 number gives nonsense. Everything in a sum
  must share the **same scale**.

## What you've learnt

- A fraction can be carried two ways: **floating-point** or **fixed-point**.
- **Floating-point** is flexible and what most languages give you free. It is **approximate**,
  because the places after the point are halves and quarters and a value like `0.1` never lands
  on one. With no hardware for it, it is also **slow**.
- **Fixed-point** stores a fraction as a **whole number** at a fixed scale, so its maths is plain
  integer addition and shifts.
- Games chose fixed-point because in a loop, **speed won**.

## Where this leaves you

That is the primer. You can look at a number the way the machine does, whole or fractional,
positive or negative, in bits or in hex, and you can mask it, shift it and scale it.

From here the road goes down into a real machine, where hex addresses, bit flags, signed offsets
and fixed-point are not background facts. They are the language the code is written in.
