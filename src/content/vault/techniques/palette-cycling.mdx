---
ai_generated: true
reviewed: false
title: "Palette Cycling"
subtitle: "Animation without redraw"
summary: "Palette cycling created animation effects by rotating colour values rather than redrawing graphics, enabling flowing water, glowing effects, and pulsing lights without CPU overhead."
category: "techniques"
platforms: ['commodore-amiga', 'ibm-pc', 'commodore-64', 'atari-st']
tags: ['graphics', 'animation', 'technique']
sources:
  c64_indirect_registers:
    - title: "Commodore 64 Programmer's Reference Guide, Commodore Business Machines"
      kind: book
      page: "116-117"
      note: "Multicolour character mode bit pairs: \"00 Background #0 color (screen color) 53281 ($D021) / 01 Background #1 color 53282 ($D022) / 10 Background #2 color 53283 ($D023) / 11 Color specified by the lower 3 bits in color memory\" — \"You're not really putting color codes in the space for character color, you're actually using references to the registers associated with those colors. This conserves memory, since 2 bits can be used to pick 16 colors (background) or 8 colors (character). This also makes some neat tricks possible. Simply changing one of the indirect registers will change every dot drawn in that color. Therefore everything drawn in the screen and background colors can be changed on the whole screen instantly. Here is an example of changing background color register #1\" — the example loops \"170 X=INT(RND(1)*16) / 180 POKE 53282,X / 190 GOTO 160\""
    - title: "Commodore 64 Programmer's Reference Guide, Commodore Business Machines"
      kind: book
      page: "119-120"
      note: "\"Extended background color mode gives you control over the background color of each individual character, as well as over the foreground color.\" — \"There are 4 registers available for extended background color mode. Each of the registers can be set to any of the 16 colors.\" — character code ranges 0-63, 64-127, 128-191 and 192-255 select background registers 53281 ($D021) to 53284 ($D024); \"only the first 64 characters in the character ROM (or the first 64 characters in your programmable character set) can be used\""
    - title: "Codebase64 — Colour Flashing Notewriter Style, by Richard Bayliss"
      kind: database
      note: "Community wiki page. \"Have you saw programs like Face Noter, Octanoter, etc. All those programs use a special extra colour mode, to activate char flashing.\" Source listing: \"lda #$5b / sta $d011 ;Swich on EBC mode\" and a raster-interrupt routine \"flash\" that loads \"colour1,x\", \"colour2,x\", \"colour3,x\" into \"$d022 ;EBC Multicolour 1\", \"$d023 ;EBC Multicolour 2\", \"$d024 ;EBC Multicolour 3\" and steps X through a 40-entry table"
  c64_period_uses:
    - ref: magazines/computes-gazette
      kind: magazine
      issue: "Volume 7, Number 10 (October 1989)"
      date: "1989-10"
      page: "56"
      note: "The Programmer's Page, edited by Randy Thompson; tip \"Printing with Style\" by Jacques Bingham, DeSoto, MO. A BASIC loader whose DATA lines install an interrupt routine that stores a table of colour values into colour RAM (\"157,0,216\" = STA $D800,X and the following pages) from two DATA lines of 32 colour codes. \"You can shorten the program if you take out the color cycling. To do this, delete lines 20, 30, and 90-160. This also makes it work on the 128, since only the color-cycling portion is 64-specific.\""
    - ref: magazines/computes-gazette
      kind: advertisement
      issue: "Volume 4, Number 42 (December 1986)"
      date: "1986-12"
      page: "71-72"
      note: "Activision advertisement: \"Picture this. Paintworks, the easy-to-use, complete graphics editor and paint program, Lets you design cards, stationery, newsletters, musical slideshows, or just plain doodle. Features color cycling for animation effects, 3 work screens, multiple brushes and instant color blending to bring out the artist in you.\""
---

Change the palette, not the pixels. Palette cycling animated graphics by shifting colour indices through different values—what was blue becomes green becomes yellow becomes red, cycling endlessly. Water appeared to flow, lights seemed to pulse, and fire flickered, all without redrawing a single pixel.

## The principle

| Step | Process |
|------|---------|
| 1 | Design image with gradient colours |
| 2 | Assign sequential palette indices |
| 3 | Each frame, shift palette values |
| 4 | Pixels appear to move |

## Common effects

| Effect | Implementation |
|--------|----------------|
| Flowing water | Blue gradients cycling |
| Fire/lava | Orange/red palette rotation |
| Glowing lights | Brightness cycling |
| Neon signs | Colour pulsing |
| Waterfalls | Vertical flow illusion |

## Technical advantages

| Benefit | Explanation |
|---------|-------------|
| Zero CPU redraw | Only palette changes |
| Tiny memory | No frame storage |
| Smooth animation | Limited only by refresh rate |
| Multiple effects | Different palette ranges |

## Platform implementations

### Amiga

The Copper could change the palette per scanline. Colour registers live at `$DFF180-$DFF1BE` (32 entries × one word each). Cycling means writing the same range each frame with the values rotated by one slot:

```m68k
; Rotate colours 1-7 for a "flowing" effect
; (palette[1] becomes palette[2], palette[2] becomes palette[3], etc.)
            move.w  $dff182,d0          ; Save palette[1]
            move.w  $dff184,$dff182     ; palette[1] <- palette[2]
            move.w  $dff186,$dff184     ; palette[2] <- palette[3]
            ; ... etc through palette[7] ...
            move.w  d0,$dff18e          ; palette[7] <- saved
```

The Copper can additionally swap palettes mid-frame for per-scanline cycling (e.g. waterfall sprite columns where each row uses a different rotated palette).

### PC (VGA)

DAC register manipulation:
- 256-colour palette
- Direct register writes
- Smooth transitions

### C64

The C64 has no programmable palette — its sixteen colours are fixed — but in multicolour and extended-background-colour modes most pixels are not coloured directly. Commodore's Programmer's Reference Guide explains that a multicolour character's bit pairs 00, 01 and 10 are "references to the registers associated with those colors" at `$D021`–`$D023`, and points out the consequence: "Simply changing one of the indirect registers will change every dot drawn in that color. Therefore everything drawn in the screen and background colors can be changed on the whole screen instantly." The guide's own demonstration POKEs a random value into 53282 in a loop. Extended background colour mode adds a fourth register, `$D024`, chosen per character by the top two bits of its screen code, at the price of only 64 usable characters.

That is the mechanism behind the "flashing" text of demo-scene notewriters such as *Face Noter* and *Octanoter*: Richard Bayliss's Codebase64 example switches on extended colour mode and, on each raster interrupt, loads the next entries of three 40-byte tables into `$D022`, `$D023` and `$D024`. Cycling the fourth colour source, colour RAM, means rewriting it, and period BASIC programmers did that too — a 1989 *COMPUTE!'s Gazette* reader tip by Jacques Bingham installs an interrupt that walks a 32-entry table of colour codes through colour RAM behind a title message. Activision's *Paintworks* advertised "color cycling for animation effects" as a feature of a C64 paint program in 1986.

## Limitations

| Constraint | Impact |
|------------|--------|
| Fixed design | Must plan for cycling |
| Colour usage | Dedicates palette entries |
| Art constraints | Limits general colour use |
| Directional only | Can't change shape |

## Famous examples

| Game | Effect |
|------|--------|
| Monkey Island | Ocean waves |
| King's Quest | Waterfalls |
| Deluxe Paint | Animation tool |
| SimCity | Water features |

## See also

- [Amiga Chipset](/vault/hardware/amiga-chipset)
- [Copper](/vault/hardware/copper)
- [Dithering](/vault/techniques/dithering)
