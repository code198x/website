---
ai_generated: true
reviewed: false
title: "Self-Modifying Code"
subtitle: "Code that rewrites itself"
summary: "Self-modifying code changed its own instructions at runtime, enabling impossible optimisations on 8-bit systems by treating code as data."
category: "techniques"
platforms: ['commodore-64', 'sinclair-zx-spectrum', 'nintendo-entertainment-system']
tags: ['optimisation', 'assembly', 'advanced']
sources:
  commodore_basic_chrget:
    - title: "Commodore 64 Programmer's Reference Guide, Commodore Business Machines"
      kind: book
      page: "312"
      note: "Commodore 64 memory map: \"CHRGET 0073-008A 115-138 Subroutine: Get Next Byte of BASIC Text\" — \"CHRGOT 0079 121 Entry to Get Same Byte of Text Again\" — \"TXTPTR 007A-007B 122-123 Pointer: Current Byte of BASIC Text\". The pointer sits inside the subroutine: it is the operand of the routine's own load instruction."
    - title: "Commodore 64 Programmer's Reference Guide, Commodore Business Machines"
      kind: book
      page: "308"
      note: "\"The CHRGET routine is used by BASIC to get each character/token. This makes it simple to add new BASIC commands.\" — \"This technique is often called a wedge.\""
    - title: "Codebase64 — Common Coding Pitfalls: Selfmodifying Madness"
      kind: database
      note: "Community wiki, not manufacturer documentation. \"The 6502 has always been a popular target for self-modifying code. It doesn't have a cache, and in many Situations, self-modifying code is faster then indirect Addressing modes. Even Commodore used self-modifying Code to read Basic-Tokens. Most of the time, the address of an absolute opcode gets modified.\""
  named_c64_uses:
    - title: "C=Hacking issue 5 — The Demo Corner: Stretching Sprites, by Pasi 'Albert' Ojala (mirrored on Codebase64)"
      kind: magazine
      date: "1993-03-07"
      note: "Article dated \"Written: 16-May-91 Translation/Revision 01-Jun-92, Dec-92\". \"The fastest routine I have ever written uses some serious selfmodification tricks.\" — \"[xx] marks selfmodification. For example loop counter, bit mask and index to the stretch and size data tables are stored straight in the code.\""
    - title: "C=Hacking issue 8 — A Different Perspective: Three-Dimensional Graphics on the C64, by Stephen Judd and George Taylor (mirrored on Codebase64)"
      kind: magazine
      date: "1994"
      note: "Line-drawing loop: \"The point here is that it's fast. If you use self-modifying code, you can get this down to 15 cycles per point. If you are clever, you can get it down to 13 cycles per point, excluding plot, worst case.\" — \"For one thing it could be made self-modifying. All variables could be stored in zero page. In fact, the entire routine could be stored in zero page!\""
    - title: "C=Hacking issue 13 — Creating 3-D Dungeon Crawls, by Todd S. Elliott (mirrored on Codebase64)"
      kind: magazine
      date: "1996-07"
      note: "\"it allows the 3-D engine to `unroll' the graphics updating loop using self-modifying code, resulting in speed increases.\" — sample code comments: \"This is the self-modifying code to draw the backdrop.\""
    - title: "C=Hacking issue 21 — Introducing Full-Screen FLI mode for the SuperCPU, by Todd S. Elliott (mirrored on Codebase64)"
      kind: magazine
      date: "2002-02-05"
      note: "\"In the FLI display loop prior to the STA SCROLY command, the current rasterline is used as an index to all three tables. The values are then fetched from the tables and inserted into the code that follows the STA SCROLY command using self-modifying code techniques. When the STA SCROLY happens, the code that immediately follows it starts writing the values onto the databus, all three in a row to complete the first three columns of the screen.\""
    - title: "Codebase64 — Making stable raster routines (C64 and VIC-20), by Marko Mäkelä"
      kind: database
      note: "\"This program is really robust, it installs itself nicely to the interrupt routine chain. It even has an entry point for deinstalling itself. But in its robustness it uses self-modifying code to store the original interrupt routine address.\" — listing: \"oldirq = * + 1 ; Placeholder for self-modifying code\" / \"jmp * ; Return to the original interrupt\""
    - title: "Codebase64 — Double IRQ stable interrupt, sourcecode by Fungus"
      kind: database
      note: "Listing comments: \"sta reseta1 ;Preserve A,X and Y\" / \"stx resetx1 ;Registers\" / \"sty resety1 ;VIA self modifying code (Faster than the STACK is!)\""
    - title: "Codebase64 — Sprite multiplexing, by Cadaver (Lasse Öörni)"
      kind: database
      note: "\"I chose to go for shortness in this routine rather than pure speed. It can be speeded up about 20% by unrolling all the plotting loops and using self modifed code instead of the second set of plotting buffers.\""
    - title: "Codebase64 — Dispatching on a Byte, by White Flame and Krill"
      kind: database
      note: "\"All but the Stack Dispatch routine use self-modification, which will run 1 cycle faster and 1 byte leaner if the dispatch routine itself is in zeropage.\""
    - title: "Codebase64 — Practical Memory Move Routines, by Bruce Clark (info file taken from 6502.org)"
      kind: database
      note: "\"Even more speed can be gained by using self-modifying code, i.e. replacing the (ZeroPage),Y addressing mode with the Absolute,Y addressing mode. This will take 2 fewer cycles per byte.\""
---

On the 6502, indexing memory costs extra cycles. What if the address in your instruction could change dynamically? Self-modifying code rewrote instructions at runtime—changing addresses, operation codes, or branch targets. It was fast, dangerous, and essential for achieving the impossible on 8-bit hardware.

## The problem

Standard indexed addressing:

```asm
    lda table,x      ; 4-5 cycles, X is index
```

But what if you need to change which table?

## The solution

Modify the instruction itself:

```asm
    lda table        ; 4 cycles, absolute addressing

; Elsewhere, change the address:
    lda #<new_table
    sta load_addr+1  ; Modify low byte
    lda #>new_table
    sta load_addr+2  ; Modify high byte
```

Now `lda table` loads from `new_table`.

## Common uses

### Unrolled loops

```asm
    lda $0400        ; First iteration
    sta $d020
    lda $0401        ; Second iteration (address modified)
    sta $d020
    ; ...repeat, modifying addresses
```

### Dynamic branch targets

```asm
jump_target = *+1
    jmp $0000        ; Address modified at runtime
```

### Variable table selection

Switch between data sources without index overhead.

## 6502 advantages

| Factor | Benefit |
|--------|---------|
| Von Neumann | Code and data same memory |
| No cache | No stale instruction problems |
| Absolute addressing | Faster than indexed |

## Examples

### Sprite multiplexer

```asm
    lda #sprite_y_1
    sta store_y+1    ; Modify destination

store_y:
    sta $d001        ; Y position (address changes)
```

### Music player

```asm
    lda pattern_ptr
    sta fetch+1
fetch:
    lda $0000        ; Address modified each note
```

## Dangers

| Risk | Consequence |
|------|-------------|
| Timing bugs | Wrong code executed |
| Debugging | Hard to trace |
| Maintenance | Confusing to read |
| Portability | Platform-specific |

## Z80 differences

The Z80 has no instruction prefetch or cache in the modern sense, so SMC works the same way as on the 6502 — write to the byte that holds an operand, the next M1 fetch sees the new value. The differences are subtler:

- **The R register increments** during M1 cycles. Code that uses `LD A,R` for a "random" seed sees the seed shift if SMC changes instruction lengths nearby.
- **Refresh cycles** put the R register on the address bus during the second half of every M1 — this is invisible to RAM-side SMC but interacts with some peripherals if the address decode is loose.
- **Multi-byte instructions are not atomic.** A `LD (HL),n` or `LD A,(nn)` that targets the *next* instruction's operand byte after the M1 fetch but before its operand fetch produces a hybrid execution. Real Z80 does this faithfully; emulators with instruction-level abstraction can miss it.

In practice, SMC on Z80 is just as common as on 6502 — Spectrum games use it constantly.

## On the Commodore 64

Commodore shipped self-modifying code in the machine's own BASIC. The Programmer's Reference Guide's memory map places the CHRGET subroutine — "Get Next Byte of BASIC Text" — at $0073–$008A in zero page, and TXTPTR, the "Pointer: Current Byte of BASIC Text", at $007A–$007B, inside the routine: the pointer is the operand of CHRGET's own load instruction, and advancing it means rewriting that instruction. Because every token passes through CHRGET, the guide recommends patching it to add commands — "This technique is often called a wedge."

The people who wrote about C64 code in the 1990s used the technique the same way and said so:

| Author | Routine | Use of self-modification |
|--------|---------|--------------------------|
| Pasi 'Albert' Ojala, *C=Hacking* 5 (written 1991) | Sprite stretcher | "The fastest routine I have ever written uses some serious selfmodification tricks" — loop counter, bit mask and table indices "stored straight in the code" |
| Stephen Judd and George Taylor, *C=Hacking* 8 (1994) | 3D line drawing | From 18 cycles per point down to "15 cycles per point" with self-modifying code, 13 "if you are clever" |
| Todd S. Elliott, *C=Hacking* 13 (1996) | 3D dungeon crawl | The engine unrolls "the graphics updating loop using self-modifying code, resulting in speed increases" |
| Todd S. Elliott, *C=Hacking* 21 (2002) | Full-screen FLI on the SuperCPU | Per-rasterline table values are "inserted into the code that follows the STA SCROLY command", so the writes that follow carry the right immediate operands |
| Marko Mäkelä, Codebase64 | Stable raster routine | Stores the original interrupt address as the operand of a `jmp` — "oldirq = * + 1 ; Placeholder for self-modifying code" |
| Fungus, Codebase64 | Double IRQ | Saves A, X and Y into the operands of the restoring loads: "VIA self modifying code (Faster than the STACK is!)" |
| Cadaver (Lasse Öörni), Codebase64 | Sprite multiplexer | "Can be speeded up about 20% by unrolling all the plotting loops and using self modifed code instead of the second set of plotting buffers" |
| White Flame and Krill, Codebase64 | Dispatch on a byte | All but the stack dispatch "use self-modification, which will run 1 cycle faster and 1 byte leaner if the dispatch routine itself is in zeropage" |
| Bruce Clark, 6502.org via Codebase64 | Memory move | Replacing `(zp),Y` with self-modified `abs,Y` addressing takes "2 fewer cycles per byte" |

## Emulator and JIT compatibility

SMC is a chronic source of bugs in emulators that compile blocks of guest code ahead of time:

- **JIT-based emulators** (some PSX, GBA, and N64 cores) cache compiled translations of guest code. When the guest writes to that code, the cache is stale. Robust JITs invalidate the affected block on write — costly, and easy to get wrong if the write granularity doesn't match the block boundary.
- **Cycle-accurate emulators** that step instruction-by-instruction handle SMC for free; the next fetch reads from current memory.
- **Static recompilers** (often used for retro ports) can't handle SMC at all without falling back to interpretation.

This is why retro emulator authors stay loyal to interpretation despite its host-CPU cost — JIT speedups don't survive contact with a self-modifying *Mayhem in Monsterland*.

## Modern perspective

Self-modifying code today:
- **Forbidden by W^X memory protection** — modern OSes mark pages either Writable or eXecutable, never both. Programs that legitimately need SMC (JIT compilers) explicitly toggle protection between phases.
- **JIT compilation** is the modern descendant — generate code at runtime, then mark the page executable.
- **Polymorphic malware** uses SMC to evade signature scanners.
- **Embedded firmware** sometimes still uses SMC where Flash and RAM are unified and tight loops need every cycle.

## Debugging tips

| Technique | Purpose |
|-----------|---------|
| Mark modifications | Comment clearly |
| Initialise explicitly | Don't assume values |
| Test boundaries | Check modified ranges |

## See also

- [6502](/vault/hardware/6502)
- [Lookup Tables](/vault/techniques/lookup-tables)
- [Sprite Multiplexing](/vault/techniques/sprite-multiplexing)
- [Raster Interrupts](/vault/techniques/raster-interrupts)
