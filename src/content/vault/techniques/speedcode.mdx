---
ai_generated: true
reviewed: false
title: "Speedcode"
subtitle: "Loop unrolling, taken to its conclusion"
summary: "Replacing a loop with thousands of straight-line instructions to strip out the cost of looping. On the C64 it was the technique that broke raster-split and DYCP records — and it is usually generated, not written."
category: "techniques"
platforms: ['commodore-64', 'commodore-amiga']
tags: ['optimisation', 'demoscene', 'assembly', 'code-generation']
originated: 1985

sources:
  what_it_is_and_why:
    - title: "Codebase64, 'Speedcode a.k.a. Loop Unrolling', written by Cruzer/CML"
      kind: technical-doc
      page: "base:speedcode"
      note: "The definition and the motive, from a signed article: 'One of the earliest optimization tricks invented was loop unrolling, aka. speedcode... The idea is that instead of a loop, you unroll the inner part of the loop that actually does something, and thereby strip away the administrative costs of the loop logic'"
  what_it_was_invented_for:
    - title: "Codebase64, 'Speedcode a.k.a. Loop Unrolling', written by Cruzer/CML"
      kind: technical-doc
      page: "base:speedcode"
      note: "The technique has a competitive origin rather than an engineering one: 'It was probably first done to get the most rastersplits on the same line, and then later utilized to break DYCP records'. Speedcode exists because the scene kept score"
  it_is_generated_not_written:
    - title: "Codebase64, 'Speedcode a.k.a. Loop Unrolling', written by Cruzer/CML"
      kind: technical-doc
      page: "base:speedcode"
      note: "⚠ The article's structure shows that hand-writing unrolled code is not the practice. Its sections are 'The slave method', 'Making the computer code for you', 'Scripting/Macros', 'Runtime Code Generators', 'Optimizing the Speedcode Generator' and 'What to do if running short on memory?' - five of six are about generating the code, and the last is about the cost of having done so"
  the_tradeoff_is_memory:
    - title: "Codebase64, 'Speedcode a.k.a. Loop Unrolling', written by Cruzer/CML"
      kind: technical-doc
      page: "base:speedcode"
      note: "The article ends on the constraint that bounds the technique - 'What to do if running short on memory?' Unrolling trades RAM for cycles, and on a 64K machine that trade runs out"
  it_shows_up_in_the_records:
    - title: "Codebase64, 'Demo records and world firsts' (vic:demo namespace)"
      kind: database
      page: "Record performance of effects"
      note: "The record categories speedcode was built to win are logged with dates and counts: 'Most plots at 50 fps' runs from Bones's The Larch 3 at 600 sine-dots (30 December 1989) through Nato's Wriggler at 672 (February 1990) and Oxyron's Natural Wonders at 960 (2007) to Camelot's Pimp My Snail at 1536 (August 2011)"
---

---

**Speedcode** is loop unrolling with the brakes off: instead of looping over an
operation, you emit the operation thousands of times in a row, so the processor
never pays for the loop.

> The idea is that instead of a loop, you "unroll" the inner part of the loop that
> actually does something, and thereby strip away the administrative costs of the
> loop logic.

On a 6510 the administrative cost is real — a decrement and a branch on every
iteration, against a store that might be three cycles.

## It was invented to win competitions

⚠ **The origin is competitive, not engineering.** Cruzer/CML's article says so
directly:

> It was probably first done to get the most rastersplits on the same line, and then
> later utilized to break DYCP records.

Both of those are scoreboard categories. The record tables bear it out — "most plots
at 50 fps" runs from **Bones**'s *The Larch 3* at 600 sine-dots in December 1989,
through **Nato**'s *Wriggler* at 672 and **Oxyron**'s *Natural Wonders* at 960, to
**Camelot**'s *Pimp My Snail* at 1,536 in 2011. Twenty-two years of the same
argument.

## Nobody writes it

The technique's own reference article is mostly about not writing it by hand. Its
sections:

- The slave method
- Making the computer code for you
- Scripting/Macros
- Runtime Code Generators
- Optimizing the Speedcode Generator
- What to do if running short on memory?

Five of the six are about **generating** the code. Speedcode is a compilation
target, produced by a script or by a generator running on the C64 itself.

## What it costs

The last section is the catch. Unrolling trades memory for cycles, and a 64K machine
runs out of memory long before it runs out of ambition — which is why the article
ends by asking what to do "if running short on memory".

## See also

- [Self-modifying code](/vault/techniques/self-modifying-code) — the neighbouring trade
- [Stable raster](/vault/techniques/stable-raster)
- [Size coding](/vault/techniques/size-coding) — the opposite discipline

