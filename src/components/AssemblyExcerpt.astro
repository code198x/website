---
import CodeFromFile from './CodeFromFile.astro';
interface Props {checkpoint:string; from:string; to:string;}
const {checkpoint,from,to}=Astro.props;
---
<CodeFromFile src={`sinclair-zx-spectrum/assembly/meteor-storm/checkpoints/${checkpoint}/meteor-storm.asm`} {from} {to} />
