---
/**
 * SystemHero — platform-branded hero for system landing pages, in the house
 * style: flat, square, a platform-colour rule for identity, a chunky specs box.
 * Two grounds — `paper` (light, the default, used by every platform page) and
 * `dark` (the CRASH! Live event masthead) — share the same geometry so the site
 * reads as one system. The old neon/animated treatment (floating shapes,
 * scanlines, beam, gradient/stripes/copper variants) was retired: only these two
 * grounds were ever used.
 *
 * Usage:
 *   <SystemHero
 *     title="Commodore 64"
 *     subtitle="Where bedroom coders became legends"
 *     platformColor="#4a4dff"
 *     specs={[{ label: 'CPU', value: 'MOS 6510' }, ...]}
 *   />
 */
interface Spec {
  label: string;
  value: string;
}

interface Props {
  title: string;
  subtitle?: string;
  kicker?: string;
  platformColor: string;
  specs?: Spec[];
  variant?: 'paper' | 'dark';
  rainbowStripe?: boolean;
}

const {
  title,
  subtitle = 'Learn programming by building games',
  kicker,
  platformColor,
  specs = [],
  variant = 'paper',
  rainbowStripe = false,
} = Astro.props;
import { inlineMarkdown } from '../lib/inline-markdown';
---

<section
  class:list={['system-hero', `variant-${variant}`, { 'has-rainbow': rainbowStripe }]}
  style={`--platform-color: ${platformColor};`}
>
  <div class="hero-content">
    {kicker && <p class="hero-kicker" set:html={inlineMarkdown(kicker)} />}
    <h1 class="system-title" set:html={inlineMarkdown(title)} />
    <div class="hero-rule"></div>
    {subtitle && <p class="system-subtitle" set:html={inlineMarkdown(subtitle)} />}
    {specs.length > 0 && (
      <div class="system-specs">
        {specs.map((spec) => (
          <div class="spec-item">
            <div class="spec-label">{spec.label}</div>
            <div class="spec-value">{spec.value}</div>
          </div>
        ))}
      </div>
    )}
  </div>
</section>

<style>
  .system-hero {
    position: relative;
    overflow: hidden;
    background: transparent;               /* let the page paper show through */
    padding: var(--space-12) var(--space-4);
    text-align: center;
  }
  .hero-content { position: relative; z-index: 1; max-width: 900px; margin: 0 auto; }

  .hero-kicker {
    font-family: var(--font-family-mono); font-size: 0.78rem; font-weight: 700;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--color-text-secondary);
    margin: 0 0 var(--space-3);
  }
  .system-title {
    font-family: var(--font-family-read); font-weight: 700;
    font-size: clamp(3rem, 6vw, var(--h-text-display)); line-height: var(--h-lh-display);
      /* Reserve the descender the tight line-height does not: Literata's ink
         drops ~0.33em past the box, which collided with the strapline. */
      padding-bottom: 0.25em;
    margin: 0; color: var(--h-ink); letter-spacing: 0;
  }
  /* Platform identity: a chunky rule in the machine's colour, with a house tail. */
  .hero-rule {
    height: 9px; width: 220px; max-width: 60%; margin: var(--space-4) auto 0;
    background: var(--platform-color); position: relative;
  }
  .hero-rule::after {
    content: ""; position: absolute; right: 0; top: 0; bottom: 0; width: 22%;
    background: var(--color-text); opacity: 0.25;
  }
  .system-subtitle {
    font-size: 1.2rem; color: var(--color-text-secondary); margin: var(--space-4) 0 0;
  }

  .system-specs {
    display: inline-flex; gap: var(--space-6); flex-wrap: wrap; justify-content: center;
    margin-top: var(--space-6); padding: var(--space-4) var(--space-8);
    background: var(--color-surface); border: 3px solid var(--color-text); box-shadow: var(--shadow-chunky);
  }
  .spec-item { text-align: center; }
  .spec-label {
    font-family: var(--font-family-mono); font-size: 0.66rem; text-transform: uppercase;
    letter-spacing: 0.1em; color: var(--color-text-secondary);
  }
  .spec-value {
    font-family: var(--font-family-mono); font-size: 1rem; font-weight: 700;
    color: var(--color-text); margin-top: 3px;
  }

  /* Dark ground — the CRASH! Live event masthead. Same geometry, dark surface. */
  .variant-dark { background: #17120d; }
  .variant-dark .system-title { color: #f6efe2; }
  .variant-dark .hero-kicker { color: color-mix(in srgb, var(--platform-color) 55%, #f6efe2); }
  .variant-dark .hero-rule::after { background: #f6efe2; }
  .variant-dark .system-subtitle { color: #cbb8a6; }
  .variant-dark .system-specs { background: rgba(255, 255, 255, 0.04); border-color: #f6efe2; box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.5); }
  .variant-dark .spec-label { color: #cbb8a6; }
  .variant-dark .spec-value { color: #f6efe2; }

  /* Rainbow stripe (Spectrum / CRASH! Live branding) */
  .has-rainbow::after {
    content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 10px; z-index: 10;
    background: linear-gradient(90deg,
      #00ff00 0%, #00ff00 14.28%, #ffff00 14.28%, #ffff00 28.57%,
      #00ffff 28.57%, #00ffff 42.85%, #ff0000 42.85%, #ff0000 57.14%,
      #ff00ff 57.14%, #ff00ff 71.42%, #00ff00 71.42%, #00ff00 85.71%,
      #00ffff 85.71%, #00ffff 100%);
  }

  @media (max-width: 768px) {
    .system-hero { padding: var(--space-10) var(--space-4); }
    .system-specs { gap: var(--space-4); padding: var(--space-3) var(--space-4); }
  }
</style>
