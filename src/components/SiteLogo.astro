---
const { variant = "stacked", linkTo = "/", width = 160 } = Astro.props;
---

<a class="brand" href={linkTo} aria-label="Code Like It's 198x">
  <picture>
    <source srcset={`/logo-${variant}.svg`} media="(prefers-color-scheme: dark)" />
    <img src={`/logo-${variant}.svg`} alt="Code Like It's 198x logo" width={width} height="auto" />
  </picture>
</a>