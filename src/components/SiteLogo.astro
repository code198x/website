---
const { variant = "stacked", linkTo = "/" } = Astro.props;
---

<a class="brand" href={linkTo} aria-label="Code Like It's 198x">
  <picture>
    <source srcset={`/images/logo-${variant}.svg`} media="(prefers-color-scheme: dark)" />
    <img src={`/images/logo-${variant}.svg`} alt="Code Like It's 198x logo" width="240" height="auto" />
  </picture>
</a>
