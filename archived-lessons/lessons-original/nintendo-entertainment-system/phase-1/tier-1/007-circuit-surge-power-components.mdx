---
title: "Circuit Surge: Power Components"
system: "nintendo-entertainment-system"
phase_number: 1
tier_number: 1
lesson_number: 7
description: "Add collectible power components and electrical enhancements to Circuit Surge! Create component power-ups and voltage boosts while learning 6502 conditional branching through component management and enhancement systems."
learning_objectives:
  - "Create collectible power components and electrical enhancement systems with component tracking"
  - "Add voltage boosts and component power-ups with effect management and duration tracking"
  - "Learn 6502 conditional branching through component storage and electrical enhancement logic"
  - "Build balanced electrical enhancement systems with component mechanics and voltage limitations"
  - "Experience authentic NES enhancement with precise electrical component effects and APU feedback"
concepts:
  - "Power component programming and electrical enhancement system architecture"
  - "6502 conditional branching for component storage and electrical enhancement logic"
  - "Electrical effect management and systematic component enhancement organization"
  - "Electrical enhancement balance mechanics with component interaction systems"
  - "Authentic NES enhancement presentation with APU electrical component effects and voltage audio"
estimated_duration: "30-45 minutes"
difficulty: "easy"
code_examples: true
practical_exercise: true
order: 7
---

# Lesson 7: Circuit Surge - Power Components

**Your electrical circuits need incredible power components!** In the last lesson, you added electrical mastery and circuit progression to your combat arena. Today, you'll create power component and enhancement systems - collectible electrical power-ups, voltage boosts, and component enhancements that transform your electrical mastery experience into an engaging electrical component enhancement adventure!

## Your Power Components Energize

Let's start with the magic - adding power components and enhancement systems to your electrical mastery world:

```nasm
; Circuit Surge - Power Components Creation
.segment "HEADER"
.byte "NES", $1A
.byte $02, $01
.byte $00, $00

.segment "CODE"
RESET:
    ; Initialize NES for precise electrical component control
    SEI
    CLD
    LDX #$FF
    TXS

    ; Set up enhanced electrical mastery world from Lessons 1-6
    LDA #$3F            ; Point to palette
    STA $2006           ; PPU address high
    LDA #$00            ; Point to palette start
    STA $2006           ; PPU address low

    ; Enhanced circuit colors with component elements
    LDA #$0F            ; Black background
    STA $2007           ; Write palette
    LDA #$30            ; White conductors
    STA $2007           ; Write palette
    LDA #$16            ; Red electrical components
    STA $2007           ; Write palette
    LDA #$27            ; Yellow voltage enhancements
    STA $2007           ; Write palette

    ; Initialize electrical mastery systems from previous lessons
    JSR InitVoltageMastery
    JSR InitElectricalProgression
    JSR InitCircuitAdvancement

    ; Initialize power component system
    JSR InitPowerComponents

    ; Initialize electrical enhancements
    JSR InitElectricalEnhancements

    ; Set up component enhancement tracking
    JSR InitComponentEnhancement

    ; Initialize component management
    JSR InitComponentManagement

    ; Main power component loop
MainLoop:
    JSR ReadCombatInput
    JSR UpdateConductorCombat
    JSR UpdateWeaponSystems
    JSR UpdateCircuitObstacles
    JSR CheckElectricalCollisions
    JSR UpdatePowerComponents
    JSR CheckComponentPickups
    JSR UpdateElectricalEnhancements
    JSR CheckVoltageMasteryConditions
    JSR UpdateComponentEffects
    JMP MainLoop

; Initialize power component system using conditional branching
InitPowerComponents:
    ; Set up component enhancement variables using branching logic
    LDA #$00            ; No components enhanced initially
    STA $2000           ; Resistor component enhanced flag
    STA $2001           ; Capacitor component enhanced flag
    STA $2002           ; Inductor component enhanced flag
    STA $2003           ; Transformer component enhanced flag
    STA $2004           ; Oscillator component enhanced flag
    STA $2005           ; Amplifier component enhanced flag
    STA $2006           ; Generator component enhanced flag
    STA $2007           ; Processor component enhanced flag

    ; Initialize component availability using conditional logic
    LDA #$01            ; All components available initially
    STA $2010           ; Resistor component available flag
    STA $2011           ; Capacitor component available flag
    STA $2012           ; Inductor component available flag
    STA $2013           ; Transformer component available flag
    STA $2014           ; Oscillator component available flag
    STA $2015           ; Amplifier component available flag
    STA $2016           ; Generator component available flag
    STA $2017           ; Processor component available flag

    ; Set component types using conditional organization
    LDA #$01            ; Resistor type
    STA $2020           ; Store resistor type
    LDA #$02            ; Capacitor type
    STA $2021           ; Store capacitor type
    LDA #$03            ; Inductor type
    STA $2022           ; Store inductor type
    LDA #$04            ; Transformer type
    STA $2023           ; Store transformer type
    LDA #$05            ; Oscillator type
    STA $2024           ; Store oscillator type
    LDA #$06            ; Amplifier type
    STA $2025           ; Store amplifier type
    LDA #$07            ; Generator type
    STA $2026           ; Store generator type
    LDA #$08            ; Processor type
    STA $2027           ; Store processor type

    ; Initialize component positions using conditional setup
    JSR InitComponentPositions

    ; Initialize component counters using conditional logic
    LDA #$00            ; Clear counters
    STA $2030           ; Components collected
    STA $2031           ; Active components
    STA $2032           ; Component combo
    STA $2033           ; Component streak

    RTS

; Initialize component positions using conditional branching
InitComponentPositions:
    ; Set component positions using conditional logic
    LDA #$0C            ; Resistor X position
    STA $2040           ; Store position
    LDA #$06            ; Resistor Y position
    STA $2041           ; Store position

    LDA #$18            ; Capacitor X position
    STA $2042           ; Store position
    LDA #$09            ; Capacitor Y position
    STA $2043           ; Store position

    LDA #$24            ; Inductor X position
    STA $2044           ; Store position
    LDA #$0C            ; Inductor Y position
    STA $2045           ; Store position

    LDA #$14            ; Transformer X position
    STA $2046           ; Store position
    LDA #$12            ; Transformer Y position
    STA $2047           ; Store position

    LDA #$30            ; Oscillator X position
    STA $2048           ; Store position
    LDA #$0F            ; Oscillator Y position
    STA $2049           ; Store position

    LDA #$20            ; Amplifier X position
    STA $204A           ; Store position
    LDA #$15            ; Amplifier Y position
    STA $204B           ; Store position

    LDA #$2C            ; Generator X position
    STA $204C           ; Store position
    LDA #$18            ; Generator Y position
    STA $204D           ; Store position

    LDA #$1C            ; Processor X position
    STA $204E           ; Store position
    LDA #$1B            ; Processor Y position
    STA $204F           ; Store position

    RTS

; Initialize electrical enhancement system using conditional branching
InitElectricalEnhancements:
    ; Set up enhancement variables using conditional logic
    LDA #$01            ; Base enhancement level
    STA $2060           ; Resistor enhancement level
    STA $2061           ; Capacitor enhancement level
    STA $2062           ; Inductor enhancement level
    STA $2063           ; Transformer enhancement level
    STA $2064           ; Oscillator enhancement level
    STA $2065           ; Amplifier enhancement level
    STA $2066           ; Generator enhancement level
    STA $2067           ; Processor enhancement level

    ; Initialize enhancement durations using conditional setup
    LDA #$78            ; Standard enhancement duration (120 frames)
    STA $2070           ; Resistor enhancement duration
    STA $2071           ; Capacitor enhancement duration
    STA $2072           ; Inductor enhancement duration
    STA $2073           ; Transformer enhancement duration
    STA $2074           ; Oscillator enhancement duration
    STA $2075           ; Amplifier enhancement duration
    STA $2076           ; Generator enhancement duration
    STA $2077           ; Processor enhancement duration

    ; Initialize enhancement timers using conditional logic
    LDA #$00            ; No timers active initially
    STA $2080           ; Resistor enhancement timer
    STA $2081           ; Capacitor enhancement timer
    STA $2082           ; Inductor enhancement timer
    STA $2083           ; Transformer enhancement timer
    STA $2084           ; Oscillator enhancement timer
    STA $2085           ; Amplifier enhancement timer
    STA $2086           ; Generator enhancement timer
    STA $2087           ; Processor enhancement timer

    ; Initialize enhancement multipliers using conditional organization
    LDA #$01            ; Base multiplier
    STA $2090           ; Resistor multiplier
    STA $2091           ; Capacitor multiplier
    STA $2092           ; Inductor multiplier
    STA $2093           ; Transformer multiplier
    STA $2094           ; Oscillator multiplier
    STA $2095           ; Amplifier multiplier
    STA $2096           ; Generator multiplier
    STA $2097           ; Processor multiplier

    RTS

; Initialize component enhancement tracking using conditional logic
InitComponentEnhancement:
    ; Set up component enhancement tracking using conditional branching
    LDA #$00            ; No enhancements active initially
    STA $20A0           ; Voltage enhancement flag
    STA $20A1           ; Current enhancement flag
    STA $20A2           ; Resistance enhancement flag
    STA $20A3           ; Frequency enhancement flag
    STA $20A4           ; Amplification enhancement flag

    ; Initialize enhancement effect values using conditional setup
    LDA #$18            ; Resistor component effect
    STA $20B0           ; Store effect value
    LDA #$14            ; Capacitor component effect
    STA $20B1           ; Store effect value
    LDA #$20            ; Inductor component effect
    STA $20B2           ; Store effect value
    LDA #$16            ; Transformer component effect
    STA $20B3           ; Store effect value
    LDA #$24            ; Oscillator component effect
    STA $20B4           ; Store effect value
    LDA #$1C            ; Amplifier component effect
    STA $20B5           ; Store effect value
    LDA #$28            ; Generator component effect
    STA $20B6           ; Store effect value
    LDA #$30            ; Processor component effect
    STA $20B7           ; Store effect value

    RTS

; Initialize component management using conditional branching
InitComponentManagement:
    ; Set up component management using conditional logic
    LDA #$0A            ; Component pickup range
    STA $20C0           ; Store pickup range
    LDA #$05            ; Component magnetism strength
    STA $20C1           ; Store magnetism strength

    ; Initialize component interaction ranges using conditional setup
    LDA #$08            ; Resistor interaction range
    STA $20D0           ; Store range
    LDA #$0A            ; Capacitor interaction range
    STA $20D1           ; Store range
    LDA #$0C            ; Inductor interaction range
    STA $20D2           ; Store range
    LDA #$09            ; Transformer interaction range
    STA $20D3           ; Store range
    LDA #$0E            ; Oscillator interaction range
    STA $20D4           ; Store range
    LDA #$0B            ; Amplifier interaction range
    STA $20D5           ; Store range
    LDA #$10            ; Generator interaction range
    STA $20D6           ; Store range
    LDA #$14            ; Processor interaction range
    STA $20D7           ; Store range

    RTS

; Update power component system using conditional branching
UpdatePowerComponents:
    ; Update all active components using conditional logic
    JSR UpdateResistorComponent
    JSR UpdateCapacitorComponent
    JSR UpdateInductorComponent
    JSR UpdateTransformerComponent
    JSR UpdateOscillatorComponent
    JSR UpdateAmplifierComponent
    JSR UpdateGeneratorComponent
    JSR UpdateProcessorComponent

    ; Update component combination systems using conditional branching
    JSR UpdateComponentComboSystem
    JSR CheckComponentInteractions

    RTS

; Update resistor component using conditional branching
UpdateResistorComponent:
    ; Check if resistor component is active using conditional logic
    LDA $2000           ; Load resistor enhancement flag
    BEQ resistor_component_done ; Branch if not active

    ; Update resistor timer using conditional branching
    LDA $2080           ; Load resistor timer
    BEQ deactivate_resistor ; Branch if timer expired
    SEC                 ; Set carry for subtraction
    SBC #$01            ; Decrement timer
    STA $2080           ; Store updated timer

    ; Apply resistor enhancement effects using conditional logic
    JSR ApplyResistorEnhancement
    JMP resistor_component_done

deactivate_resistor:
    ; Deactivate resistor component using conditional branching
    LDA #$00            ; Clear enhancement flag
    STA $2000           ; Store cleared flag
    LDA #$01            ; Reset multiplier to base
    STA $2090           ; Store base multiplier

    ; Play component deactivation sound
    JSR PlayComponentDeactivateSound

resistor_component_done:
    RTS

; Update capacitor component using conditional branching
UpdateCapacitorComponent:
    ; Check if capacitor component is active using conditional logic
    LDA $2001           ; Load capacitor enhancement flag
    BEQ capacitor_component_done ; Branch if not active

    ; Update capacitor timer using conditional branching
    LDA $2081           ; Load capacitor timer
    BEQ deactivate_capacitor ; Branch if timer expired
    SEC                 ; Set carry for subtraction
    SBC #$01            ; Decrement timer
    STA $2081           ; Store updated timer

    ; Apply capacitor enhancement effects using conditional logic
    JSR ApplyCapacitorEnhancement
    JMP capacitor_component_done

deactivate_capacitor:
    ; Deactivate capacitor component using conditional branching
    LDA #$00            ; Clear enhancement flag
    STA $2001           ; Store cleared flag
    LDA #$01            ; Reset multiplier to base
    STA $2091           ; Store base multiplier

capacitor_component_done:
    RTS

; Update inductor component using conditional branching
UpdateInductorComponent:
    ; Check if inductor component is active using conditional logic
    LDA $2002           ; Load inductor enhancement flag
    BEQ inductor_component_done ; Branch if not active

    ; Update inductor timer using conditional branching
    LDA $2082           ; Load inductor timer
    BEQ deactivate_inductor ; Branch if timer expired
    SEC                 ; Set carry for subtraction
    SBC #$01            ; Decrement timer
    STA $2082           ; Store updated timer

    ; Apply inductor enhancement effects using conditional logic
    JSR ApplyInductorEnhancement
    JMP inductor_component_done

deactivate_inductor:
    ; Deactivate inductor component using conditional branching
    LDA #$00            ; Clear enhancement flag
    STA $2002           ; Store cleared flag
    LDA #$01            ; Reset multiplier to base
    STA $2092           ; Store base multiplier

inductor_component_done:
    RTS

; Update remaining components using conditional branching (simplified for space)
UpdateTransformerComponent:
    LDA $2003           ; Load transformer enhancement flag
    BEQ transformer_done ; Branch if not active
    LDA $2083           ; Load transformer timer
    BEQ deactivate_transformer ; Branch if expired
    DEC $2083           ; Decrement timer
    JSR ApplyTransformerEnhancement
    JMP transformer_done
deactivate_transformer:
    LDA #$00            ; Clear enhancement flag
    STA $2003           ; Store cleared flag
transformer_done:
    RTS

UpdateOscillatorComponent:
    LDA $2004           ; Load oscillator enhancement flag
    BEQ oscillator_done ; Branch if not active
    LDA $2084           ; Load oscillator timer
    BEQ deactivate_oscillator ; Branch if expired
    DEC $2084           ; Decrement timer
    JSR ApplyOscillatorEnhancement
    JMP oscillator_done
deactivate_oscillator:
    LDA #$00            ; Clear enhancement flag
    STA $2004           ; Store cleared flag
oscillator_done:
    RTS

UpdateAmplifierComponent:
    LDA $2005           ; Load amplifier enhancement flag
    BEQ amplifier_done  ; Branch if not active
    LDA $2085           ; Load amplifier timer
    BEQ deactivate_amplifier ; Branch if expired
    DEC $2085           ; Decrement timer
    JSR ApplyAmplifierEnhancement
    JMP amplifier_done
deactivate_amplifier:
    LDA #$00            ; Clear enhancement flag
    STA $2005           ; Store cleared flag
amplifier_done:
    RTS

UpdateGeneratorComponent:
    LDA $2006           ; Load generator enhancement flag
    BEQ generator_done  ; Branch if not active
    LDA $2086           ; Load generator timer
    BEQ deactivate_generator ; Branch if expired
    DEC $2086           ; Decrement timer
    JSR ApplyGeneratorEnhancement
    JMP generator_done
deactivate_generator:
    LDA #$00            ; Clear enhancement flag
    STA $2006           ; Store cleared flag
generator_done:
    RTS

UpdateProcessorComponent:
    LDA $2007           ; Load processor enhancement flag
    BEQ processor_done  ; Branch if not active
    LDA $2087           ; Load processor timer
    BEQ deactivate_processor ; Branch if expired
    DEC $2087           ; Decrement timer
    JSR ApplyProcessorEnhancement
    JMP processor_done
deactivate_processor:
    LDA #$00            ; Clear enhancement flag
    STA $2007           ; Store cleared flag
processor_done:
    RTS

; Check component pickups using conditional branching
CheckComponentPickups:
    ; Check collision with all components using conditional logic
    JSR CheckResistorPickup
    JSR CheckCapacitorPickup
    JSR CheckInductorPickup
    JSR CheckTransformerPickup
    JSR CheckOscillatorPickup
    JSR CheckAmplifierPickup
    JSR CheckGeneratorPickup
    JSR CheckProcessorPickup

    RTS

; Check resistor pickup using conditional branching
CheckResistorPickup:
    ; Check if resistor is available using conditional logic
    LDA $2010           ; Load resistor available flag
    BEQ resistor_pickup_done ; Branch if not available

    ; Check distance to resistor using conditional branching
    LDA $80             ; Load conductor X
    SEC                 ; Set carry for subtraction
    SBC $2040           ; Subtract resistor X
    BCS resistor_x_positive ; Branch if positive
    EOR #$FF            ; Make positive (complement)
    CLC                 ; Clear carry
    ADC #$01            ; Add 1 (two's complement)

resistor_x_positive:
    CMP $20C0           ; Compare with pickup range
    BCS resistor_pickup_done ; Branch if too far

    ; Check Y distance using conditional logic
    LDA $81             ; Load conductor Y
    SEC                 ; Set carry for subtraction
    SBC $2041           ; Subtract resistor Y
    BCS resistor_y_positive ; Branch if positive
    EOR #$FF            ; Make positive (complement)
    CLC                 ; Clear carry
    ADC #$01            ; Add 1 (two's complement)

resistor_y_positive:
    CMP $20C0           ; Compare with pickup range
    BCS resistor_pickup_done ; Branch if too far

    ; Resistor pickup detected using conditional logic
    JSR PickupResistor

resistor_pickup_done:
    RTS

; Check capacitor pickup using conditional branching
CheckCapacitorPickup:
    ; Check if capacitor is available using conditional logic
    LDA $2011           ; Load capacitor available flag
    BEQ capacitor_pickup_done ; Branch if not available

    ; Simplified distance check for capacitor using conditional logic
    LDA $80             ; Load conductor X
    CMP $2042           ; Compare with capacitor X
    BNE capacitor_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $2043           ; Compare with capacitor Y
    BNE capacitor_pickup_done ; Branch if different Y

    ; Capacitor pickup detected
    JSR PickupCapacitor

capacitor_pickup_done:
    RTS

; Similar pickup checks for remaining components (simplified for space)
CheckInductorPickup:
    LDA $2012           ; Load inductor available flag
    BEQ inductor_pickup_done ; Branch if not available
    LDA $80             ; Load conductor X
    CMP $2044           ; Compare with inductor X
    BNE inductor_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $2045           ; Compare with inductor Y
    BNE inductor_pickup_done ; Branch if different Y
    JSR PickupInductor
inductor_pickup_done:
    RTS

CheckTransformerPickup:
    LDA $2013           ; Load transformer available flag
    BEQ transformer_pickup_done ; Branch if not available
    LDA $80             ; Load conductor X
    CMP $2046           ; Compare with transformer X
    BNE transformer_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $2047           ; Compare with transformer Y
    BNE transformer_pickup_done ; Branch if different Y
    JSR PickupTransformer
transformer_pickup_done:
    RTS

CheckOscillatorPickup:
    LDA $2014           ; Load oscillator available flag
    BEQ oscillator_pickup_done ; Branch if not available
    LDA $80             ; Load conductor X
    CMP $2048           ; Compare with oscillator X
    BNE oscillator_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $2049           ; Compare with oscillator Y
    BNE oscillator_pickup_done ; Branch if different Y
    JSR PickupOscillator
oscillator_pickup_done:
    RTS

CheckAmplifierPickup:
    LDA $2015           ; Load amplifier available flag
    BEQ amplifier_pickup_done ; Branch if not available
    LDA $80             ; Load conductor X
    CMP $204A           ; Compare with amplifier X
    BNE amplifier_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $204B           ; Compare with amplifier Y
    BNE amplifier_pickup_done ; Branch if different Y
    JSR PickupAmplifier
amplifier_pickup_done:
    RTS

CheckGeneratorPickup:
    LDA $2016           ; Load generator available flag
    BEQ generator_pickup_done ; Branch if not available
    LDA $80             ; Load conductor X
    CMP $204C           ; Compare with generator X
    BNE generator_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $204D           ; Compare with generator Y
    BNE generator_pickup_done ; Branch if different Y
    JSR PickupGenerator
generator_pickup_done:
    RTS

CheckProcessorPickup:
    LDA $2017           ; Load processor available flag
    BEQ processor_pickup_done ; Branch if not available
    LDA $80             ; Load conductor X
    CMP $204E           ; Compare with processor X
    BNE processor_pickup_done ; Branch if different X
    LDA $81             ; Load conductor Y
    CMP $204F           ; Compare with processor Y
    BNE processor_pickup_done ; Branch if different Y
    JSR PickupProcessor
processor_pickup_done:
    RTS

; Pickup resistor component using conditional branching
PickupResistor:
    ; Mark resistor as picked up using conditional logic
    LDA #$00            ; Clear available flag
    STA $2010           ; Store cleared flag

    ; Activate resistor enhancement using conditional branching
    LDA #$01            ; Set enhancement flag
    STA $2000           ; Store enhancement flag
    LDA $2070           ; Load resistor enhancement duration
    STA $2080           ; Store in resistor timer

    ; Apply resistor boost using conditional logic
    LDA $2090           ; Load current resistor multiplier
    CLC                 ; Clear carry
    ADC #$01            ; Increase multiplier
    CMP #$04            ; Check maximum multiplier
    BCC store_resistor_multiplier ; Store if within limit
    LDA #$04            ; Clamp to maximum

store_resistor_multiplier:
    STA $2090           ; Store updated multiplier

    ; Update pickup counters using conditional logic
    INC $2030           ; Increment components collected
    INC $2031           ; Increment active components
    INC $2032           ; Increment component combo

    ; Award pickup bonus score
    LDA #$07            ; Component bonus points
    JSR AwardElectricalPoints

    ; Play resistor pickup sound
    JSR PlayResistorPickupSound

    ; Check for component combo bonus
    JSR CheckComponentCombo

    RTS

; Pickup capacitor component using conditional branching
PickupCapacitor:
    ; Mark capacitor as picked up using conditional logic
    LDA #$00            ; Clear available flag
    STA $2011           ; Store cleared flag

    ; Activate capacitor enhancement using conditional branching
    LDA #$01            ; Set enhancement flag
    STA $2001           ; Store enhancement flag
    LDA $2071           ; Load capacitor enhancement duration
    STA $2081           ; Store in capacitor timer

    ; Apply immediate capacitor boost using conditional logic
    LDA $A1             ; Load current charge
    CLC                 ; Clear carry
    ADC $20B1           ; Add capacitor boost value
    CMP #$64            ; Check maximum charge
    BCC store_capacitor_boost ; Store if within limit
    LDA #$64            ; Clamp to maximum

store_capacitor_boost:
    STA $A1             ; Store updated charge

    ; Update pickup counters using conditional logic
    INC $2030           ; Increment components collected
    INC $2031           ; Increment active components
    INC $2032           ; Increment component combo

    ; Play capacitor pickup sound
    JSR PlayCapacitorPickupSound

    RTS

; Pickup inductor component using conditional branching
PickupInductor:
    ; Mark inductor as picked up using conditional logic
    LDA #$00            ; Clear available flag
    STA $2012           ; Store cleared flag

    ; Activate inductor enhancement using conditional branching
    LDA #$01            ; Set enhancement flag
    STA $2002           ; Store enhancement flag
    LDA $2072           ; Load inductor enhancement duration
    STA $2082           ; Store in inductor timer

    ; Apply immediate inductor boost using conditional logic
    LDA $A0             ; Load current voltage
    CLC                 ; Clear carry
    ADC $20B2           ; Add inductor boost value
    CMP #$64            ; Check maximum voltage
    BCC store_inductor_boost ; Store if within limit
    LDA #$64            ; Clamp to maximum

store_inductor_boost:
    STA $A0             ; Store updated voltage

    ; Update pickup counters using conditional logic
    INC $2030           ; Increment components collected
    INC $2031           ; Increment active components
    INC $2032           ; Increment component combo

    ; Play inductor pickup sound
    JSR PlayInductorPickupSound

    RTS

; Pickup remaining components using conditional branching (simplified for space)
PickupTransformer:
    LDA #$00            ; Clear available flag
    STA $2013           ; Store cleared flag
    LDA #$01            ; Set enhancement flag
    STA $2003           ; Store enhancement flag
    INC $2030           ; Increment components collected
    JSR PlayTransformerPickupSound
    RTS

PickupOscillator:
    LDA #$00            ; Clear available flag
    STA $2014           ; Store cleared flag
    LDA #$01            ; Set enhancement flag
    STA $2004           ; Store enhancement flag
    INC $2030           ; Increment components collected
    JSR PlayOscillatorPickupSound
    RTS

PickupAmplifier:
    LDA #$00            ; Clear available flag
    STA $2015           ; Store cleared flag
    LDA #$01            ; Set enhancement flag
    STA $2005           ; Store enhancement flag
    INC $2030           ; Increment components collected
    JSR PlayAmplifierPickupSound
    RTS

PickupGenerator:
    LDA #$00            ; Clear available flag
    STA $2016           ; Store cleared flag
    LDA #$01            ; Set enhancement flag
    STA $2006           ; Store enhancement flag
    INC $2030           ; Increment components collected
    JSR PlayGeneratorPickupSound
    RTS

PickupProcessor:
    LDA #$00            ; Clear available flag
    STA $2017           ; Store cleared flag
    LDA #$01            ; Set enhancement flag
    STA $2007           ; Store enhancement flag

    ; Activate all electrical enhancements using conditional logic
    LDA #$01            ; Set all enhancement flags
    STA $2000           ; Resistor enhancement
    STA $2001           ; Capacitor enhancement
    STA $2002           ; Inductor enhancement
    STA $2003           ; Transformer enhancement
    STA $2004           ; Oscillator enhancement
    STA $2005           ; Amplifier enhancement
    STA $2006           ; Generator enhancement

    ; Set extended durations using conditional logic
    LDA #$B4            ; Extended duration (180 frames)
    STA $2080           ; Resistor timer
    STA $2081           ; Capacitor timer
    STA $2082           ; Inductor timer
    STA $2083           ; Transformer timer
    STA $2084           ; Oscillator timer
    STA $2085           ; Amplifier timer
    STA $2086           ; Generator timer
    STA $2087           ; Processor timer

    INC $2030           ; Increment components collected
    JSR PlayProcessorPickupSound

    RTS

; Apply enhancement effects using conditional branching
ApplyResistorEnhancement:
    ; Enhanced resistance calculation using conditional logic
    LDA $D0             ; Load base weapon damage
    LDX $2090           ; Load resistor multiplier
    CPX #$01            ; Check if base multiplier
    BEQ resistor_enhancement_done ; Skip if base

    ; Apply resistor multiplier using conditional branching
resistor_multiplier_loop:
    CLC                 ; Clear carry
    ADC $D0             ; Add base damage
    DEX                 ; Decrement multiplier
    CPX #$01            ; Check if reached base
    BNE resistor_multiplier_loop ; Continue loop

    ; Store enhanced damage using conditional logic
    STA $2100           ; Store enhanced weapon damage

resistor_enhancement_done:
    RTS

ApplyCapacitorEnhancement:
    ; Enhanced charge storage using conditional logic
    LDA $E0             ; Load charge timer
    BNE capacitor_enhancement_done ; Skip if timer not zero

    ; Regenerate charge with capacitor enhancement using conditional branching
    LDA $A1             ; Load current charge
    CLC                 ; Clear carry
    ADC $E1             ; Add base regen amount
    LDX $2091           ; Load capacitor multiplier
    CPX #$01            ; Check if enhanced
    BEQ store_enhanced_charge ; Skip if base

    ; Apply capacitor enhancement bonus using conditional logic
    CLC                 ; Clear carry
    ADC $E1             ; Add bonus regen

store_enhanced_charge:
    CMP #$64            ; Check maximum charge
    BCC store_charge_regen ; Store if within limit
    LDA #$64            ; Clamp to maximum

store_charge_regen:
    STA $A1             ; Store updated charge

    ; Reset charge timer using conditional logic
    LDA #$3C            ; Reset timer
    STA $E0             ; Store reset value

capacitor_enhancement_done:
    RTS

ApplyInductorEnhancement:
    ; Enhanced voltage regulation using conditional logic
    LDA $E2             ; Load voltage timer
    BNE inductor_enhancement_done ; Skip if timer not zero

    ; Regenerate voltage with inductor enhancement using conditional branching
    LDA $A0             ; Load current voltage
    CLC                 ; Clear carry
    ADC $E3             ; Add base regen amount
    LDX $2092           ; Load inductor multiplier
    CPX #$01            ; Check if enhanced
    BEQ store_enhanced_voltage ; Skip if base

    ; Apply inductor enhancement bonus using conditional logic
    CLC                 ; Clear carry
    ADC $E3             ; Add bonus regen

store_enhanced_voltage:
    CMP #$64            ; Check maximum voltage
    BCC store_voltage_regen ; Store if within limit
    LDA #$64            ; Clamp to maximum

store_voltage_regen:
    STA $A0             ; Store updated voltage

    ; Reset voltage timer using conditional logic
    LDA #$FF            ; Reset timer
    STA $E2             ; Store reset value

inductor_enhancement_done:
    RTS

; Apply remaining enhancements using conditional branching (simplified for space)
ApplyTransformerEnhancement:
    ; Enhanced power conversion using conditional logic
    LDA $2093           ; Load transformer multiplier
    CMP #$01            ; Check if enhanced
    BEQ transformer_enhancement_done ; Skip if base
    ; Additional transformer logic here
transformer_enhancement_done:
    RTS

ApplyOscillatorEnhancement:
    ; Enhanced frequency control using conditional logic
    LDA $2094           ; Load oscillator multiplier
    CMP #$01            ; Check if enhanced
    BEQ oscillator_enhancement_done ; Skip if base
    ; Additional oscillator logic here
oscillator_enhancement_done:
    RTS

ApplyAmplifierEnhancement:
    ; Enhanced signal amplification using conditional logic
    LDA $2095           ; Load amplifier multiplier
    CMP #$01            ; Check if enhanced
    BEQ amplifier_enhancement_done ; Skip if base
    ; Additional amplifier logic here
amplifier_enhancement_done:
    RTS

ApplyGeneratorEnhancement:
    ; Enhanced power generation using conditional logic
    LDA $2096           ; Load generator multiplier
    CMP #$01            ; Check if enhanced
    BEQ generator_enhancement_done ; Skip if base
    ; Additional generator logic here
generator_enhancement_done:
    RTS

ApplyProcessorEnhancement:
    ; Enhanced processing power using conditional logic
    LDA $2097           ; Load processor multiplier
    CMP #$01            ; Check if enhanced
    BEQ processor_enhancement_done ; Skip if base
    ; Additional processor logic here
processor_enhancement_done:
    RTS

; Update component combo system using conditional branching
UpdateComponentComboSystem:
    ; Check combo counter using conditional logic
    LDA $2032           ; Load component combo counter
    BEQ component_combo_done ; Skip if no combo

    ; Check for combo thresholds using conditional branching
    CMP #$03            ; Check for 3-component combo
    BCC component_combo_done ; Skip if below threshold
    BEQ trigger_component_combo_3 ; Branch if exactly 3
    CMP #$05            ; Check for 5-component combo
    BEQ trigger_component_combo_5 ; Branch if exactly 5
    CMP #$08            ; Check for 8-component combo (all components)
    BEQ trigger_component_combo_8 ; Branch if perfect combo
    JMP component_combo_done

trigger_component_combo_3:
    ; 3-component combo bonus
    LDA #$0E            ; Combo bonus points
    JSR AwardElectricalPoints
    JSR PlayComponentComboSound
    JMP component_combo_done

trigger_component_combo_5:
    ; 5-component combo bonus (super combo)
    LDA #$24            ; Super combo bonus points
    JSR AwardElectricalPoints
    JSR PlayComponentSuperComboSound

    ; Award super combo enhancement
    JSR AwardComponentSuperComboEnhancement
    JMP component_combo_done

trigger_component_combo_8:
    ; Perfect 8-component combo bonus
    LDA #$40            ; Perfect combo bonus points
    JSR AwardElectricalPoints
    JSR PlayComponentPerfectComboSound

    ; Award perfect combo enhancement
    JSR AwardComponentPerfectComboEnhancement

component_combo_done:
    RTS

; Check component interactions using conditional branching
CheckComponentInteractions:
    ; Check for specific component combinations using conditional logic
    LDA $2000           ; Load resistor enhancement flag
    BEQ component_interaction_done ; Skip if resistor not active
    LDA $2001           ; Load capacitor enhancement flag
    BEQ component_interaction_done ; Skip if capacitor not active

    ; Resistor + Capacitor combo detected using conditional logic
    JSR TriggerResistorCapacitorCombo

component_interaction_done:
    RTS

; Check component combo bonuses
CheckComponentCombo:
    ; Award combo bonus based on current combo count using conditional logic
    LDA $2032           ; Load component combo counter
    CMP #$02            ; Check for 2-component streak
    BCC component_combo_check_done ; Skip if below streak

    ; Award streak bonus
    LDA #$05            ; Streak bonus points
    JSR AwardElectricalPoints

component_combo_check_done:
    RTS

; Trigger component combinations
TriggerResistorCapacitorCombo:
    ; Create special resistor-capacitor interaction using conditional logic
    LDA #$02            ; Combo multiplier
    STA $2110           ; Store combo multiplier for calculations

    ; Play special combo sound
    JSR PlayResistorCapacitorComboSound

    RTS

; Award super combo enhancement
AwardComponentSuperComboEnhancement:
    ; Extend all active component durations using conditional logic
    LDA $2080           ; Load resistor timer
    BEQ check_capacitor_timer ; Skip if not active
    CLC                 ; Clear carry
    ADC #$1E            ; Add 30 frames extension
    STA $2080           ; Store extended timer

check_capacitor_timer:
    LDA $2081           ; Load capacitor timer
    BEQ check_inductor_timer ; Skip if not active
    CLC                 ; Clear carry
    ADC #$1E            ; Add 30 frames extension
    STA $2081           ; Store extended timer

check_inductor_timer:
    LDA $2082           ; Load inductor timer
    BEQ check_transformer_timer ; Skip if not active
    CLC                 ; Clear carry
    ADC #$1E            ; Add 30 frames extension
    STA $2082           ; Store extended timer

check_transformer_timer:
    LDA $2083           ; Load transformer timer
    BEQ component_extension_done ; Skip if not active
    CLC                 ; Clear carry
    ADC #$1E            ; Add 30 frames extension
    STA $2083           ; Store extended timer

component_extension_done:
    RTS

; Award perfect combo enhancement
AwardComponentPerfectComboEnhancement:
    ; Maximize all component multipliers using conditional logic
    LDA #$05            ; Maximum multiplier
    STA $2090           ; Set resistor multiplier
    STA $2091           ; Set capacitor multiplier
    STA $2092           ; Set inductor multiplier
    STA $2093           ; Set transformer multiplier
    STA $2094           ; Set oscillator multiplier
    STA $2095           ; Set amplifier multiplier
    STA $2096           ; Set generator multiplier
    STA $2097           ; Set processor multiplier

    RTS

; Update component visual effects
UpdateComponentEffects:
    ; Create visual effects for active components
    JSR DrawComponentIndicators
    JSR UpdateComponentParticles

    RTS

; Draw component indicators using conditional logic
DrawComponentIndicators:
    ; Draw indicators for active components using conditional branching
    LDA $2000           ; Load resistor enhancement flag
    BEQ check_capacitor_indicator ; Skip if not active

    ; Draw resistor enhancement indicator
    LDA #$2A            ; Resistor indicator character
    STA $2007           ; PPU data

check_capacitor_indicator:
    LDA $2001           ; Load capacitor enhancement flag
    BEQ check_inductor_indicator ; Skip if not active

    ; Draw capacitor enhancement indicator
    LDA #$2B            ; Capacitor indicator character
    STA $2007           ; PPU data

check_inductor_indicator:
    LDA $2002           ; Load inductor enhancement flag
    BEQ indicator_drawing_done ; Skip if not active

    ; Draw inductor enhancement indicator
    LDA #$2C            ; Inductor indicator character
    STA $2007           ; PPU data

indicator_drawing_done:
    RTS

; Update component particle effects
UpdateComponentParticles:
    ; Create particle effects around active components using conditional logic
    LDA $2031           ; Load active components count
    BEQ particle_update_done ; Skip if no active components

    ; Create electrical sparkle effect using conditional branching
    LDA $2001           ; Load PPU control register value
    CLC                 ; Clear carry
    ADC #$01            ; Cycle effect
    AND #$1F            ; Mask to valid range
    STA $2001           ; Store cycled effect

particle_update_done:
    RTS

; Component sound effects
PlayResistorPickupSound:
    ; Use pulse channel for resistor pickup sound
    LDA #$81            ; Duty cycle, constant volume
    STA $4000           ; APU pulse 1 control

    LDA #$60            ; Resistor pickup frequency
    STA $4002           ; APU pulse 1 frequency low
    LDA #$06            ; Resistor pickup frequency high
    STA $4003           ; APU pulse 1 frequency high

    RTS

PlayCapacitorPickupSound:
    ; Use pulse channel for capacitor pickup sound
    LDA #$41            ; Different waveform
    STA $4000           ; APU pulse 1 control

    LDA #$80            ; Capacitor pickup frequency
    STA $4002           ; APU pulse 1 frequency low
    LDA #$05            ; Capacitor pickup frequency high
    STA $4003           ; APU pulse 1 frequency high

    RTS

PlayInductorPickupSound:
    ; Use triangle channel for inductor pickup sound
    LDA #$81            ; Triangle control
    STA $4008           ; APU triangle control

    LDA #$A0            ; Inductor pickup frequency
    STA $400A           ; APU triangle frequency low
    LDA #$04            ; Inductor pickup frequency high
    STA $400B           ; APU triangle frequency high

    RTS

; Additional component sound functions (simplified for space)
PlayTransformerPickupSound:
    LDA #$81
    STA $4000
    LDA #$40
    STA $4002
    LDA #$08
    STA $4003
    RTS

PlayOscillatorPickupSound:
    LDA #$21
    STA $4000
    LDA #$C0
    STA $4002
    LDA #$03
    STA $4003
    RTS

PlayAmplifierPickupSound:
    LDA #$81
    STA $4004
    LDA #$30
    STA $4006
    LDA #$09
    STA $4007
    RTS

PlayGeneratorPickupSound:
    LDA #$81
    STA $4008
    LDA #$E0
    STA $400A
    LDA #$02
    STA $400B
    RTS

PlayProcessorPickupSound:
    ; Master component pickup sound using all channels
    LDA #$81            ; Pulse 1 setup
    STA $4000           ; APU pulse 1 control
    LDA #$00            ; Processor chord 1
    STA $4002           ; APU pulse 1 frequency low
    LDA #$0E            ; Processor chord 1 high
    STA $4003           ; APU pulse 1 frequency high

    LDA #$81            ; Pulse 2 setup
    STA $4004           ; APU pulse 2 control
    LDA #$80            ; Processor chord 2
    STA $4006           ; APU pulse 2 frequency low
    LDA #$0A            ; Processor chord 2 high
    STA $4007           ; APU pulse 2 frequency high

    LDA #$81            ; Triangle setup
    STA $4008           ; APU triangle control
    LDA #$40            ; Processor bass
    STA $400A           ; APU triangle frequency low
    LDA #$07            ; Processor bass high
    STA $400B           ; APU triangle frequency high

    RTS

PlayComponentComboSound:
    ; Use pulse channel for component combo sound
    LDA #$81            ; Pulse control
    STA $4000           ; APU pulse 1 control

    LDA #$70            ; Combo frequency
    STA $4002           ; APU pulse 1 frequency low
    LDA #$07            ; Combo frequency high
    STA $4003           ; APU pulse 1 frequency high

    RTS

PlayComponentSuperComboSound:
    ; Use multiple channels for super combo sound
    LDA #$81            ; Pulse 1 setup
    STA $4000           ; APU pulse 1 control
    LDA #$50            ; Super combo frequency 1
    STA $4002           ; APU pulse 1 frequency low
    LDA #$08            ; Super combo frequency 1 high
    STA $4003           ; APU pulse 1 frequency high

    LDA #$81            ; Pulse 2 setup
    STA $4004           ; APU pulse 2 control
    LDA #$90            ; Super combo frequency 2
    STA $4006           ; APU pulse 2 frequency low
    LDA #$06            ; Super combo frequency 2 high
    STA $4007           ; APU pulse 2 frequency high

    RTS

PlayComponentPerfectComboSound:
    ; Perfect component combo sound using all channels
    LDA #$81            ; Pulse 1 setup
    STA $4000           ; APU pulse 1 control
    LDA #$20            ; Perfect combo chord 1
    STA $4002           ; APU pulse 1 frequency low
    LDA #$0C            ; Perfect combo chord 1 high
    STA $4003           ; APU pulse 1 frequency high

    LDA #$81            ; Pulse 2 setup
    STA $4004           ; APU pulse 2 control
    LDA #$60            ; Perfect combo chord 2
    STA $4006           ; APU pulse 2 frequency low
    LDA #$09            ; Perfect combo chord 2 high
    STA $4007           ; APU pulse 2 frequency high

    LDA #$81            ; Triangle setup
    STA $4008           ; APU triangle control
    LDA #$A0            ; Perfect combo bass
    STA $400A           ; APU triangle frequency low
    LDA #$05            ; Perfect combo bass high
    STA $400B           ; APU triangle frequency high

    RTS

PlayResistorCapacitorComboSound:
    ; Special resistor-capacitor combo sound
    LDA #$81            ; Pulse control
    STA $4000           ; APU pulse 1 control

    LDA #$B0            ; Resistor-capacitor frequency
    STA $4002           ; APU pulse 1 frequency low
    LDA #$08            ; Resistor-capacitor frequency high
    STA $4003           ; APU pulse 1 frequency high

    RTS

PlayComponentDeactivateSound:
    ; Component deactivation sound
    LDA #$41            ; Decay waveform
    STA $4000           ; APU pulse 1 control

    LDA #$18            ; Deactivation frequency
    STA $4002           ; APU pulse 1 frequency low
    LDA #$02            ; Deactivation frequency high
    STA $4003           ; APU pulse 1 frequency high

    RTS

.segment "VECTORS"
.word RESET, RESET, RESET
```

## Run It Right Now!

**Don't wait** - let's see your power components in action:

```bash
# Save as circuit7.nes, then:
# Load into NES emulator (Mesen, FCEUX, etc.)
# You should see: Power components with electrical enhancement systems!
```

**You should see**: Your electrical mastery world from Lessons 1-6, plus comprehensive power component systems with collectible electrical components, electrical enhancements, component enhancement tracking, and spectacular electrical component effects that create engaging electrical component enhancement gameplay!

## What Just Happened? The Magic Behind Power Component Systems

Now that you've experienced power components, let's understand how **6502 conditional branching** orchestrated this sophisticated electrical component enhancement system.

### 6502 Conditional Branching: Your Component Enhancement Architecture

The most important thing to understand: **6502 conditional branching enables sophisticated electrical logic and component state management**. In your power component code:

**Component Enhancement Checking**: Multiple enhancement paths with branching logic

```nasm
UpdateResistorComponent:
    LDA $2000           ; Load resistor enhancement flag
    BEQ resistor_component_done ; Branch if not active

    LDA $2080           ; Load resistor timer
    BEQ deactivate_resistor ; Branch if timer expired
    ; Apply resistor enhancement effects
```

**Component Pickup Logic**: Conditional electrical component collection based on proximity

```nasm
CheckResistorPickup:
    LDA $2010           ; Load resistor available flag
    BEQ resistor_pickup_done ; Branch if not available

    LDA $80             ; Load conductor X
    CMP $20C0           ; Compare with pickup range
    BCS resistor_pickup_done ; Branch if too far
```

**Power Component Examples Using 6502 Conditional Branching:**

| Technique              | Example                 | Use Case                                      |
| ---------------------- | ----------------------- | --------------------------------------------- |
| **Component Checking** | `LDA / BEQ component`   | Check multiple electrical component states    |
| **Enhancement Logic**  | `CMP / BCS enhancement` | Award conditional electrical bonuses          |
| **State Management**   | `BEQ / BNE state`       | Control electrical component progression flow |

### Why 6502 Conditional Branching Matters for Electrical Component Systems

**Multiple Component Enhancement Paths** - Essential for engaging electrical component progression:

```nasm
UpdateComponentComboSystem:
    LDA $2032           ; Load component combo counter
    CMP #$03            ; Check for 3-component combo
    BEQ trigger_component_combo_3 ; Branch for 3-combo
    CMP #$05            ; Check for 5-component combo
    BEQ trigger_component_combo_5 ; Branch for 5-combo
```

**Dynamic Electrical Component Reward Systems** - Handles conditional electrical component bonuses:

```nasm
PickupResistor:
    LDA $2090           ; Load current resistor multiplier
    CLC                 ; Clear carry
    ADC #$01            ; Increase multiplier
    CMP #$04            ; Check maximum multiplier
    BCC store_resistor_multiplier ; Store if within limit
```

**Progressive Electrical Component Difficulty** - Scales based on electrical component progress:

```nasm
PickupProcessor:
    LDA #$01            ; Set all enhancement flags
    STA $2000           ; Resistor enhancement
    STA $2001           ; Capacitor enhancement
    ; Activate all electrical enhancements
```

## Practice: Create Advanced Electrical Component Mechanics

Let's enhance your electrical component system with advanced electrical component progression features:

```nasm
    ; Create electrical component stacking system
    JSR InitElectricalComponentStacking

InitElectricalComponentStacking:
    ; Set up electrical component stacking using conditional logic
    LDA #$00            ; No stacks initially
    STA electrical_resistor_stacks     ; Resistor component stack count
    STA electrical_capacitor_stacks    ; Capacitor component stack count
    STA electrical_inductor_stacks     ; Inductor component stack count
    STA electrical_transformer_stacks  ; Transformer component stack count

    ; Initialize electrical component stack limits using conditional setup
    LDA #$03            ; Maximum stacks per electrical component type
    STA electrical_resistor_stack_limit     ; Resistor stack limit
    STA electrical_capacitor_stack_limit    ; Capacitor stack limit
    STA electrical_inductor_stack_limit     ; Inductor stack limit
    STA electrical_transformer_stack_limit  ; Transformer stack limit

    RTS

PickupElectricalStackingComponent:
    ; Enhanced electrical component pickup with stacking support
    ; A register contains electrical component type

    ; Check current electrical component stack count using conditional logic
    CMP #$01            ; Check if resistor component
    BEQ check_resistor_stack ; Branch for resistor
    CMP #$02            ; Check if capacitor component
    BEQ check_capacitor_stack ; Branch for capacitor
    JMP electrical_stacking_done

check_resistor_stack:
    LDA electrical_resistor_stacks ; Load current resistor stack count
    CMP electrical_resistor_stack_limit ; Compare with limit
    BCS electrical_stack_full ; Branch if at maximum

    ; Add resistor stack using conditional logic
    INC electrical_resistor_stacks ; Increment stack count
    JSR ApplyResistorStackBonus ; Apply stack bonus
    JMP electrical_stacking_done

check_capacitor_stack:
    LDA electrical_capacitor_stacks ; Load current capacitor stack count
    CMP electrical_capacitor_stack_limit ; Compare with limit
    BCS electrical_stack_full ; Branch if at maximum

    ; Add capacitor stack using conditional logic
    INC electrical_capacitor_stacks ; Increment stack count
    JSR ApplyCapacitorStackBonus ; Apply stack bonus
    JMP electrical_stacking_done

electrical_stack_full:
    ; Award electrical overflow bonus instead of stacking using conditional logic
    LDA #$20            ; Electrical overflow bonus points
    JSR AwardElectricalPoints

electrical_stacking_done:
    RTS

ApplyResistorStackBonus:
    ; Apply resistor stack bonus using conditional logic
    LDA electrical_resistor_stacks ; Load stack count
    CLC                 ; Clear carry
    ADC #$05            ; Add base bonus per stack
    STA electrical_resistor_bonus ; Store total bonus
    RTS

ApplyCapacitorStackBonus:
    ; Apply capacitor stack bonus using conditional logic
    LDA electrical_capacitor_stacks ; Load stack count
    CLC                 ; Clear carry
    ADC #$08            ; Add base bonus per stack
    STA electrical_capacitor_bonus ; Store total bonus
    RTS

; Electrical component stack data
electrical_resistor_stacks:         .byte $00
electrical_capacitor_stacks:        .byte $00
electrical_inductor_stacks:         .byte $00
electrical_transformer_stacks:      .byte $00
electrical_resistor_stack_limit:    .byte $03
electrical_capacitor_stack_limit:   .byte $03
electrical_inductor_stack_limit:    .byte $03
electrical_transformer_stack_limit: .byte $03
electrical_resistor_bonus:          .byte $00
electrical_capacitor_bonus:         .byte $00
```

**Run it again** and experience advanced electrical component stacking systems!

## Visual Progress Check

After running your enhanced code, you should see:

- Your original electrical mastery world with voltage progression as the foundation
- Collectible power components scattered throughout the electrical circuit environment
- Component enhancement indicators showing active electrical component power-ups and their durations
- Electrical component enhancement effects applied to voltage, current, resistance, and electrical mechanics
- Combo bonuses for collecting multiple electrical components in sequence
- Spectacular visual and audio feedback for electrical component pickups and enhancement activation

**Take a screenshot!** Your circuit world now has complete power component systems.

## What You've Accomplished

In this lesson, you've:

✅ **Created power component collection systems** with multiple electrical component types using 6502 conditional branching  
✅ **Learned 6502 conditional branching** through component storage and electrical enhancement logic  
✅ **Built electrical component enhancement tracking systems** with duration timers and electrical multipliers  
✅ **Used comparison operations** for electrical component condition checking and threshold management  
✅ **Used branching logic** for progressive electrical component difficulty scaling and reward systems  
✅ **Used state management** for electrical component advancement and enhancement completion sequences

More importantly, you've experienced **authentic NES electrical component enhancement programming** - creating sophisticated power component systems with comprehensive electrical component advancement mechanics.

## Power Component Game Architecture

**Why This Approach Matters**:

Your power component system demonstrates core electrical component enhancement programming:

- **Multi-condition electrical component logic** using branching for flexible electrical component enhancement conditions
- **Dynamic electrical component reward systems** with conditional bonuses based on electrical component performance
- **Progressive electrical component difficulty scaling** that adapts to electrical component advancement
- **Electrical component achievement tracking systems** providing long-term electrical component engagement goals
- **Authentic NES presentation** with APU electrical component celebration audio and voltage effects

These same techniques enabled the satisfying electrical component progression that made NES games legendary for rewarding electrical component gameplay advancement.

## Looking Ahead: Your Electrical Component Journey

**Next lesson**, you'll add the final essential element - **complete electrical game systems**! You'll learn:

- How to integrate all electrical component systems into a cohesive electrical game experience
- Building complete electrical gameplay loops with all mechanics working together
- Adding final electrical polish and completion sequences
- Creating balanced electrical difficulty progression through all systems

By **Lesson 8**, you'll have complete electrical game mechanics with full mastery, progression, power components, and enhancement systems in a finished NES electrical experience.

## Ready to Continue?

Your circuit world now surges with incredible power components! Electrical component collection and enhancement systems create the engaging electrical empowerment that defines classic electrical component gaming advancement. The final lesson will integrate everything into a complete electrical game experience.

But first, **collect power components in the electrical component arena**. You've just implemented comprehensive electrical component enhancement systems using authentic 6502 conditional branching. This demonstrates the electrical component enhancement gameplay programming that made NES games famous for sophisticated electrical component advancement mechanics.

**Welcome to power component programming!**

---

### Quick Reference: Today's Power Component Programming Instructions

- **LDA $address**: Load Accumulator (check electrical component enhancement states from memory)
- **CMP #value**: Compare electrical component values for conditional enhancement logic
- **BEQ label**: Branch if Equal (exact electrical component condition matching)
- **BNE label**: Branch if Not Equal (electrical component state change detection)
- **BCC/BCS label**: Branch on electrical component threshold checking and enhancement unlocking

Save this reference - you'll use these 6502 conditional branching techniques to manage all your electrical component enhancement and achievement systems!
