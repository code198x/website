---
title: "Prism Quest: Dimensional Artifacts"
system: "zx-spectrum"
phase_number: 1
tier_number: 1
lesson_number: 7
description: "Add collectible artifacts and dimensional enhancements to Prism Quest! Create attribute-based power-ups and dimensional boosts while learning data structures through artifact management and enhancement organization."
learning_objectives:
  - "Create collectible dimensional artifacts and attribute-based enhancement systems"
  - "Add dimensional boosts and attribute power-ups with effect tracking and duration management"
  - "Learn data structures through artifact storage and dimensional enhancement organization"
  - "Build balanced dimensional enhancement systems with artifact mechanics and attribute limitations"
  - "Experience creative Spectrum enhancement with spectacular attribute-based power-up effects"
concepts:
  - "Dimensional artifact programming and attribute-based enhancement architecture"
  - "Data structures for artifact storage and dimensional enhancement organization"
  - "Attribute manipulation for power-up effects and dimensional boost visualization"
  - "Dimensional enhancement balance mechanics and artifact interaction systems"
  - "Creative Spectrum enhancement presentation with attribute power-up effects and dimensional audio"
estimated_duration: "30-45 minutes"
difficulty: "easy"
code_examples: true
practical_exercise: true
order: 7
---

# Lesson 7: Prism Quest - Dimensional Artifacts

**Your dimensional world needs incredible artifacts!** In the last lesson, you added dimensional puzzle mastery and progression to your attribute showcase. Today, you'll create dimensional artifact and enhancement systems - collectible dimensional power-ups, attribute-based boosts, and spectral enhancements that transform your mastery experience into an engaging dimensional artifact adventure!

## Your Dimensional Artifacts Manifest

Let's start with the magic - adding dimensional artifacts and enhancement systems to your dimensional mastery world:

```nasm
; Prism Quest - Dimensional Artifacts Creation
    ORG $8000

start:
    ; Set up enhanced dimensional mastery world from Lessons 1-6
    CALL clear_screen
    CALL draw_dimensional_maze
    CALL init_attribute_magic
    CALL init_mastery_system
    CALL init_puzzle_tracking

    ; Initialize navigator from previous lessons
    LD BC, $0205        ; Navigator position: B=X(2), C=Y(5)
    LD (navigator_x), BC ; Store in memory

    ; Initialize dimensional artifact system
    CALL init_artifact_system

    ; Initialize dimensional enhancements
    CALL init_dimensional_enhancements

    ; Set up attribute power-ups
    CALL init_attribute_powerups

    ; Initialize artifact management
    CALL init_artifact_management

    ; Main dimensional artifact loop
main_loop:
    CALL read_navigator_input
    CALL update_navigator_position
    CALL update_attribute_magic
    CALL update_artifact_system
    CALL check_artifact_pickups
    CALL update_dimensional_enhancements
    CALL check_puzzle_completion
    CALL update_mastery_effects
    JR main_loop

; Initialize dimensional artifact system using data structures
init_artifact_system:
    ; Set up artifact variables using structured data organization
    LD HL, artifact_data        ; Point to artifact data structure
    LD (artifact_pointer), HL   ; Store pointer

    ; Initialize artifact flags using data structure
    LD B, 8                     ; Number of artifacts
    LD HL, artifact_flags       ; Point to artifact flags

clear_artifact_flags:
    LD (HL), $00                ; Clear artifact flag (not collected)
    INC HL                      ; Next artifact flag
    DJNZ clear_artifact_flags   ; Continue until all cleared

    ; Initialize artifact positions using structured data
    LD HL, artifact_positions   ; Point to position data
    LD DE, artifact_data        ; Point to main data structure
    LD BC, 16                   ; 8 artifacts * 2 bytes each (X,Y)
    LDIR                        ; Copy position data to structure

    ; Set artifact types using data structure
    LD HL, artifact_types       ; Point to type data
    LD A, $01                   ; Prism Amplifier type
    LD (HL), A                  ; Store type 1
    INC HL                      ; Next position
    LD A, $02                   ; Dimensional Boost type
    LD (HL), A                  ; Store type 2
    INC HL                      ; Next position
    LD A, $03                   ; Attribute Enhancer type
    LD (HL), A                  ; Store type 3
    INC HL                      ; Next position
    LD A, $04                   ; Spectrum Multiplier type
    LD (HL), A                  ; Store type 4
    INC HL                      ; Next position
    LD A, $05                   ; Color Catalyst type
    LD (HL), A                  ; Store type 5
    INC HL                      ; Next position
    LD A, $06                   ; Pattern Prism type
    LD (HL), A                  ; Store type 6
    INC HL                      ; Next position
    LD A, $07                   ; Dimensional Crystal type
    LD (HL), A                  ; Store type 7
    INC HL                      ; Next position
    LD A, $08                   ; Master Artifact type
    LD (HL), A                  ; Store type 8

    ; Initialize artifact counters using structured data
    LD A, $00                   ; Clear counters
    LD (artifacts_collected), A ; Total artifacts collected
    LD (active_artifacts), A    ; Active artifacts count
    LD (artifact_combo), A      ; Artifact combo counter
    LD (artifact_streak), A     ; Artifact streak counter

    RET

; Initialize dimensional enhancement system using data structures
init_dimensional_enhancements:
    ; Set up enhancement data structure
    LD HL, enhancement_data     ; Point to enhancement structure
    LD (enhancement_pointer), HL ; Store pointer

    ; Initialize enhancement flags using structured approach
    LD B, 5                     ; Number of enhancement types
    LD HL, enhancement_flags    ; Point to enhancement flags

clear_enhancement_flags:
    LD (HL), $00                ; Clear enhancement flag (not active)
    INC HL                      ; Next enhancement flag
    DJNZ clear_enhancement_flags ; Continue until all cleared

    ; Initialize enhancement levels using data structure
    LD HL, enhancement_levels   ; Point to level data
    LD B, 5                     ; Number of enhancement types

init_enhancement_levels:
    LD (HL), $01                ; Base enhancement level
    INC HL                      ; Next enhancement level
    DJNZ init_enhancement_levels ; Continue until all initialized

    ; Set enhancement durations using structured data
    LD HL, enhancement_durations ; Point to duration data
    LD A, $60                   ; Standard enhancement duration (96 frames)
    LD B, 5                     ; Number of enhancement types

init_enhancement_durations:
    LD (HL), A                  ; Store duration
    INC HL                      ; Next enhancement duration
    DJNZ init_enhancement_durations ; Continue until all set

    ; Initialize enhancement timers using data structure
    LD HL, enhancement_timers   ; Point to timer data
    LD B, 5                     ; Number of enhancement types

clear_enhancement_timers:
    LD (HL), $00                ; Clear timer (not active)
    INC HL                      ; Next enhancement timer
    DJNZ clear_enhancement_timers ; Continue until all cleared

    RET

; Initialize attribute power-up system using data structures
init_attribute_powerups:
    ; Set up attribute power-up data structure
    LD HL, attribute_powerup_data ; Point to power-up structure
    LD (powerup_pointer), HL    ; Store pointer

    ; Initialize attribute enhancement values using structured approach
    LD HL, attribute_enhancements ; Point to enhancement values
    LD A, $40                   ; Base attribute value
    LD B, 8                     ; Number of attribute positions

init_attribute_values:
    LD (HL), A                  ; Store base attribute
    INC HL                      ; Next attribute position
    DJNZ init_attribute_values  ; Continue until all set

    ; Set attribute multipliers using data structure
    LD HL, attribute_multipliers ; Point to multiplier data
    LD A, $01                   ; Base multiplier
    LD B, 8                     ; Number of multiplier positions

init_attribute_multipliers:
    LD (HL), A                  ; Store base multiplier
    INC HL                      ; Next multiplier position
    DJNZ init_attribute_multipliers ; Continue until all set

    ; Initialize attribute effect timers using structured data
    LD HL, attribute_effect_timers ; Point to timer data
    LD B, 8                     ; Number of timer positions

clear_attribute_timers:
    LD (HL), $00                ; Clear timer
    INC HL                      ; Next timer position
    DJNZ clear_attribute_timers ; Continue until all cleared

    RET

; Initialize artifact management system using data structures
init_artifact_management:
    ; Set up artifact management data structure
    LD HL, artifact_management_data ; Point to management structure
    LD (management_pointer), HL ; Store pointer

    ; Initialize pickup parameters using structured approach
    LD A, $08                   ; Artifact pickup range
    LD (pickup_range), A        ; Store range
    LD A, $04                   ; Artifact magnetism strength
    LD (magnetism_strength), A  ; Store magnetism

    ; Set artifact effects using data structure
    LD HL, artifact_effect_values ; Point to effect data
    LD A, $10                   ; Prism Amplifier effect
    LD (HL), A                  ; Store effect 1
    INC HL                      ; Next position
    LD A, $18                   ; Dimensional Boost effect
    LD (HL), A                  ; Store effect 2
    INC HL                      ; Next position
    LD A, $0C                   ; Attribute Enhancer effect
    LD (HL), A                  ; Store effect 3
    INC HL                      ; Next position
    LD A, $14                   ; Spectrum Multiplier effect
    LD (HL), A                  ; Store effect 4
    INC HL                      ; Next position
    LD A, $20                   ; Color Catalyst effect
    LD (HL), A                  ; Store effect 5
    INC HL                      ; Next position
    LD A, $16                   ; Pattern Prism effect
    LD (HL), A                  ; Store effect 6
    INC HL                      ; Next position
    LD A, $24                   ; Dimensional Crystal effect
    LD (HL), A                  ; Store effect 7
    INC HL                      ; Next position
    LD A, $30                   ; Master Artifact effect
    LD (HL), A                  ; Store effect 8

    RET

; Update dimensional artifact system using data structures
update_artifact_system:
    ; Update all active artifacts using structured approach
    CALL update_prism_amplifier
    CALL update_dimensional_boost
    CALL update_attribute_enhancer
    CALL update_spectrum_multiplier
    CALL update_color_catalyst
    CALL update_pattern_prism
    CALL update_dimensional_crystal
    CALL update_master_artifact

    ; Update artifact combo system
    CALL update_artifact_combo_system

    ; Check for artifact interactions
    CALL check_artifact_interactions

    RET

; Update prism amplifier using data structures
update_prism_amplifier:
    ; Check if prism amplifier is active using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD A, (HL)                  ; Load prism amplifier flag
    CP $00                      ; Check if not active
    JR Z, prism_amplifier_done  ; Jump if not active

    ; Update prism amplifier timer using structured approach
    LD HL, enhancement_timers   ; Point to timer data
    LD A, (HL)                  ; Load timer
    CP $00                      ; Check if expired
    JR Z, deactivate_prism_amplifier ; Jump if expired
    DEC A                       ; Decrement timer
    LD (HL), A                  ; Store updated timer

    ; Apply prism enhancement effects using data structure
    CALL apply_prism_enhancement
    JR prism_amplifier_done

deactivate_prism_amplifier:
    ; Deactivate prism amplifier using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD (HL), $00                ; Clear active flag

    ; Reset enhancement level using data structure
    LD HL, enhancement_levels   ; Point to level data
    LD (HL), $01                ; Reset to base level

    ; Play amplifier deactivation sound
    CALL play_artifact_deactivate_sound

prism_amplifier_done:
    RET

; Update dimensional boost using data structures
update_dimensional_boost:
    ; Check if dimensional boost is active using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    INC HL                      ; Move to boost flag position
    LD A, (HL)                  ; Load dimensional boost flag
    CP $00                      ; Check if not active
    JR Z, dimensional_boost_done ; Jump if not active

    ; Update dimensional boost timer using structured approach
    LD HL, enhancement_timers   ; Point to timer data
    INC HL                      ; Move to boost timer position
    LD A, (HL)                  ; Load timer
    CP $00                      ; Check if expired
    JR Z, deactivate_dimensional_boost ; Jump if expired
    DEC A                       ; Decrement timer
    LD (HL), A                  ; Store updated timer

    ; Apply dimensional enhancement effects using data structure
    CALL apply_dimensional_enhancement
    JR dimensional_boost_done

deactivate_dimensional_boost:
    ; Deactivate dimensional boost using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    INC HL                      ; Move to boost flag position
    LD (HL), $00                ; Clear active flag

dimensional_boost_done:
    RET

; Update attribute enhancer using data structures
update_attribute_enhancer:
    ; Check if attribute enhancer is active using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    INC HL                      ; Move to position 2
    INC HL                      ; Move to enhancer flag position
    LD A, (HL)                  ; Load attribute enhancer flag
    CP $00                      ; Check if not active
    JR Z, attribute_enhancer_done ; Jump if not active

    ; Update attribute enhancer timer using structured approach
    LD HL, enhancement_timers   ; Point to timer data
    INC HL                      ; Move to position 2
    INC HL                      ; Move to enhancer timer position
    LD A, (HL)                  ; Load timer
    CP $00                      ; Check if expired
    JR Z, deactivate_attribute_enhancer ; Jump if expired
    DEC A                       ; Decrement timer
    LD (HL), A                  ; Store updated timer

    ; Apply attribute enhancement effects using data structure
    CALL apply_attribute_enhancement
    JR attribute_enhancer_done

deactivate_attribute_enhancer:
    ; Deactivate attribute enhancer using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    INC HL                      ; Move to position 2
    INC HL                      ; Move to enhancer flag position
    LD (HL), $00                ; Clear active flag

attribute_enhancer_done:
    RET

; Update spectrum multiplier using data structures
update_spectrum_multiplier:
    ; Check if spectrum multiplier is active using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD BC, $0003                ; Offset to multiplier flag position
    ADD HL, BC                  ; Move to multiplier flag position
    LD A, (HL)                  ; Load spectrum multiplier flag
    CP $00                      ; Check if not active
    JR Z, spectrum_multiplier_done ; Jump if not active

    ; Update spectrum multiplier timer using structured approach
    LD HL, enhancement_timers   ; Point to timer data
    ADD HL, BC                  ; Move to multiplier timer position
    LD A, (HL)                  ; Load timer
    CP $00                      ; Check if expired
    JR Z, deactivate_spectrum_multiplier ; Jump if expired
    DEC A                       ; Decrement timer
    LD (HL), A                  ; Store updated timer

    ; Apply spectrum enhancement effects using data structure
    CALL apply_spectrum_enhancement
    JR spectrum_multiplier_done

deactivate_spectrum_multiplier:
    ; Deactivate spectrum multiplier using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD BC, $0003                ; Offset to multiplier flag position
    ADD HL, BC                  ; Move to multiplier flag position
    LD (HL), $00                ; Clear active flag

spectrum_multiplier_done:
    RET

; Update color catalyst using data structures
update_color_catalyst:
    ; Check if color catalyst is active using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD BC, $0004                ; Offset to catalyst flag position
    ADD HL, BC                  ; Move to catalyst flag position
    LD A, (HL)                  ; Load color catalyst flag
    CP $00                      ; Check if not active
    JR Z, color_catalyst_done   ; Jump if not active

    ; Update color catalyst timer using structured approach
    LD HL, enhancement_timers   ; Point to timer data
    ADD HL, BC                  ; Move to catalyst timer position
    LD A, (HL)                  ; Load timer
    CP $00                      ; Check if expired
    JR Z, deactivate_color_catalyst ; Jump if expired
    DEC A                       ; Decrement timer
    LD (HL), A                  ; Store updated timer

    ; Apply color enhancement effects using data structure
    CALL apply_color_enhancement
    JR color_catalyst_done

deactivate_color_catalyst:
    ; Deactivate color catalyst using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD BC, $0004                ; Offset to catalyst flag position
    ADD HL, BC                  ; Move to catalyst flag position
    LD (HL), $00                ; Clear active flag

color_catalyst_done:
    RET

; Update pattern prism and remaining artifacts (simplified for space)
update_pattern_prism:
    ; Similar structure to other artifacts
    RET

update_dimensional_crystal:
    ; Similar structure to other artifacts
    RET

update_master_artifact:
    ; Similar structure to other artifacts
    RET

; Check artifact pickups using data structures
check_artifact_pickups:
    ; Check collision with all artifacts using structured approach
    LD B, 8                     ; Number of artifacts to check
    LD HL, artifact_flags       ; Point to artifact flags
    LD DE, artifact_positions   ; Point to artifact positions

artifact_pickup_loop:
    ; Check if artifact is available using structured data
    LD A, (HL)                  ; Load artifact flag
    CP $01                      ; Check if available
    JR NZ, next_artifact_check  ; Jump if not available

    ; Check distance to artifact using structured approach
    CALL check_artifact_distance
    CP $01                      ; Check if in pickup range
    JR NZ, next_artifact_check  ; Jump if not in range

    ; Artifact pickup detected! using structured approach
    CALL pickup_artifact        ; A register contains artifact index

next_artifact_check:
    INC HL                      ; Next artifact flag
    INC DE                      ; Next artifact position (X)
    INC DE                      ; Skip Y position
    DJNZ artifact_pickup_loop   ; Continue until all checked

    RET

; Check artifact distance using data structures
check_artifact_distance:
    ; Check X distance using structured data
    LD A, (navigator_x)         ; Load navigator X
    LD C, A                     ; Store in C
    LD A, (DE)                  ; Load artifact X from structure
    SUB C                       ; Calculate X difference
    JR NC, artifact_x_positive  ; Jump if positive
    NEG                         ; Make positive

artifact_x_positive:
    LD C, A                     ; Store X difference
    LD A, (pickup_range)        ; Load pickup range from structure
    CP C                        ; Compare with X difference
    JR C, artifact_too_far      ; Jump if too far

    ; Check Y distance using structured data
    INC DE                      ; Move to Y position
    LD A, (navigator_y)         ; Load navigator Y
    LD C, A                     ; Store in C
    LD A, (DE)                  ; Load artifact Y from structure
    SUB C                       ; Calculate Y difference
    JR NC, artifact_y_positive  ; Jump if positive
    NEG                         ; Make positive

artifact_y_positive:
    LD C, A                     ; Store Y difference
    LD A, (pickup_range)        ; Load pickup range from structure
    CP C                        ; Compare with Y difference
    JR C, artifact_too_far      ; Jump if too far

    ; Artifact is in range
    DEC DE                      ; Move back to X position
    LD A, $01                   ; Return in range
    RET

artifact_too_far:
    DEC DE                      ; Move back to X position
    LD A, $00                   ; Return out of range
    RET

; Pickup artifact using data structures
pickup_artifact:
    ; Mark artifact as picked up using structured data
    LD (HL), $00                ; Clear available flag in structure

    ; Get artifact type using structured approach
    LD A, B                     ; Get artifact index (from loop counter)
    LD C, A                     ; Store index
    LD A, 8                     ; Total artifacts
    SUB B                       ; Calculate actual index
    LD B, A                     ; Store actual index

    ; Get artifact type from data structure
    LD HL, artifact_types       ; Point to type data
    LD C, $00                   ; Clear high byte
    ADD HL, BC                  ; Add index offset
    LD A, (HL)                  ; Load artifact type

    ; Branch to specific pickup handler based on type
    CP $01                      ; Check if Prism Amplifier
    JR Z, pickup_prism_amplifier ; Jump to handler
    CP $02                      ; Check if Dimensional Boost
    JR Z, pickup_dimensional_boost ; Jump to handler
    CP $03                      ; Check if Attribute Enhancer
    JR Z, pickup_attribute_enhancer ; Jump to handler
    CP $04                      ; Check if Spectrum Multiplier
    JR Z, pickup_spectrum_multiplier ; Jump to handler
    CP $05                      ; Check if Color Catalyst
    JR Z, pickup_color_catalyst ; Jump to handler
    CP $06                      ; Check if Pattern Prism
    JR Z, pickup_pattern_prism  ; Jump to handler
    CP $07                      ; Check if Dimensional Crystal
    JR Z, pickup_dimensional_crystal ; Jump to handler
    CP $08                      ; Check if Master Artifact
    JR Z, pickup_master_artifact ; Jump to handler

    RET

; Pickup prism amplifier using data structures
pickup_prism_amplifier:
    ; Activate prism amplifier using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD (HL), $01                ; Set active flag

    ; Set enhancement duration using structured approach
    LD HL, enhancement_durations ; Point to duration data
    LD A, (HL)                  ; Load duration
    LD HL, enhancement_timers   ; Point to timer data
    LD (HL), A                  ; Store duration in timer

    ; Apply prism boost using structured data
    LD HL, enhancement_levels   ; Point to level data
    LD A, (HL)                  ; Load current level
    INC A                       ; Increase level
    CP $04                      ; Check maximum level
    JR C, store_prism_level     ; Jump if within limit
    LD A, $04                   ; Clamp to maximum

store_prism_level:
    LD (HL), A                  ; Store updated level

    ; Update pickup counters using structured data
    LD HL, artifacts_collected  ; Point to counter
    INC (HL)                    ; Increment total pickups
    LD HL, active_artifacts     ; Point to active counter
    INC (HL)                    ; Increment active artifacts
    LD HL, artifact_combo       ; Point to combo counter
    INC (HL)                    ; Increment combo counter

    ; Award pickup bonus score
    LD A, $06                   ; Pickup bonus points
    CALL award_mastery_points

    ; Play prism amplifier pickup sound
    CALL play_prism_pickup_sound

    ; Check for pickup combo bonus
    CALL check_artifact_combo

    RET

; Pickup dimensional boost using data structures
pickup_dimensional_boost:
    ; Activate dimensional boost using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    INC HL                      ; Move to boost flag position
    LD (HL), $01                ; Set active flag

    ; Set enhancement duration using structured approach
    LD HL, enhancement_durations ; Point to duration data
    INC HL                      ; Move to boost duration position
    LD A, (HL)                  ; Load duration
    LD HL, enhancement_timers   ; Point to timer data
    INC HL                      ; Move to boost timer position
    LD (HL), A                  ; Store duration in timer

    ; Apply immediate dimensional boost using structured data
    CALL apply_immediate_dimensional_boost

    ; Update pickup counters using structured data
    LD HL, artifacts_collected  ; Point to counter
    INC (HL)                    ; Increment total pickups
    LD HL, active_artifacts     ; Point to active counter
    INC (HL)                    ; Increment active artifacts
    LD HL, artifact_combo       ; Point to combo counter
    INC (HL)                    ; Increment combo counter

    ; Play dimensional boost pickup sound
    CALL play_dimensional_pickup_sound

    RET

; Pickup attribute enhancer using data structures
pickup_attribute_enhancer:
    ; Activate attribute enhancer using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    INC HL                      ; Move to position 2
    INC HL                      ; Move to enhancer flag position
    LD (HL), $01                ; Set active flag

    ; Set enhancement duration using structured approach
    LD HL, enhancement_durations ; Point to duration data
    INC HL                      ; Move to position 2
    INC HL                      ; Move to enhancer duration position
    LD A, (HL)                  ; Load duration
    LD HL, enhancement_timers   ; Point to timer data
    INC HL                      ; Move to position 2
    INC HL                      ; Move to enhancer timer position
    LD (HL), A                  ; Store duration in timer

    ; Apply immediate attribute enhancement using structured data
    CALL apply_immediate_attribute_enhancement

    ; Update pickup counters using structured data
    LD HL, artifacts_collected  ; Point to counter
    INC (HL)                    ; Increment total pickups
    LD HL, active_artifacts     ; Point to active counter
    INC (HL)                    ; Increment active artifacts
    LD HL, artifact_combo       ; Point to combo counter
    INC (HL)                    ; Increment combo counter

    ; Play attribute enhancer pickup sound
    CALL play_attribute_pickup_sound

    RET

; Pickup remaining artifacts (simplified for space)
pickup_spectrum_multiplier:
    ; Similar structure using data organization
    LD HL, artifacts_collected
    INC (HL)
    CALL play_spectrum_pickup_sound
    RET

pickup_color_catalyst:
    ; Similar structure using data organization
    LD HL, artifacts_collected
    INC (HL)
    CALL play_color_pickup_sound
    RET

pickup_pattern_prism:
    ; Similar structure using data organization
    LD HL, artifacts_collected
    INC (HL)
    CALL play_pattern_pickup_sound
    RET

pickup_dimensional_crystal:
    ; Similar structure using data organization
    LD HL, artifacts_collected
    INC (HL)
    CALL play_crystal_pickup_sound
    RET

pickup_master_artifact:
    ; Master artifact with special effects using structured data
    LD HL, artifacts_collected
    INC (HL)

    ; Activate all enhancements using structured approach
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD B, 5                     ; Number of enhancement types

activate_all_enhancements:
    LD (HL), $01                ; Activate enhancement
    INC HL                      ; Next enhancement
    DJNZ activate_all_enhancements ; Continue until all activated

    ; Set extended durations using structured data
    LD HL, enhancement_timers   ; Point to timer data
    LD A, $B4                   ; Extended duration (180 frames)
    LD B, 5                     ; Number of timers

set_extended_durations:
    LD (HL), A                  ; Set extended duration
    INC HL                      ; Next timer
    DJNZ set_extended_durations ; Continue until all set

    ; Play master artifact pickup sound
    CALL play_master_pickup_sound

    RET

; Apply enhancement effects using data structures
apply_prism_enhancement:
    ; Enhanced attribute manipulation using structured data
    LD HL, attribute_enhancements ; Point to enhancement data
    LD B, 8                     ; Number of attributes to enhance
    LD A, (enhancement_levels)  ; Load enhancement level
    LD C, A                     ; Store level in C

prism_enhancement_loop:
    LD A, (HL)                  ; Load current attribute value
    ADD A, C                    ; Add enhancement level
    CP $47                      ; Check maximum attribute value
    JR C, store_prism_attribute ; Jump if within limit
    LD A, $47                   ; Clamp to maximum

store_prism_attribute:
    LD (HL), A                  ; Store enhanced attribute
    INC HL                      ; Next attribute
    DJNZ prism_enhancement_loop ; Continue until all enhanced

    RET

apply_dimensional_enhancement:
    ; Enhanced dimensional effects using structured data
    LD HL, attribute_enhancements ; Point to enhancement data
    LD B, 4                     ; Number of dimensional attributes

dimensional_enhancement_loop:
    LD A, (HL)                  ; Load current attribute
    ADD A, $02                  ; Add dimensional boost
    CP $47                      ; Check maximum
    JR C, store_dimensional_attribute ; Jump if within limit
    LD A, $47                   ; Clamp to maximum

store_dimensional_attribute:
    LD (HL), A                  ; Store enhanced attribute
    INC HL                      ; Next attribute
    DJNZ dimensional_enhancement_loop ; Continue enhancement

    RET

apply_attribute_enhancement:
    ; Enhanced color attributes using structured data
    LD HL, ATTR_FILE            ; Point to attribute memory
    LD B, 32                    ; Number of attribute cells to enhance

attribute_enhancement_loop:
    LD A, (HL)                  ; Load current attribute
    AND $07                     ; Get ink color
    INC A                       ; Enhance ink color
    AND $07                     ; Wrap around if necessary
    LD C, A                     ; Store enhanced ink

    LD A, (HL)                  ; Load attribute again
    AND $F8                     ; Clear ink color bits
    OR C                        ; Apply enhanced ink color
    LD (HL), A                  ; Store enhanced attribute

    INC HL                      ; Next attribute cell
    DJNZ attribute_enhancement_loop ; Continue enhancement

    RET

apply_spectrum_enhancement:
    ; Enhanced spectrum effects using structured data
    LD HL, attribute_multipliers ; Point to multiplier data
    LD B, 8                     ; Number of multipliers

spectrum_enhancement_loop:
    LD A, (HL)                  ; Load current multiplier
    INC A                       ; Increase multiplier
    CP $05                      ; Check maximum multiplier
    JR C, store_spectrum_multiplier ; Jump if within limit
    LD A, $05                   ; Clamp to maximum

store_spectrum_multiplier:
    LD (HL), A                  ; Store enhanced multiplier
    INC HL                      ; Next multiplier
    DJNZ spectrum_enhancement_loop ; Continue enhancement

    RET

apply_color_enhancement:
    ; Enhanced color cycling using structured data
    LD HL, ATTR_FILE            ; Point to attribute memory
    LD B, 64                    ; Number of cells for color enhancement

color_enhancement_loop:
    LD A, (HL)                  ; Load current attribute
    RLA                         ; Rotate left (shift colors)
    RLA                         ; Rotate again for effect
    LD (HL), A                  ; Store enhanced color
    INC HL                      ; Next attribute cell
    DJNZ color_enhancement_loop ; Continue enhancement

    RET

; Apply immediate enhancements
apply_immediate_dimensional_boost:
    ; Immediate dimensional boost effects using structured data
    LD HL, mastery_score        ; Point to score
    LD A, (HL)                  ; Load current score
    ADD A, $08                  ; Add dimensional boost bonus
    LD (HL), A                  ; Store boosted score

    RET

apply_immediate_attribute_enhancement:
    ; Immediate attribute enhancement using structured data
    LD HL, ATTR_FILE+128        ; Point to specific attribute area
    LD A, $46                   ; Enhanced attribute value
    LD B, 16                    ; Number of cells to enhance

immediate_attribute_loop:
    LD (HL), A                  ; Store enhanced attribute
    INC HL                      ; Next cell
    DJNZ immediate_attribute_loop ; Continue enhancement

    RET

; Update artifact combo system using data structures
update_artifact_combo_system:
    ; Check combo counter using structured data
    LD A, (artifact_combo)      ; Load combo counter
    CP $00                      ; Check if no combo
    JR Z, artifact_combo_done   ; Jump if no combo

    ; Check for combo thresholds using structured data
    CP $03                      ; Check for 3-artifact combo
    JR C, artifact_combo_done   ; Jump if below threshold
    JR Z, trigger_artifact_combo_3 ; Jump if exactly 3
    CP $05                      ; Check for 5-artifact combo
    JR Z, trigger_artifact_combo_5 ; Jump if exactly 5
    JR artifact_combo_done

trigger_artifact_combo_3:
    ; 3-artifact combo bonus
    LD A, $0C                   ; Combo bonus points
    CALL award_mastery_points
    CALL play_artifact_combo_sound
    JR artifact_combo_done

trigger_artifact_combo_5:
    ; 5-artifact combo bonus (super combo)
    LD A, $20                   ; Super combo bonus points
    CALL award_mastery_points
    CALL play_artifact_super_combo_sound

    ; Award super combo enhancement
    CALL award_artifact_super_combo_enhancement

artifact_combo_done:
    RET

; Check artifact interactions using data structures
check_artifact_interactions:
    ; Check for specific artifact combinations using structured data
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD A, (HL)                  ; Load prism amplifier flag
    CP $00                      ; Check if not active
    JR Z, artifact_interaction_done ; Jump if not active

    INC HL                      ; Move to dimensional boost flag
    INC HL                      ; Move to attribute enhancer flag
    LD A, (HL)                  ; Load attribute enhancer flag
    CP $00                      ; Check if not active
    JR Z, artifact_interaction_done ; Jump if not active

    ; Prism + Attribute combo detected
    CALL trigger_prism_attribute_combo

artifact_interaction_done:
    RET

; Check artifact combo bonuses
check_artifact_combo:
    ; Award combo bonus based on current combo count using structured data
    LD A, (artifact_combo)      ; Load combo counter
    CP $02                      ; Check for 2-artifact streak
    JR C, artifact_combo_check_done ; Jump if below streak

    ; Award streak bonus
    LD A, $04                   ; Streak bonus points
    CALL award_mastery_points

artifact_combo_check_done:
    RET

; Trigger artifact combinations
trigger_prism_attribute_combo:
    ; Create special prism-attribute interaction using structured data
    LD A, $02                   ; Combo multiplier
    LD (combo_multiplier), A    ; Store combo multiplier

    ; Play special combo sound
    CALL play_prism_attribute_combo_sound

    RET

; Award super combo enhancement
award_artifact_super_combo_enhancement:
    ; Extend all active enhancement durations using structured data
    LD HL, enhancement_timers   ; Point to timer data
    LD B, 5                     ; Number of timers

extend_enhancement_loop:
    LD A, (HL)                  ; Load current timer
    CP $00                      ; Check if active
    JR Z, next_enhancement_timer ; Jump if not active
    ADD A, $1E                  ; Add 30 frames extension
    LD (HL), A                  ; Store extended timer

next_enhancement_timer:
    INC HL                      ; Next timer
    DJNZ extend_enhancement_loop ; Continue until all extended

    RET

; Dimensional artifact sound effects
play_prism_pickup_sound:
    ; Use BEEP for prism pickup sound
    LD A, $40                   ; Prism frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $18                   ; Duration counter
prism_sound_loop:
    DJNZ prism_sound_loop       ; Loop for duration
    RET

play_dimensional_pickup_sound:
    ; Use BEEP for dimensional pickup sound
    LD A, $60                   ; Dimensional frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $20                   ; Duration counter
dimensional_sound_loop:
    DJNZ dimensional_sound_loop ; Loop for duration
    RET

play_attribute_pickup_sound:
    ; Use BEEP for attribute pickup sound
    LD A, $50                   ; Attribute frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $15                   ; Duration counter
attribute_sound_loop:
    DJNZ attribute_sound_loop   ; Loop for duration
    RET

play_spectrum_pickup_sound:
    ; Use BEEP for spectrum pickup sound
    LD A, $70                   ; Spectrum frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $12                   ; Duration counter
spectrum_sound_loop:
    DJNZ spectrum_sound_loop    ; Loop for duration
    RET

play_color_pickup_sound:
    ; Use BEEP for color pickup sound
    LD A, $30                   ; Color frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $25                   ; Duration counter
color_sound_loop:
    DJNZ color_sound_loop       ; Loop for duration
    RET

play_pattern_pickup_sound:
    ; Use BEEP for pattern pickup sound
    LD A, $45                   ; Pattern frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $1A                   ; Duration counter
pattern_sound_loop:
    DJNZ pattern_sound_loop     ; Loop for duration
    RET

play_crystal_pickup_sound:
    ; Use BEEP for crystal pickup sound
    LD A, $80                   ; Crystal frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $10                   ; Duration counter
crystal_sound_loop:
    DJNZ crystal_sound_loop     ; Loop for duration
    RET

play_master_pickup_sound:
    ; Use BEEP for master artifact pickup sound
    LD A, $20                   ; Low frequency start
    OUT ($FE), A                ; Output to speaker
    LD B, $30                   ; Duration counter
master_sound_loop_1:
    DJNZ master_sound_loop_1    ; Loop for duration

    LD A, $90                   ; High frequency finish
    OUT ($FE), A                ; Output to speaker
    LD B, $30                   ; Duration counter
master_sound_loop_2:
    DJNZ master_sound_loop_2    ; Loop for duration
    RET

play_artifact_combo_sound:
    ; Use BEEP for artifact combo sound
    LD A, $55                   ; Combo frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $20                   ; Duration counter
artifact_combo_sound_loop:
    DJNZ artifact_combo_sound_loop ; Loop for duration
    RET

play_artifact_super_combo_sound:
    ; Use BEEP for super combo sound
    LD A, $35                   ; Super combo frequency 1
    OUT ($FE), A                ; Output to speaker
    LD B, $25                   ; Duration counter
super_combo_sound_loop_1:
    DJNZ super_combo_sound_loop_1 ; Loop for duration

    LD A, $75                   ; Super combo frequency 2
    OUT ($FE), A                ; Output to speaker
    LD B, $25                   ; Duration counter
super_combo_sound_loop_2:
    DJNZ super_combo_sound_loop_2 ; Loop for duration
    RET

play_prism_attribute_combo_sound:
    ; Special prism-attribute combo sound
    LD A, $65                   ; Prism-attribute frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $28                   ; Duration counter
prism_attribute_sound_loop:
    DJNZ prism_attribute_sound_loop ; Loop for duration
    RET

play_artifact_deactivate_sound:
    ; Artifact deactivation sound
    LD A, $15                   ; Deactivation frequency
    OUT ($FE), A                ; Output to speaker
    LD B, $08                   ; Duration counter
deactivate_sound_loop:
    DJNZ deactivate_sound_loop  ; Loop for duration
    RET

; Data storage for dimensional artifact system using structured approach
artifact_data:              DEFS 64     ; Main artifact data structure
artifact_pointer:           DEFW 0      ; Pointer to artifact data
artifact_flags:             DEFB 1,1,1,1,1,1,1,1 ; Artifact availability flags
artifact_positions:         DEFB $0C,$06,$18,$09,$24,$0E,$14,$12,$30,$0F,$1C,$15,$28,$18,$10,$0A ; X,Y positions
artifact_types:             DEFB 1,2,3,4,5,6,7,8 ; Artifact types

enhancement_data:           DEFS 32     ; Enhancement data structure
enhancement_pointer:        DEFW 0      ; Pointer to enhancement data
enhancement_flags:          DEFB 0,0,0,0,0 ; Enhancement active flags
enhancement_levels:         DEFB 1,1,1,1,1 ; Enhancement levels
enhancement_durations:      DEFB 96,96,96,96,96 ; Enhancement durations
enhancement_timers:         DEFB 0,0,0,0,0 ; Enhancement timers

attribute_powerup_data:     DEFS 32     ; Attribute power-up data structure
powerup_pointer:            DEFW 0      ; Pointer to power-up data
attribute_enhancements:     DEFB $40,$40,$40,$40,$40,$40,$40,$40 ; Attribute enhancement values
attribute_multipliers:      DEFB 1,1,1,1,1,1,1,1 ; Attribute multipliers
attribute_effect_timers:    DEFB 0,0,0,0,0,0,0,0 ; Attribute effect timers

artifact_management_data:   DEFS 16     ; Artifact management data structure
management_pointer:         DEFW 0      ; Pointer to management data
pickup_range:               DEFB 8      ; Artifact pickup range
magnetism_strength:         DEFB 4      ; Artifact magnetism strength
artifact_effect_values:     DEFB $10,$18,$0C,$14,$20,$16,$24,$30 ; Effect values per artifact type

artifacts_collected:        DEFB 0      ; Total artifacts collected
active_artifacts:           DEFB 0      ; Active artifacts count
artifact_combo:             DEFB 0      ; Artifact combo counter
artifact_streak:            DEFB 0      ; Artifact streak counter
combo_multiplier:           DEFB 1      ; Combo multiplier for effects

navigator_x:                DEFB 2      ; Navigator X position
navigator_y:                DEFB 5      ; Navigator Y position
mastery_score:              DEFB 0      ; Current mastery score
```

## Run It Right Now!

**Don't wait** - let's see your dimensional artifacts in action:

```bash
# Save as prism7.tap, then:
# Load into ZX Spectrum emulator (Fuse, etc.)
# You should see: Dimensional artifacts with spectacular attribute-based enhancements!
```

**You should see**: Your dimensional mastery world from Lessons 1-6, plus comprehensive dimensional artifact systems with collectible power-ups, attribute-based enhancements, dimensional boosts, and spectacular dimensional artifact effects that create engaging dimensional enhancement gameplay!

## What Just Happened? The Magic Behind Dimensional Artifact Systems

Now that you've experienced dimensional artifacts, let's understand how **data structures** orchestrated this sophisticated dimensional enhancement system.

### Data Structures: Your Dimensional Artifact Architecture

The most important thing to understand: **data structures enable organized storage and efficient management of complex dimensional enhancement states**. In your dimensional artifact code:

**Structured Data Organization**: Systematic artifact storage using organized data layout

```nasm
init_artifact_system:
    LD HL, artifact_data        ; Point to artifact data structure
    LD (artifact_pointer), HL   ; Store pointer

    LD HL, artifact_flags       ; Point to artifact flags
    LD B, 8                     ; Number of artifacts
clear_artifact_flags:
    LD (HL), $00                ; Clear artifact flag
    INC HL                      ; Next artifact flag
    DJNZ clear_artifact_flags   ; Continue until all cleared
```

**Enhancement State Management**: Organized enhancement tracking with structured access

```nasm
update_prism_amplifier:
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD A, (HL)                  ; Load prism amplifier flag

    LD HL, enhancement_timers   ; Point to timer data
    LD A, (HL)                  ; Load timer
    DEC A                       ; Decrement timer
    LD (HL), A                  ; Store updated timer
```

**Dimensional Artifact Examples Using Data Structures:**

| Technique                | Example                     | Use Case                               |
| ------------------------ | --------------------------- | -------------------------------------- |
| **Structured Storage**   | `LD HL, artifact_data`      | Organized artifact information storage |
| **Pointer Management**   | `LD (artifact_pointer), HL` | Efficient data structure access        |
| **Organized Processing** | `DJNZ clear_artifact_flags` | Systematic data structure operations   |

### Why Data Structures Matter for Dimensional Enhancement Systems

**Organized State Management** - Essential for complex dimensional tracking:

```nasm
init_dimensional_enhancements:
    LD HL, enhancement_data     ; Point to enhancement structure
    LD B, 5                     ; Number of enhancement types

clear_enhancement_flags:
    LD (HL), $00                ; Clear enhancement flag
    INC HL                      ; Next enhancement flag
    DJNZ clear_enhancement_flags ; Continue until all cleared
```

**Efficient Enhancement Processing** - Handles multiple simultaneous dimensional effects:

```nasm
pickup_prism_amplifier:
    LD HL, enhancement_flags    ; Point to enhancement flags
    LD (HL), $01                ; Set active flag

    LD HL, enhancement_timers   ; Point to timer data
    LD A, (HL)                  ; Load duration
    LD (HL), A                  ; Store duration in timer
```

**Scalable Enhancement Architecture** - Supports complex dimensional artifact interactions:

```nasm
check_artifact_pickups:
    LD B, 8                     ; Number of artifacts to check
    LD HL, artifact_flags       ; Point to artifact flags
    LD DE, artifact_positions   ; Point to artifact positions

artifact_pickup_loop:
    LD A, (HL)                  ; Load artifact flag from structure
    CP $01                      ; Check if available
```

## Practice: Create Advanced Dimensional Artifact Mechanics

Let's enhance your dimensional artifact system with advanced structured features:

```nasm
    ; Create dimensional artifact stacking system using organized data structures
    CALL init_dimensional_stacking_system

init_dimensional_stacking_system:
    ; Set up dimensional stacking using structured data organization
    LD HL, dimensional_stack_data ; Point to stack data structure
    LD (stack_pointer), HL      ; Store structure pointer

    ; Initialize stack counts using organized data layout
    LD B, 8                     ; Number of artifact types
    LD HL, dimensional_stack_counts ; Point to stack count structure

clear_dimensional_stacks:
    LD (HL), $00                ; Clear stack count
    INC HL                      ; Next stack position
    DJNZ clear_dimensional_stacks ; Continue until all cleared

    ; Set stack limits using structured approach
    LD HL, dimensional_stack_limits ; Point to limit structure
    LD A, $03                   ; Maximum stacks per artifact type
    LD B, 8                     ; Number of artifact types

init_dimensional_stack_limits:
    LD (HL), A                  ; Store stack limit
    INC HL                      ; Next limit position
    DJNZ init_dimensional_stack_limits ; Continue until all set

    ; Initialize stack bonus values using data structure
    LD HL, dimensional_stack_bonuses ; Point to bonus structure
    LD DE, stack_bonus_values   ; Point to bonus value data
    LD BC, 8                    ; Copy 8 bonus values
    LDIR                        ; Copy bonus data to structure

    RET

pickup_dimensional_stacking_artifact:
    ; Enhanced artifact pickup with dimensional stacking support
    ; A register contains artifact type

    ; Use artifact type as index into data structure
    LD C, A                     ; Store artifact type
    LD B, $00                   ; Clear high byte
    LD HL, dimensional_stack_counts ; Point to stack count structure
    ADD HL, BC                  ; Add offset for artifact type

    ; Check current stack count using structured data
    LD A, (HL)                  ; Load current stack count
    LD DE, dimensional_stack_limits ; Point to limit structure
    EX DE, HL                   ; Swap pointers
    ADD HL, BC                  ; Add offset for artifact type
    CP (HL)                     ; Compare with maximum stacks
    JR NC, dimensional_stack_full ; Jump if at maximum

    ; Add stack using structured data management
    EX DE, HL                   ; Restore stack count pointer
    INC (HL)                    ; Increment stack count

    ; Apply stack bonus using organized data structure
    LD A, (HL)                  ; Load new stack count
    LD D, A                     ; Store count in D
    LD HL, dimensional_stack_bonuses ; Point to bonus structure
    ADD HL, BC                  ; Add offset for artifact type
    LD A, (HL)                  ; Load stack bonus value

dimensional_stack_bonus_loop:
    ; Apply bonus for each stack using structured calculation
    LD HL, dimensional_total_enhancement ; Point to total enhancement
    ADD HL, BC                  ; Add offset for artifact type
    ADD A, (HL)                 ; Add to existing enhancement
    LD (HL), A                  ; Store total enhancement
    DEC D                       ; Decrement stack counter
    JR NZ, dimensional_stack_bonus_loop ; Continue until all stacks applied

    JR dimensional_stacking_pickup_done

dimensional_stack_full:
    ; Award dimensional overflow bonus instead of stacking
    LD A, $18                   ; Dimensional overflow bonus points
    CALL award_mastery_points

dimensional_stacking_pickup_done:
    RET

create_rare_dimensional_artifacts:
    ; Create rare dimensional artifacts with special effects using structured data
    LD HL, rare_artifact_data   ; Point to rare artifact structure
    LD (rare_artifact_pointer), HL ; Store structure pointer

    ; Set rare artifact positions using organized data layout
    LD HL, rare_artifact_positions ; Point to position structure
    LD A, $35                   ; Rare artifact 1 X position
    LD (HL), A                  ; Store position
    INC HL                      ; Next position
    LD A, $18                   ; Rare artifact 1 Y position
    LD (HL), A                  ; Store position

    ; Set rare artifact types using structured approach
    LD HL, rare_artifact_types  ; Point to type structure
    LD A, $09                   ; Dimensional convergence artifact type
    LD (HL), A                  ; Store rare artifact type

    ; Set rare artifact availability using data structure
    LD HL, rare_artifact_flags  ; Point to availability structure
    LD A, $01                   ; Rare artifact available
    LD (HL), A                  ; Store availability flag

    RET

pickup_rare_dimensional_artifact:
    ; Pickup rare dimensional convergence artifact using structured data
    LD HL, rare_artifact_flags  ; Point to availability structure
    LD (HL), $00                ; Mark as picked up

    ; Activate multiple dimensional enhancements using organized structure
    LD HL, enhancement_flags    ; Point to enhancement flag structure
    LD B, 5                     ; Number of enhancement types

activate_all_dimensional_enhancements:
    LD (HL), $01                ; Activate enhancement
    INC HL                      ; Next enhancement flag
    DJNZ activate_all_dimensional_enhancements ; Continue until all activated

    ; Set extended durations using structured data management
    LD HL, enhancement_timers   ; Point to timer structure
    LD A, $E1                   ; Extended duration (225 frames)
    LD B, 5                     ; Number of timers

set_extended_dimensional_durations:
    LD (HL), A                  ; Set extended duration
    INC HL                      ; Next timer
    DJNZ set_extended_dimensional_durations ; Continue until all set

    ; Play rare dimensional pickup sound
    CALL play_rare_dimensional_pickup_sound

    RET

play_rare_dimensional_pickup_sound:
    ; Special rare dimensional artifact pickup sound
    LD A, $25                   ; Very low frequency start
    OUT ($FE), A                ; Output to speaker
    LD B, $40                   ; Extended duration
rare_dimensional_sound_loop_1:
    DJNZ rare_dimensional_sound_loop_1 ; Loop for duration

    LD A, $95                   ; Very high frequency finish
    OUT ($FE), A                ; Output to speaker
    LD B, $40                   ; Extended duration
rare_dimensional_sound_loop_2:
    DJNZ rare_dimensional_sound_loop_2 ; Loop for duration
    RET

; Additional data structures for advanced dimensional artifact system
dimensional_stack_data:         DEFS 32     ; Dimensional stacking data structure
stack_pointer:                  DEFW 0      ; Pointer to stack data
dimensional_stack_counts:       DEFB 0,0,0,0,0,0,0,0 ; Stack counts per artifact type
dimensional_stack_limits:       DEFB 3,3,3,3,3,3,3,3 ; Stack limits per artifact type
dimensional_stack_bonuses:      DEFB 0,0,0,0,0,0,0,0 ; Stack bonuses per artifact type
stack_bonus_values:             DEFB $06,$08,$05,$07,$0A,$09,$0C,$0F ; Bonus values data
dimensional_total_enhancement:  DEFB 0,0,0,0,0,0,0,0 ; Total enhancement per artifact type

rare_artifact_data:             DEFS 16     ; Rare artifact data structure
rare_artifact_pointer:          DEFW 0      ; Pointer to rare artifact data
rare_artifact_positions:        DEFB $35,$18 ; Rare artifact positions
rare_artifact_types:            DEFB 9      ; Rare artifact types
rare_artifact_flags:            DEFB 1      ; Rare artifact availability flags
```

**Run it again** and experience advanced dimensional artifact stacking systems with rare convergence artifacts!

## Visual Progress Check

After running your enhanced code, you should see:

- Your original dimensional mastery world with puzzle completion as the foundation
- Collectible dimensional artifacts scattered throughout the dimensional maze
- Dimensional enhancement indicators showing active power-ups and their durations
- Attribute-based enhancement effects applied to colors, patterns, and dimensional properties
- Combo bonuses for collecting multiple dimensional artifacts in sequence
- Spectacular visual feedback for dimensional artifact pickups and enhancement activation

**Take a screenshot!** Your dimensional world now has complete dimensional artifact systems.

## What You've Accomplished

In this lesson, you've:

✅ **Created dimensional artifact collection systems** with multiple enhancement types using data structures  
✅ **Learned data structures** through artifact storage and dimensional enhancement organization  
✅ **Built dimensional enhancement tracking systems** with duration timers and attribute multipliers  
✅ **Used structured data organization** for organized artifact information storage and retrieval  
✅ **Used pointer management** for efficient dimensional data structure access and manipulation  
✅ **Used systematic processing** for complex dimensional artifact interaction and combo systems

More importantly, you've experienced **creative Spectrum enhancement programming** - creating sophisticated dimensional artifact systems with organized data structures and spectacular attribute-based enhancement mechanics.

## Dimensional Artifact Game Architecture

**Why This Approach Matters**:

Your dimensional artifact system demonstrates professional dimensional enhancement programming:

- **Structured data organization** using organized layouts for scalable dimensional enhancement systems
- **Efficient dimensional state tracking** with systematic data structure management for multiple enhancement types
- **Complex dimensional interaction systems** that handle temporary effects, stacking bonuses, and artifact combo mechanics
- **Balanced dimensional enhancement progression** providing meaningful but temporary dimensional advantage systems
- **Creative Spectrum presentation** with attribute manipulation dimensional effects and dimensional audio

These same data structure techniques enabled the sophisticated dimensional enhancement systems that made Spectrum games legendary for creative constraint-based enhancement mechanics and organized dimensional data management.

## Looking Ahead: Your Dimensional Journey

**Next lesson**, you'll add the final essential element - **complete dimensional game systems**! You'll learn:

- How to integrate all dimensional systems into a cohesive dimensional game experience
- Building complete dimensional gameplay loops with all mechanics working together
- Adding final dimensional polish and completion sequences
- Creating balanced dimensional difficulty progression through all systems

By **Lesson 8**, you'll have complete dimensional game mechanics with full puzzle mastery, progression, artifacts, and enhancement systems in a finished Spectrum dimensional experience.

## Ready to Continue?

Your dimensional world now manifests with incredible dimensional artifacts! Dimensional artifact collection and enhancement systems create the engaging dimensional empowerment that defines classic dimensional gaming advancement. The final lesson will integrate everything into a complete dimensional game experience.

But first, **collect dimensional artifacts in the dimensional artifact realm**. You've just implemented comprehensive dimensional enhancement systems using authentic Z80 data structures. This demonstrates the dimensional artifact gameplay programming that made Spectrum games famous for creative constraint-based enhancement mechanics and organized dimensional data management.

**Welcome to dimensional artifact programming!**

---

### Quick Reference: Today's Dimensional Artifact Programming Instructions

- **LD HL, structure**: Load HL with structure address (organized dimensional data access)
- **LD (pointer), HL**: Store structure pointer (efficient dimensional data management)
- **ADD HL, BC**: Add offset to structure pointer (indexed dimensional data access)
- **LDIR**: Copy data between structures (bulk dimensional data transfer)
- **DJNZ loop**: Decrement and loop through structures (systematic dimensional data processing)

Save this reference - you'll use these data structure techniques to manage all your dimensional artifact and enhancement systems!
