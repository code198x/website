---
title: "Crystal Cascade: Evolution Mastery"
system: "commodore-amiga"
phase_number: 1
tier_number: 1
lesson_number: 6
description: "Add evolutionary progression and mastery systems to Crystal Cascade! Create crystal evolution tracking and cascade mastery while learning subroutines and functions through modular progression and achievement architecture."
learning_objectives:
  - "Create evolutionary progression systems with crystal cascade mastery tracking"
  - "Add cascade evolution and crystal advancement with modular progression architecture"
  - "Learn subroutines and functions through modular progression and achievement system design"
  - "Build evolutionary rewards and mastery systems with organized function libraries"
  - "Experience professional-quality progression with responsive cascade evolution feedback"
concepts:
  - "Evolutionary progression programming with crystal cascade mastery systems"
  - "Subroutines and functions for modular progression and achievement architecture"
  - "Cascade evolution organization and systematic progression function design"
  - "Professional mastery mechanics with crystal evolution interaction systems"
  - "Scalable progression development with organized modular code architecture"
estimated_duration: "30-45 minutes"
difficulty: "easy"
code_examples: true
practical_exercise: true
order: 6
---

# Lesson 6: Crystal Cascade - Evolution Mastery

**Your crystal cascade needs evolutionary mastery!** In the last lesson, you added interactive gameplay and collision systems to your parallax world. Today, you'll create evolutionary progression and mastery systems - crystal evolution tracking, cascade advancement, and mastery rewards that transform your interactive showcase into an engaging evolutionary progression experience using professional modular architecture!

## Your Evolution Mastery Emerges

Let's start with the magic - adding evolutionary progression and mastery systems to your interactive crystal world:

```nasm
; Crystal Cascade - Evolution Mastery Creation
    SECTION code,CODE

start:
    ; Set up enhanced interactive crystal world from Lessons 1-5
    MOVE.L  #$00040000, A0      ; Background bitplane 0
    MOVE.L  #$00042000, A1      ; Middle layer bitplane 1
    MOVE.L  #$00044000, A2      ; Foreground bitplane 2

    ; Initialize interactive gameplay from previous lessons
    JSR     InitGameplaySystem
    JSR     CreateGameplayEntities
    JSR     InitCollisionSystem

    ; Initialize evolutionary mastery system
    JSR     InitEvolutionMastery

    ; Initialize crystal evolution tracking
    JSR     InitCrystalEvolution

    ; Set up cascade progression
    JSR     InitCascadeProgression

    ; Initialize evolutionary achievement system
    JSR     InitEvolutionAchievements

    ; Professional evolutionary game loop
main_loop:
    JSR     ProcessPlayerInput
    JSR     UpdateScrollPosition
    JSR     UpdateGameplayEntities
    JSR     ProcessCollisionDetection
    JSR     CheckEvolutionConditions
    JSR     UpdateCrystalEvolution
    JSR     CheckCascadeProgression
    JSR     UpdateEvolutionEffects
    JSR     UpdateParallaxLayers
    BRA.S   main_loop

; Initialize evolutionary mastery system using modular subroutines
InitEvolutionMastery:
    ; Set up evolution state variables
    MOVE.W  #0, EvolutionFlag       ; No evolution achieved initially
    MOVE.W  #0, CascadeComplete     ; Cascade not complete
    MOVE.W  #0, MasteryComplete     ; Mastery not complete

    ; Initialize evolution conditions
    MOVE.W  #8, CrystalsRequired    ; Crystals required for evolution
    MOVE.W  #0, CrystalsEvolved     ; Crystals evolved this cascade

    ; Set evolution timing
    MOVE.W  #0, EvolutionTimer      ; Evolution sequence timer
    MOVE.W  #120, EvolutionDuration ; Evolution sequence duration (120 frames)

    ; Initialize evolution rewards using function calls
    JSR     InitEvolutionRewards

    RTS

; Initialize crystal evolution tracking with organized functions
InitCrystalEvolution:
    ; Set up crystal evolution variables using modular approach
    JSR     InitCrystalStates
    JSR     InitEvolutionTracking
    JSR     InitCrystalDifficulty
    JSR     InitEvolutionScoring

    RTS

; Modular crystal state initialization
InitCrystalStates:
    ; Clear crystal evolution flags using systematic approach
    MOVE.W  #0, Crystal1Evolved     ; Crystal 1 status
    MOVE.W  #0, Crystal2Evolved     ; Crystal 2 status
    MOVE.W  #0, Crystal3Evolved     ; Crystal 3 status
    MOVE.W  #0, Crystal4Evolved     ; Crystal 4 status
    MOVE.W  #0, Crystal5Evolved     ; Crystal 5 status
    MOVE.W  #0, Crystal6Evolved     ; Crystal 6 status
    MOVE.W  #0, Crystal7Evolved     ; Crystal 7 status
    MOVE.W  #0, Crystal8Evolved     ; Crystal 8 status

    RTS

; Modular evolution tracking initialization
InitEvolutionTracking:
    ; Initialize evolution tracking variables
    MOVE.W  #1, CurrentEvolutionLevel  ; Base evolution difficulty
    MOVE.W  #0, EvolutionAttempts      ; Evolution attempt counter

    ; Set evolution parameters using function organization
    JSR     SetEvolutionParameters

    RTS

; Organized evolution parameter setting
SetEvolutionParameters:
    ; Set evolution scoring
    MOVE.W  #15, CrystalPoints      ; Points per crystal evolved
    MOVE.W  #8, SpeedBonus          ; Bonus for quick evolution
    MOVE.W  #25, PerfectBonus       ; Bonus for perfect evolution

    ; Set evolution timing thresholds
    MOVE.W  #60, SpeedThreshold     ; Speed evolution threshold (60 frames)
    MOVE.W  #30, MasteryThreshold   ; Mastery evolution threshold (30 frames)

    RTS

; Initialize cascade progression system with modular architecture
InitCascadeProgression:
    ; Set up cascade variables using organized functions
    JSR     InitCascadeLevels
    JSR     InitDifficultyScaling
    JSR     InitCascadeRequirements

    RTS

; Modular cascade level initialization
InitCascadeLevels:
    ; Initialize cascade level variables
    MOVE.W  #1, CurrentCascade      ; Starting cascade
    MOVE.W  #5, MaxCascades         ; Maximum cascade levels

    RTS

; Organized difficulty scaling initialization
InitDifficultyScaling:
    ; Initialize cascade scaling using systematic approach
    MOVE.W  #4, CrystalComplexity   ; Crystals per cascade
    MOVE.W  #3, EvolutionSpeed      ; Evolution speed base
    MOVE.W  #6, CascadePatterns     ; Patterns per cascade

    RTS

; Modular cascade requirements setup
InitCascadeRequirements:
    ; Set cascade requirements using function organization
    MOVE.W  #8, CascadeRequirement  ; Crystals to evolve per cascade

    RTS

; Initialize evolutionary achievement system with professional architecture
InitEvolutionAchievements:
    ; Set up achievement flags using modular approach
    JSR     InitAchievementFlags
    JSR     InitAchievementThresholds
    JSR     InitAchievementRewards

    RTS

; Organized achievement flag initialization
InitAchievementFlags:
    ; No achievements unlocked initially
    MOVE.W  #0, FirstEvolution      ; First crystal evolution achievement
    MOVE.W  #0, PerfectCascade      ; Perfect cascade achievement (no misses)
    MOVE.W  #0, SpeedMaster         ; Speed evolution achievement
    MOVE.W  #0, EvolutionMaster     ; Evolution mastery achievement

    RTS

; Modular achievement threshold setup
InitAchievementThresholds:
    ; Initialize achievement thresholds using systematic approach
    MOVE.W  #80, SpeedMasteryThreshold  ; Speed mastery threshold (80 points)
    MOVE.W  #20, EvolutionMasteryThreshold ; Evolution mastery threshold (20 crystals)

    RTS

; Organized achievement reward initialization
InitAchievementRewards:
    ; Set achievement rewards using function organization
    MOVE.W  #12, AchievementCrystalBonus ; Achievement crystal bonus
    MOVE.W  #18, AchievementPointBonus   ; Achievement point bonus

    RTS

; Initialize evolution reward system using modular functions
InitEvolutionRewards:
    ; Set up evolution reward variables using organized approach
    MOVE.W  #20, EvolutionCrystalBonus  ; Crystal evolution bonus
    MOVE.W  #15, EvolutionEnergyBonus   ; Energy evolution bonus

    RTS

; Check evolution conditions using sophisticated function architecture
CheckEvolutionConditions:
    ; Check if evolution flag already set using modular approach
    JSR     CheckEvolutionInProgress
    TST.W   D0                      ; Test return value
    BNE     evolution_sequence      ; Branch if evolution in progress

    ; Check primary evolution condition using organized functions
    JSR     CheckPrimaryEvolutionCondition
    TST.W   D0                      ; Test return value
    BNE     trigger_cascade_evolution ; Branch if primary condition met

    ; Check alternative evolution conditions using modular checking
    JSR     CheckAlternativeEvolutionConditions
    TST.W   D0                      ; Test return value
    BNE     trigger_bonus_evolution ; Branch if alternative condition met

    ; Check mastery evolution condition using function organization
    JSR     CheckMasteryEvolutionCondition
    TST.W   D0                      ; Test return value
    BNE     trigger_mastery_evolution ; Branch if mastery condition met

    BRA     evolution_check_done

trigger_cascade_evolution:
    ; Primary evolution achieved using organized functions
    JSR     TriggerCascadeEvolution
    BRA     evolution_check_done

trigger_bonus_evolution:
    ; Bonus evolution achieved using modular approach
    JSR     TriggerBonusEvolution
    BRA     evolution_check_done

trigger_mastery_evolution:
    ; Mastery evolution achieved using function organization
    JSR     TriggerMasteryEvolution

evolution_sequence:
    ; Handle evolution sequence using systematic approach
    JSR     UpdateEvolutionSequence

evolution_check_done:
    RTS

; Modular evolution condition checking functions
CheckEvolutionInProgress:
    ; Check if evolution flag is set using organized approach
    MOVE.W  EvolutionFlag, D0       ; Load evolution flag
    CMP.W   #0, D0                  ; Compare with zero
    BEQ     no_evolution_in_progress ; Branch if no evolution

    MOVE.W  #1, D0                  ; Return true (evolution in progress)
    RTS

no_evolution_in_progress:
    MOVE.W  #0, D0                  ; Return false (no evolution)
    RTS

; Organized primary evolution condition checking
CheckPrimaryEvolutionCondition:
    ; Check crystals evolved using modular function
    MOVE.W  CrystalsEvolved, D0     ; Load crystals evolved
    MOVE.W  CrystalsRequired, D1    ; Load required count
    CMP.W   D1, D0                  ; Compare required with evolved
    BLT     primary_condition_not_met ; Branch if not enough crystals evolved

    MOVE.W  #1, D0                  ; Return true (primary condition met)
    RTS

primary_condition_not_met:
    MOVE.W  #0, D0                  ; Return false (condition not met)
    RTS

; Systematic alternative evolution condition checking
CheckAlternativeEvolutionConditions:
    ; Check high energy threshold using function organization
    JSR     CheckHighEnergyThreshold
    TST.W   D0                      ; Test return value
    BNE     alternative_condition_met ; Branch if high energy achieved

    ; Check survival evolution condition using modular approach
    JSR     CheckSurvivalEvolutionCondition
    TST.W   D0                      ; Test return value
    BNE     alternative_condition_met ; Branch if survival condition met

    MOVE.W  #0, D0                  ; Return false (no alternative conditions met)
    RTS

alternative_condition_met:
    MOVE.W  #1, D0                  ; Return true (alternative condition met)
    RTS

; Organized high energy threshold checking
CheckHighEnergyThreshold:
    ; Check if energy threshold reached using systematic approach
    MOVE.W  PlayerEnergy, D0        ; Load player energy
    CMP.W   #80, D0                 ; Compare with high energy threshold
    BLT     high_energy_not_met     ; Branch if energy too low

    MOVE.W  #1, D0                  ; Return true (high energy achieved)
    RTS

high_energy_not_met:
    MOVE.W  #0, D0                  ; Return false (energy not high enough)
    RTS

; Modular survival evolution condition checking
CheckSurvivalEvolutionCondition:
    ; Check survival time using function organization
    MOVE.W  GameTime, D0            ; Load game time (simplified timer)
    CMP.W   #300, D0                ; Compare with survival threshold (300 frames)
    BLT     survival_not_met        ; Branch if not survived long enough

    ; Check if minimal damage taken using organized approach
    MOVE.W  PlayerHealth, D0        ; Load current health
    CMP.W   #90, D0                 ; Compare with survival health threshold
    BLT     survival_not_met        ; Branch if too much damage taken

    MOVE.W  #1, D0                  ; Return true (survival condition met)
    RTS

survival_not_met:
    MOVE.W  #0, D0                  ; Return false (survival condition not met)
    RTS

; Systematic mastery evolution condition checking
CheckMasteryEvolutionCondition:
    ; Check mastery score using function organization
    MOVE.W  EvolutionScore, D0      ; Load evolution score
    CMP.W   #100, D0                ; Compare with mastery threshold
    BLT     mastery_not_met         ; Branch if score too low

    ; Check perfect evolution record using modular approach
    JSR     CheckPerfectEvolutionRecord
    TST.W   D0                      ; Test return value
    BEQ     mastery_not_met         ; Branch if not perfect record

    MOVE.W  #1, D0                  ; Return true (mastery condition met)
    RTS

mastery_not_met:
    MOVE.W  #0, D0                  ; Return false (mastery condition not met)
    RTS

; Organized perfect evolution record checking
CheckPerfectEvolutionRecord:
    ; Check if all evolutions were perfect using systematic approach
    MOVE.W  EvolutionAttempts, D0   ; Load total attempts
    MOVE.W  CrystalsEvolved, D1     ; Load crystals evolved
    CMP.W   D1, D0                  ; Compare crystals evolved with attempts
    BNE     perfect_record_not_met  ; Branch if attempts don't match evolutions

    MOVE.W  #1, D0                  ; Return true (perfect record)
    RTS

perfect_record_not_met:
    MOVE.W  #0, D0                  ; Return false (not perfect record)
    RTS

; Trigger cascade evolution using modular function architecture
TriggerCascadeEvolution:
    ; Set evolution flags using organized approach
    JSR     SetEvolutionFlags

    ; Award base evolution rewards using function organization
    JSR     AwardEvolutionRewards

    ; Check for perfect cascade bonus using modular checking
    JSR     CheckForPerfectCascadeBonus

    ; Check for speed evolution bonus using systematic approach
    JSR     CheckForSpeedEvolutionBonus

    ; Initialize evolution visual effects using function organization
    JSR     InitializeEvolutionEffects

    RTS

; Organized evolution flag setting
SetEvolutionFlags:
    ; Set evolution flags using systematic approach
    MOVE.W  #1, EvolutionFlag       ; Set evolution flag
    MOVE.W  #1, CascadeComplete     ; Set cascade complete flag

    RTS

; Modular evolution reward awarding
AwardEvolutionRewards:
    ; Award base crystal reward using function organization
    JSR     AwardBaseCrystalReward

    ; Award base energy reward using organized approach
    JSR     AwardBaseEnergyReward

    ; Award base score bonus using systematic functions
    JSR     AwardBaseScoreBonus

    RTS

; Systematic base reward functions
AwardBaseCrystalReward:
    ; Award base crystal reward using organized approach
    MOVE.W  PlayerCrystals, D0      ; Load current crystals
    MOVE.W  EvolutionCrystalBonus, D1 ; Load crystal bonus
    ADD.W   D1, D0                  ; Add bonus
    CMP.W   #100, D0                ; Check maximum crystals
    BLE     store_crystal_reward    ; Branch if within limit
    MOVE.W  #100, D0                ; Clamp to maximum

store_crystal_reward:
    MOVE.W  D0, PlayerCrystals      ; Store updated crystals

    RTS

AwardBaseEnergyReward:
    ; Award base energy reward using function organization
    MOVE.W  PlayerEnergy, D0        ; Load current energy
    MOVE.W  EvolutionEnergyBonus, D1 ; Load energy bonus
    ADD.W   D1, D0                  ; Add bonus
    CMP.W   #100, D0                ; Check maximum energy
    BLE     store_energy_reward     ; Branch if within limit
    MOVE.W  #100, D0                ; Clamp to maximum

store_energy_reward:
    MOVE.W  D0, PlayerEnergy        ; Store updated energy

    RTS

AwardBaseScoreBonus:
    ; Award base score bonus using organized approach
    MOVE.W  CrystalPoints, D0       ; Load crystal points
    JSR     AwardEvolutionPoints    ; Award points using modular function

    RTS

; Organized perfect cascade bonus checking
CheckForPerfectCascadeBonus:
    ; Check if perfect cascade achieved using modular approach
    JSR     CheckPerfectEvolutionRecord
    TST.W   D0                      ; Test return value
    BEQ     no_perfect_bonus        ; Branch if not perfect

    ; Perfect cascade achieved using function organization
    JSR     AwardPerfectCascadeBonus
    JSR     SetPerfectCascadeAchievement

no_perfect_bonus:
    RTS

; Systematic perfect cascade bonus awarding
AwardPerfectCascadeBonus:
    ; Double evolution bonus for perfect cascade using organized approach
    MOVE.W  EvolutionCrystalBonus, D0 ; Load base bonus
    LSL.W   #1, D0                  ; Double it (shift left)
    MOVE.W  PlayerCrystals, D1      ; Load current crystals
    ADD.W   D0, D1                  ; Add bonus
    CMP.W   #100, D1                ; Check maximum
    BLE     store_perfect_crystals  ; Branch if within limit
    MOVE.W  #100, D1                ; Clamp to maximum

store_perfect_crystals:
    MOVE.W  D1, PlayerCrystals      ; Store updated crystals

    ; Award achievement crystal bonus using function organization
    MOVE.W  AchievementCrystalBonus, D0 ; Load achievement bonus
    JSR     AwardEvolutionPoints    ; Award points using modular function

    RTS

; Organized perfect cascade achievement setting
SetPerfectCascadeAchievement:
    ; Set perfect cascade achievement using systematic approach
    MOVE.W  #1, PerfectCascade      ; Set achievement flag

    RTS

; Modular speed evolution bonus checking
CheckForSpeedEvolutionBonus:
    ; Check for speed evolution bonus using function organization
    MOVE.W  EvolutionTimer, D0      ; Load completion time
    MOVE.W  SpeedThreshold, D1      ; Load speed threshold
    CMP.W   D1, D0                  ; Compare threshold with time
    BGT     no_speed_bonus          ; Branch if too slow

    ; Speed evolution achieved using organized approach
    JSR     AwardSpeedEvolutionBonus
    JSR     SetSpeedMasterAchievement

no_speed_bonus:
    RTS

; Systematic speed evolution bonus awarding
AwardSpeedEvolutionBonus:
    ; Triple evolution bonus for speed completion using function organization
    MOVE.W  EvolutionCrystalBonus, D0 ; Load base bonus
    MOVE.W  D0, D1                  ; Copy to D1
    LSL.W   #1, D0                  ; Double it
    ADD.W   D1, D0                  ; Add base again (triple total)
    MOVE.W  PlayerCrystals, D1      ; Load current crystals
    ADD.W   D0, D1                  ; Add bonus
    CMP.W   #100, D1                ; Check maximum
    BLE     store_speed_crystals    ; Branch if within limit
    MOVE.W  #100, D1                ; Clamp to maximum

store_speed_crystals:
    MOVE.W  D1, PlayerCrystals      ; Store updated crystals

    RTS

; Organized speed master achievement setting
SetSpeedMasterAchievement:
    ; Set speed master achievement using systematic approach
    MOVE.W  #1, SpeedMaster         ; Set achievement flag

    RTS

; Initialize evolution visual effects using function organization
InitializeEvolutionEffects:
    ; Initialize evolution visual effects using modular approach
    MOVE.W  EvolutionDuration, D0   ; Load evolution duration
    MOVE.W  D0, EvolutionTimer      ; Set evolution timer

    ; Play evolution sound using organized functions
    JSR     PlayEvolutionSound

    RTS

; Trigger bonus evolution using organized architecture
TriggerBonusEvolution:
    ; Set evolution flags using modular approach
    JSR     SetEvolutionFlags

    ; Award high energy achievement using function organization
    JSR     AwardHighEnergyAchievement

    ; Award bonus rewards using systematic approach
    JSR     AwardBonusEvolutionRewards

    ; Initialize evolution effects using organized functions
    JSR     InitializeEvolutionEffects

    RTS

; Systematic bonus evolution reward awarding
AwardBonusEvolutionRewards:
    ; Massive crystal bonus for high energy achievement using function organization
    MOVE.W  EvolutionCrystalBonus, D0 ; Load base bonus
    LSL.W   #2, D0                  ; Quadruple it (shift left twice)
    MOVE.W  PlayerCrystals, D1      ; Load current crystals
    ADD.W   D0, D1                  ; Add bonus
    CMP.W   #100, D1                ; Check maximum
    BLE     store_bonus_crystals    ; Branch if within limit
    MOVE.W  #100, D1                ; Clamp to maximum

store_bonus_crystals:
    MOVE.W  D1, PlayerCrystals      ; Store updated crystals

    ; Award achievement point bonus using organized approach
    MOVE.W  AchievementPointBonus, D0 ; Load achievement bonus
    JSR     AwardEvolutionPoints    ; Award points using modular function

    RTS

; Organized high energy achievement awarding
AwardHighEnergyAchievement:
    ; Set high energy achievement using systematic approach
    MOVE.W  #1, FirstEvolution      ; Set achievement flag

    RTS

; Trigger mastery evolution using modular architecture
TriggerMasteryEvolution:
    ; Set mastery flags using organized approach
    JSR     SetMasteryFlags

    ; Award mastery rewards using function organization
    JSR     AwardMasteryEvolutionRewards

    ; Initialize mastery effects using systematic approach
    JSR     InitializeMasteryEffects

    RTS

; Systematic mastery flag setting
SetMasteryFlags:
    ; Set mastery flags using organized approach
    MOVE.W  #1, EvolutionFlag       ; Set evolution flag
    MOVE.W  #1, MasteryComplete     ; Set mastery complete flag

    RTS

; Organized mastery evolution reward awarding
AwardMasteryEvolutionRewards:
    ; Award mastery crystal bonus using function organization
    MOVE.W  EvolutionCrystalBonus, D0 ; Load base bonus
    LSL.W   #3, D0                  ; Octuple it (shift left three times)
    MOVE.W  PlayerCrystals, D1      ; Load current crystals
    ADD.W   D0, D1                  ; Add bonus
    CMP.W   #100, D1                ; Check maximum
    BLE     store_mastery_crystals  ; Branch if within limit
    MOVE.W  #100, D1                ; Clamp to maximum

store_mastery_crystals:
    MOVE.W  D1, PlayerCrystals      ; Store updated crystals

    ; Set evolution master achievement using organized approach
    MOVE.W  #1, EvolutionMaster     ; Set mastery achievement

    RTS

; Initialize mastery effects using function organization
InitializeMasteryEffects:
    ; Extended evolution sequence for mastery using systematic approach
    MOVE.W  #180, EvolutionTimer    ; Extended evolution duration (180 frames)

    ; Play mastery evolution sound using organized functions
    JSR     PlayMasteryEvolutionSound

    RTS

; Update evolution sequence using modular architecture
UpdateEvolutionSequence:
    ; Check if evolution sequence active using organized approach
    JSR     CheckEvolutionSequenceActive
    TST.W   D0                      ; Test return value
    BEQ     evolution_sequence_done ; Branch if sequence finished

    ; Update evolution timer using function organization
    JSR     UpdateEvolutionTimer

    ; Create evolution visual effects using systematic approach
    JSR     CreateEvolutionVisualEffects

    BRA     evolution_sequence_continue

evolution_sequence_done:
    ; Evolution sequence finished using organized functions
    JSR     RestoreNormalEffects

evolution_sequence_continue:
    RTS

; Organized evolution sequence activity checking
CheckEvolutionSequenceActive:
    ; Check if evolution sequence is active using systematic approach
    MOVE.W  EvolutionTimer, D0      ; Load evolution timer
    CMP.W   #0, D0                  ; Compare with zero
    BEQ     evolution_sequence_inactive ; Branch if finished

    MOVE.W  #1, D0                  ; Return true (sequence active)
    RTS

evolution_sequence_inactive:
    MOVE.W  #0, D0                  ; Return false (sequence finished)
    RTS

; Modular evolution timer updating
UpdateEvolutionTimer:
    ; Update evolution timer using organized approach
    MOVE.W  EvolutionTimer, D0      ; Load evolution timer
    SUB.W   #1, D0                  ; Decrement timer
    MOVE.W  D0, EvolutionTimer      ; Store updated timer

    RTS

; Systematic evolution visual effects creation
CreateEvolutionVisualEffects:
    ; Create evolution visual effects based on timer using function organization
    JSR     CalculateEvolutionPhase

    ; Apply visual effects based on phase using modular approach
    CMP.W   #1, D0                  ; Check if phase 1
    BEQ     apply_phase_1_effects   ; Branch for phase 1
    CMP.W   #2, D0                  ; Check if phase 2
    BEQ     apply_phase_2_effects   ; Branch for phase 2
    CMP.W   #3, D0                  ; Check if phase 3
    BEQ     apply_phase_3_effects   ; Branch for phase 3

    RTS

apply_phase_1_effects:
    ; First phase - bright crystal flashing using organized approach
    JSR     ApplyBrightCrystalFlashing
    RTS

apply_phase_2_effects:
    ; Second phase - cascade color cycling using function organization
    JSR     ApplyCascadeColorCycling
    RTS

apply_phase_3_effects:
    ; Third phase - spectacular evolution finale using systematic approach
    JSR     ApplyEvolutionFinale
    RTS

; Organized evolution phase calculation
CalculateEvolutionPhase:
    ; Calculate current evolution phase using systematic approach
    MOVE.W  EvolutionTimer, D0      ; Load evolution timer
    MOVE.W  EvolutionDuration, D1   ; Load total duration
    MOVE.W  D1, D2                  ; Copy duration
    DIVS    #3, D2                  ; Divide by 3 for phase calculation

    ; Determine phase based on remaining time using function organization
    CMP.W   D2, D0                  ; Compare with phase 3 threshold
    BLE     evolution_phase_3       ; Branch if phase 3
    LSL.W   #1, D2                  ; Double threshold for phase 2
    CMP.W   D2, D0                  ; Compare with phase 2 threshold
    BLE     evolution_phase_2       ; Branch if phase 2

    MOVE.W  #1, D0                  ; Return phase 1
    RTS

evolution_phase_2:
    MOVE.W  #2, D0                  ; Return phase 2
    RTS

evolution_phase_3:
    MOVE.W  #3, D0                  ; Return phase 3
    RTS

; Modular visual effect application functions
ApplyBrightCrystalFlashing:
    ; Apply bright crystal flashing effects using organized approach
    MOVE.W  EvolutionTimer, D0      ; Load timer
    AND.W   #$0008, D0              ; Mask for flashing
    BEQ     crystal_flash_bright    ; Branch for bright frame

    ; Dim frame using systematic approach
    MOVE.W  #$0222, $DFF180         ; Dim crystal colors
    RTS

crystal_flash_bright:
    ; Bright frame using function organization
    MOVE.W  #$0FFF, $DFF180         ; Bright crystal colors
    RTS

ApplyCascadeColorCycling:
    ; Apply cascade color cycling using organized approach
    MOVE.W  EvolutionTimer, D0      ; Load timer
    AND.W   #$000F, D0              ; Mask for color cycle
    LSL.W   #4, D0                  ; Shift for color value
    ADD.W   #$0400, D0              ; Offset colors
    MOVE.W  D0, $DFF180             ; Apply cycling colors

    RTS

ApplyEvolutionFinale:
    ; Apply spectacular evolution finale using function organization
    MOVE.W  EvolutionTimer, D0      ; Load timer
    AND.W   #$0007, D0              ; Mask for finale effect
    LSL.W   #8, D0                  ; Shift for finale colors
    ADD.W   #$0800, D0              ; Offset finale colors
    MOVE.W  D0, $DFF180             ; Apply finale effects

    RTS

; Systematic normal effects restoration
RestoreNormalEffects:
    ; Restore normal visual effects using organized approach
    MOVE.W  #$0456, $DFF180         ; Normal crystal colors

    RTS

; Sound effect functions using modular architecture
PlayEvolutionSound:
    ; Play evolution sound using organized approach
    MOVE.W  #$0100, $DFF096         ; DMA control
    MOVE.L  #EvolutionSoundData, $DFF0A0 ; Audio pointer
    MOVE.W  #100, $DFF0A4           ; Audio length
    MOVE.W  #300, $DFF0A6           ; Audio period
    MOVE.W  #64, $DFF0A8            ; Audio volume
    MOVE.W  #$8001, $DFF096         ; Enable audio DMA

    RTS

PlayMasteryEvolutionSound:
    ; Play mastery evolution fanfare using function organization
    MOVE.W  #$0100, $DFF096         ; DMA control
    MOVE.L  #MasteryEvolutionSoundData, $DFF0A0 ; Audio pointer
    MOVE.W  #200, $DFF0A4           ; Audio length
    MOVE.W  #200, $DFF0A6           ; Audio period
    MOVE.W  #64, $DFF0A8            ; Audio volume
    MOVE.W  #$8001, $DFF096         ; Enable audio DMA

    RTS

; Organized evolution point awarding
AwardEvolutionPoints:
    ; A register contains points to award using systematic approach
    ; Add to total evolution score (simplified)
    MOVE.W  D0, D1                  ; Store points in D1
    MOVE.W  EvolutionScore, D0      ; Load current score
    ADD.W   D1, D0                  ; Add new points
    MOVE.W  D0, EvolutionScore      ; Store updated score

    ; Check for evolution score milestones using function organization
    CMP.W   #100, D0                ; Check for 100 point milestone
    BLT     score_award_done        ; Branch if below milestone

    ; Milestone reached using organized approach
    JSR     AwardMilestoneBonus

score_award_done:
    RTS

; Systematic milestone bonus awarding
AwardMilestoneBonus:
    ; Award milestone bonus using function organization
    MOVE.W  #20, D0                 ; Milestone bonus amount
    MOVE.W  PlayerCrystals, D1      ; Load current crystals
    ADD.W   D0, D1                  ; Add bonus
    CMP.W   #100, D1                ; Check maximum
    BLE     store_milestone_crystals ; Branch if within limit
    MOVE.W  #100, D1                ; Clamp to maximum

store_milestone_crystals:
    MOVE.W  D1, PlayerCrystals      ; Store updated crystals

    RTS

; Data storage for evolution mastery system using organized architecture
    SECTION data,DATA

; Evolution system variables
EvolutionFlag:              DC.W    0
CascadeComplete:            DC.W    0
MasteryComplete:            DC.W    0
CrystalsRequired:           DC.W    8
CrystalsEvolved:            DC.W    0
EvolutionTimer:             DC.W    0
EvolutionDuration:          DC.W    120
EvolutionCrystalBonus:      DC.W    20
EvolutionEnergyBonus:       DC.W    15

; Crystal evolution tracking
Crystal1Evolved:            DC.W    0
Crystal2Evolved:            DC.W    0
Crystal3Evolved:            DC.W    0
Crystal4Evolved:            DC.W    0
Crystal5Evolved:            DC.W    0
Crystal6Evolved:            DC.W    0
Crystal7Evolved:            DC.W    0
Crystal8Evolved:            DC.W    0
CurrentEvolutionLevel:      DC.W    1
EvolutionAttempts:          DC.W    0
CrystalPoints:              DC.W    15
SpeedBonus:                 DC.W    8
PerfectBonus:               DC.W    25
SpeedThreshold:             DC.W    60
MasteryThreshold:           DC.W    30

; Cascade progression variables
CurrentCascade:             DC.W    1
MaxCascades:                DC.W    5
CrystalComplexity:          DC.W    4
EvolutionSpeed:             DC.W    3
CascadePatterns:            DC.W    6
CascadeRequirement:         DC.W    8

; Achievement system variables
FirstEvolution:             DC.W    0
PerfectCascade:             DC.W    0
SpeedMaster:                DC.W    0
EvolutionMaster:            DC.W    0
SpeedMasteryThreshold:      DC.W    80
EvolutionMasteryThreshold:  DC.W    20
AchievementCrystalBonus:    DC.W    12
AchievementPointBonus:      DC.W    18

; Game state variables
PlayerCrystals:             DC.W    50
PlayerEnergy:               DC.W    100
PlayerHealth:               DC.W    100
GameTime:                   DC.W    0
EvolutionScore:             DC.W    0

; Sound data (simplified)
EvolutionSoundData:         DC.B    $80,$40,$20,$10,$08,$04,$02,$01
MasteryEvolutionSoundData:  DC.B    $FF,$80,$60,$40,$30,$20,$18,$10,$0C,$08,$06,$04,$03,$02,$01,$00
```

## Run It Right Now!

**Don't wait** - let's see your evolution mastery in action:

```bash
# Save as crystal6.adf, then:
# Load into Amiga emulator (WinUAE, FS-UAE, etc.)
# You should see: Crystal evolution mastery with professional modular progression!
```

**You should see**: Your interactive crystal world from Lessons 1-5, plus comprehensive evolutionary progression with crystal evolution mastery, cascade advancement, achievement systems, and spectacular evolution celebration sequences using professional modular architecture that creates engaging progression gameplay!

## What Just Happened? The Magic Behind Evolutionary Mastery Systems

Now that you've experienced evolutionary mastery, let's understand how **subroutines and functions** orchestrated this sophisticated modular progression system.

### Subroutines and Functions: Your Evolutionary Mastery Architecture

The most important thing to understand: **subroutines and functions enable sophisticated modular programming and organized system architecture**. In your evolutionary mastery code:

**Modular System Organization**: Organized initialization with function separation

```nasm
InitEvolutionMastery:
    JSR     InitEvolutionRewards    ; Organized reward initialization
    RTS

InitCrystalEvolution:
    JSR     InitCrystalStates       ; Modular state setup
    JSR     InitEvolutionTracking   ; Systematic tracking setup
    JSR     InitCrystalDifficulty   ; Organized difficulty setup
    RTS
```

**Systematic Condition Checking**: Multiple condition paths with organized function calls

```nasm
CheckEvolutionConditions:
    JSR     CheckEvolutionInProgress ; Modular progress checking
    TST.W   D0                      ; Test return value
    BNE     evolution_sequence      ; Branch based on function result

    JSR     CheckPrimaryEvolutionCondition ; Organized condition checking
    TST.W   D0                      ; Test return value
```

**Evolutionary Mastery Examples Using Subroutines and Functions:**

| Technique                  | Example                       | Use Case                       |
| -------------------------- | ----------------------------- | ------------------------------ |
| **Modular Initialization** | `JSR InitCrystalStates`       | Organized system setup         |
| **Function Return Values** | `TST.W D0 / BNE result`       | Systematic condition checking  |
| **Organized Architecture** | `JSR Award... / JSR Check...` | Professional code organization |

### Why Subroutines and Functions Matter for Evolutionary Systems

**Organized System Architecture** - Essential for complex progression systems:

```nasm
TriggerCascadeEvolution:
    JSR     SetEvolutionFlags           ; Organized flag management
    JSR     AwardEvolutionRewards       ; Modular reward system
    JSR     CheckForPerfectCascadeBonus ; Systematic bonus checking
    JSR     InitializeEvolutionEffects  ; Professional effect setup
    RTS
```

**Modular Reward Systems** - Handles organized bonus distribution:

```nasm
AwardEvolutionRewards:
    JSR     AwardBaseCrystalReward  ; Modular crystal awarding
    JSR     AwardBaseEnergyReward   ; Organized energy distribution
    JSR     AwardBaseScoreBonus     ; Systematic score management
    RTS
```

**Professional Code Organization** - Scales with systematic function design:

```nasm
CheckEvolutionConditions:
    JSR     CheckPrimaryEvolutionCondition    ; Primary condition function
    JSR     CheckAlternativeEvolutionConditions ; Alternative condition function
    JSR     CheckMasteryEvolutionCondition    ; Mastery condition function
```

## Practice: Create Advanced Evolutionary Mechanics

Let's enhance your evolutionary system with advanced modular features:

```nasm
    ; Create multi-tier evolutionary achievement system using organized architecture
    JSR     InitAdvancedEvolutionaryAchievements

InitAdvancedEvolutionaryAchievements:
    ; Set up tier-based evolutionary achievements using modular approach
    JSR     InitEvolutionaryTierFlags
    JSR     InitEvolutionaryTierThresholds
    JSR     InitEvolutionaryTierRewards
    RTS

InitEvolutionaryTierFlags:
    ; No tiers unlocked initially using systematic approach
    MOVE.W  #0, EvolutionaryTier1   ; Basic evolutionary achievements
    MOVE.W  #0, EvolutionaryTier2   ; Advanced evolutionary achievements
    MOVE.W  #0, EvolutionaryTier3   ; Master evolutionary achievements
    RTS

InitEvolutionaryTierThresholds:
    ; Initialize tier requirements using function organization
    MOVE.W  #8, EvolutionaryTier1Threshold  ; Tier 1 requirement
    MOVE.W  #25, EvolutionaryTier2Threshold ; Tier 2 requirement
    MOVE.W  #60, EvolutionaryTier3Threshold ; Tier 3 requirement
    RTS

InitEvolutionaryTierRewards:
    ; Set tier rewards using organized approach
    MOVE.W  #15, EvolutionaryTier1Reward    ; Tier 1 crystal reward
    MOVE.W  #35, EvolutionaryTier2Reward    ; Tier 2 crystal reward
    MOVE.W  #75, EvolutionaryTier3Reward    ; Tier 3 crystal reward
    RTS

CheckEvolutionaryAchievementTiers:
    ; Check current evolution score for tier progression using modular functions
    JSR     CheckEvolutionaryTier1
    JSR     CheckEvolutionaryTier2
    JSR     CheckEvolutionaryTier3
    RTS

CheckEvolutionaryTier1:
    ; Check tier 1 progression using organized approach
    MOVE.W  EvolutionScore, D0      ; Load evolution score
    MOVE.W  EvolutionaryTier1Threshold, D1 ; Load tier 1 threshold
    CMP.W   D1, D0                  ; Compare with current score
    BLT     tier_1_check_done       ; Branch if below tier 1

    ; Check if already unlocked using systematic approach
    MOVE.W  EvolutionaryTier1, D0   ; Check if already unlocked
    CMP.W   #0, D0                  ; Compare with zero
    BNE     tier_1_check_done       ; Branch if already unlocked

    ; Unlock tier 1 using function organization
    JSR     UnlockEvolutionaryTier1

tier_1_check_done:
    RTS

UnlockEvolutionaryTier1:
    ; Unlock tier 1 using organized approach
    MOVE.W  #1, EvolutionaryTier1   ; Set tier 1 unlocked
    JSR     AwardEvolutionaryTier1Bonus ; Award tier 1 bonus
    RTS

AwardEvolutionaryTier1Bonus:
    ; Award tier 1 bonus using systematic approach
    MOVE.W  EvolutionaryTier1Reward, D0 ; Load tier 1 reward
    MOVE.W  PlayerCrystals, D1      ; Load current crystals
    ADD.W   D0, D1                  ; Add reward
    CMP.W   #100, D1                ; Check maximum
    BLE     store_tier_1_crystals   ; Branch if within limit
    MOVE.W  #100, D1                ; Clamp to maximum

store_tier_1_crystals:
    MOVE.W  D1, PlayerCrystals      ; Store updated crystals
    RTS

; Additional tier variables
EvolutionaryTier1:           DC.W    0
EvolutionaryTier2:           DC.W    0
EvolutionaryTier3:           DC.W    0
EvolutionaryTier1Threshold:  DC.W    8
EvolutionaryTier2Threshold:  DC.W    25
EvolutionaryTier3Threshold:  DC.W    60
EvolutionaryTier1Reward:     DC.W    15
EvolutionaryTier2Reward:     DC.W    35
EvolutionaryTier3Reward:     DC.W    75
```

**Run it again** and experience advanced multi-tier evolutionary achievement systems with professional modular architecture!

## Visual Progress Check

After running your enhanced code, you should see:

- Your original interactive crystal world with collision systems as the foundation
- Crystal evolution mastery tracking with organized progression conditions
- Cascade advancement with increasing evolutionary complexity
- Achievement notifications and tier unlocking systems using modular architecture
- Spectacular evolution celebration sequences with professional visual effects
- Satisfying mastery sequences with visual and audio evolution celebration

**Take a screenshot!** Your crystal world now has complete evolutionary progression with professional modular architecture.

## What You've Accomplished

In this lesson, you've:

✅ **Created evolutionary progression systems** with multiple mastery paths using subroutines and functions  
✅ **Learned subroutines and functions** through modular progression and achievement system architecture  
✅ **Built crystal evolution tracking systems** with organized scoring and achievement bonuses  
✅ **Used modular programming** for organized system initialization and management  
✅ **Used function organization** for systematic condition checking and reward distribution  
✅ **Used professional architecture** for scalable progression and achievement systems

More importantly, you've experienced **professional Amiga progression programming** - creating sophisticated evolutionary mastery systems with organized modular architecture and systematic function design.

## Evolutionary Mastery Game Architecture

**Why This Approach Matters**:

Your evolutionary mastery system demonstrates professional game programming architecture:

- **Modular system organization** using subroutines for scalable progression systems
- **Systematic function design** with organized condition checking and reward distribution
- **Professional code architecture** that enables complex evolutionary progression mechanics
- **Organized achievement systems** providing structured long-term engagement goals
- **Professional Amiga presentation** with hardware-accelerated evolution effects and audio

These same modular programming techniques enabled the sophisticated progression systems that made Amiga games legendary for professional-quality advancement mechanics and organized code architecture.

## Looking Ahead: Your Evolutionary Journey

**Next lesson**, you'll add the most essential element - **crystal power-ups and enhancements**! You'll learn:

- How to create collectible crystal artifacts and evolutionary upgrades
- Building temporary and permanent enhancement systems using modular architecture
- Adding visual crystal upgrade effects and enhancement feedback
- Creating balanced evolutionary progression through organized upgrade mechanics

By **Lesson 8**, you'll have complete evolutionary game mechanics with full crystal mastery, progression, power-ups, and victory conditions in a finished Amiga evolutionary experience using professional modular architecture.

## Ready to Continue?

Your crystal world now evolves with sophisticated mastery systems! Crystal evolution progression and achievement systems create the engaging advancement that defines professional evolutionary gaming. The next lesson will add crystal power-ups to create complete enhancement experiences using organized modular architecture.

But first, **achieve mastery in the crystal cascade realm**. You've just implemented comprehensive evolutionary progression systems using professional 68000 subroutines and functions. This demonstrates the modular progression gameplay programming that made Amiga games famous for sophisticated advancement mechanics and organized code architecture.

**Welcome to evolutionary mastery programming with professional modular architecture!**

---

### Quick Reference: Today's Evolutionary Programming Instructions

- **JSR label**: Jump to Subroutine (call organized function for modular programming)
- **RTS**: Return from Subroutine (return from function call)
- **TST.W reg**: Test register (check function return values)
- **BEQ/BNE label**: Branch based on function results (systematic condition handling)
- **MOVE.W #value, variable**: Store values using organized data management

Save this reference - you'll use these subroutine and function techniques to create all your modular evolutionary systems and professional code architecture!
