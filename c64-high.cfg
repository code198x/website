MEMORY {
    ZP: start = $0002, size = $001A, type = rw, define = yes;
    LOADADDR: start = $0000, size = $0002, type = ro;
    RAM: start = $c000, size = $3000, type = rw, define = yes;
}

SEGMENTS {
    LOADADDR: load = LOADADDR, type = ro;
    CODE: load = RAM, type = ro, start = $c000;
}

FILES {
    %O: format = bin;
}