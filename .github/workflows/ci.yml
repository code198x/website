name: CI Pipeline (Simplified)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  checks: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Essential Quality Checks
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Check code formatting
        run: npm run format:check

      - name: TypeScript check
        run: npx astro check

      - name: Run unit tests
        run: npm run test:run

      - name: Build site
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-output
          path: dist/
          retention-days: 7

  # Optional: Visual Tests (can fail without blocking)
  visual-tests:
    name: Visual Tests
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Build site
        run: npm run build

      - name: Start preview server
        run: npm run preview &
        env:
          PORT: 4321

      - name: Wait for server
        run: npx wait-on http://localhost:4321 --timeout 30000

      - name: Run visual tests (Chromium only)
        run: npm run test:visual:chromium

      - name: Upload visual test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: visual-test-results
          path: |
            playwright-report/
            test-results/
          retention-days: 3

  # Optional: Security Scan (basic)
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: npm audit --audit-level=moderate

  # Test Summary
  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [build-and-test, visual-tests, security]
    if: always()
    steps:
      - name: Check results
        run: |
          echo "Build & Test: ${{ needs.build-and-test.result }}"
          echo "Visual Tests: ${{ needs.visual-tests.result }}"
          echo "Security: ${{ needs.security.result }}"

          if [ "${{ needs.build-and-test.result }}" != "success" ]; then
            echo "❌ Essential tests failed"
            exit 1
          fi

          echo "✅ Essential tests passed"

      - name: Generate summary
        run: |
          echo "# CI Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.build-and-test.result }}" = "success" ]; then
            echo "✅ **Build & Test**: PASSED" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Build & Test**: FAILED" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ needs.visual-tests.result }}" = "success" ]; then
            echo "✅ **Visual Tests**: PASSED" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.visual-tests.result }}" = "failure" ]; then
            echo "⚠️ **Visual Tests**: FAILED (non-blocking)" >> $GITHUB_STEP_SUMMARY
          else
            echo "⏭️ **Visual Tests**: SKIPPED" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ needs.security.result }}" = "success" ]; then
            echo "✅ **Security**: PASSED" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.security.result }}" = "failure" ]; then
            echo "⚠️ **Security**: ISSUES FOUND (non-blocking)" >> $GITHUB_STEP_SUMMARY
          else
            echo "⏭️ **Security**: SKIPPED" >> $GITHUB_STEP_SUMMARY
          fi
